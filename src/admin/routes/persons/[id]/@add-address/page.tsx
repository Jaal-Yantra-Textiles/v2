import { RouteFocusModal } from "../../../../components/modal/route-focus-modal";
import AddAddressForPerson from "../../../../components/persons/add-address-for-person";

const CreatePersonAddressModal = () => {
  return (
    <RouteFocusModal>
      <AddAddressForPerson />
    </RouteFocusModal>
  );
};

export default CreatePersonAddressModal;
