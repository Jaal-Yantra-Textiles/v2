{
  "bulk-resolve-attributions": {
    "fields": [
      {
        "name": "days_back",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "limit",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/attribution/bulk-resolve-attributions.ts"
  },
  "resolve-session-attribution": {
    "fields": [
      {
        "name": "session_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/attribution/resolve-session-attribution.ts"
  },
  "track-conversion": {
    "fields": [
      {
        "name": "visitor_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "conversion_value",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency }}"
      },
      {
        "name": "order_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.order_id }}"
      },
      {
        "name": "product_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.product_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "form_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.form_id }}"
      },
      {
        "name": "form_response_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.form_response_id }}"
      },
      {
        "name": "utm_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_source }}"
      },
      {
        "name": "utm_medium",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_medium }}"
      },
      {
        "name": "utm_campaign",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_campaign }}"
      },
      {
        "name": "utm_term",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_term }}"
      },
      {
        "name": "utm_content",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_content }}"
      },
      {
        "name": "landing_page",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.landing_page }}"
      },
      {
        "name": "conversion_page",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.conversion_page }}"
      },
      {
        "name": "custom_event_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.custom_event_name }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/conversions/track-conversion.ts"
  },
  "track-lead-conversion": {
    "fields": [
      {
        "name": "lead_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.lead_id }}"
      },
      {
        "name": "form_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.form_id }}"
      },
      {
        "name": "form_response_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.form_response_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "visitor_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "lead_value",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "utm_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_source }}"
      },
      {
        "name": "utm_medium",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_medium }}"
      },
      {
        "name": "utm_campaign",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_campaign }}"
      },
      {
        "name": "utm_term",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_term }}"
      },
      {
        "name": "utm_content",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_content }}"
      },
      {
        "name": "landing_page",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.landing_page }}"
      },
      {
        "name": "conversion_page",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.conversion_page }}"
      },
      {
        "name": "lead_data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/conversions/track-lead-conversion.ts"
  },
  "track-purchase-conversion": {
    "fields": [
      {
        "name": "order_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.order_id }}"
      },
      {
        "name": "customer_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.customer_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "visitor_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/conversions/track-purchase-conversion.ts"
  },
  "calculate-churn-risk": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/predictive/calculate-churn-risk.ts"
  },
  "calculate-clv": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/predictive/calculate-clv.ts"
  },
  "calculate-engagement": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "recalculate",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/scoring/calculate-engagement.ts"
  },
  "calculate-nps": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "rating",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "scale",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.scale }}"
      },
      {
        "name": "source_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.source_id }}"
      },
      {
        "name": "source_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.source_type }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/scoring/calculate-nps.ts"
  },
  "build-segment": {
    "fields": [
      {
        "name": "segment_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.segment_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/segments/build-segment.ts"
  },
  "analyze-sentiment": {
    "fields": [
      {
        "name": "text",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.text }}"
      },
      {
        "name": "source_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.source_type }}"
      },
      {
        "name": "source_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.source_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ad-planning/sentiment/analyze-sentiment.ts"
  },
  "create-agreement-response": {
    "fields": [
      {
        "name": "agreement_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.agreement_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "sent_at",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.sent_at }}"
      },
      {
        "name": "viewed_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.viewed_at }}"
      },
      {
        "name": "responded_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.responded_at }}"
      },
      {
        "name": "agreed",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "response_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_notes }}"
      },
      {
        "name": "email_sent_to",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.email_sent_to }}"
      },
      {
        "name": "email_opened",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "email_opened_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.email_opened_at }}"
      },
      {
        "name": "response_ip",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_ip }}"
      },
      {
        "name": "response_user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_user_agent }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/create-agreement-response.ts"
  },
  "create-agreement": {
    "fields": [
      {
        "name": "title",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "content",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.content }}"
      },
      {
        "name": "template_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.template_key }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "valid_from",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.valid_from }}"
      },
      {
        "name": "valid_until",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.valid_until }}"
      },
      {
        "name": "subject",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.subject }}"
      },
      {
        "name": "from_email",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.from_email }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/create-agreement.ts"
  },
  "delete-agreement-response": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/delete-agreement-response.ts"
  },
  "delete-agreement": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/delete-agreement.ts"
  },
  "fetch-agreement-for-web": {
    "fields": [
      {
        "name": "response_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.response_id }}"
      },
      {
        "name": "access_token",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.access_token }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/fetch-agreement-for-web.ts"
  },
  "list-agreement-response": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/list-agreement-response.ts"
  },
  "list-agreement": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/list-agreement.ts"
  },
  "send-agreement-email-multi": {
    "fields": [
      {
        "name": "agreement_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.agreement_id }}"
      },
      {
        "name": "person_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.person_ids }}"
      },
      {
        "name": "template_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.template_key }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/send-agreement-email-multi.ts"
  },
  "send-agreement-email": {
    "fields": [
      {
        "name": "agreement_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.agreement_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "person_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.person_ids }}"
      },
      {
        "name": "template_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.template_key }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/send-agreement-email.ts"
  },
  "track-agreement-view": {
    "fields": [
      {
        "name": "response_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.response_id }}"
      },
      {
        "name": "access_token",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.access_token }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/track-agreement-view.ts"
  },
  "update-agreement-response-web": {
    "fields": [
      {
        "name": "response_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.response_id }}"
      },
      {
        "name": "access_token",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.access_token }}"
      },
      {
        "name": "agreed",
        "type": "boolean",
        "required": true,
        "placeholder": "false"
      },
      {
        "name": "response_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_notes }}"
      },
      {
        "name": "response_ip",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_ip }}"
      },
      {
        "name": "response_user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_user_agent }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/update-agreement-response-web.ts"
  },
  "update-agreement-response": {
    "fields": [
      {
        "name": "response_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.response_id }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "agreed",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "responded_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.responded_at }}"
      },
      {
        "name": "viewed_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.viewed_at }}"
      },
      {
        "name": "response_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_notes }}"
      },
      {
        "name": "response_ip",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_ip }}"
      },
      {
        "name": "response_user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.response_user_agent }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/update-agreement-response.ts"
  },
  "update-agreement": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/agreements/update-agreement.ts"
  },
  "approve-id-extraction-batch": {
    "fields": [
      {
        "name": "batch_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.batch_id }}"
      },
      {
        "name": "item_ids",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.item_ids }}",
        "description": "Turning batch drafts into people — the human half of #1816. 🔴 This is a SEPARATE act on purpose. The reading workflow never creates a person, however confident the model sounded. In prod the same ID card read five times did not split the name identically (4x \"Tarun Debnath\", 1x \"Tarun\"); at one photo a time an operator catches that, at ten a time it seeds a roster with wrong names and nothing marks which ones. So the drafts sit until somebody says which are right. Partial success is the normal case and is reported per item, not thrown: approving eight of ten must not roll back the eight because the ninth had no surname. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, } from \"@medusajs/framework/workflows-sdk\"; import { ContainerRegistrationKeys, MedusaError } from \"@medusajs/framework/utils\"; import type { Link } from \"@medusajs/framework/modules-sdk\"; import { PERSON_MODULE } from \"../../modules/person\"; import { PARTNER_MODULE } from \"../../modules/partner\"; import { personCreateInputFromDraft, type PersonDraft, } from \"../../lib/people/id-card\"; export type ApproveIdExtractionBatchInput = { batch_id: string; /** Which items to approve. Omitted means every item that carries a usable draft — the \"accept the batch\" button."
      },
      {
        "name": "corrections",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "Turning batch drafts into people — the human half of #1816. 🔴 This is a SEPARATE act on purpose. The reading workflow never creates a person, however confident the model sounded. In prod the same ID card read five times did not split the name identically (4x \"Tarun Debnath\", 1x \"Tarun\"); at one photo a time an operator catches that, at ten a time it seeds a roster with wrong names and nothing marks which ones. So the drafts sit until somebody says which are right. Partial success is the normal case and is reported per item, not thrown: approving eight of ten must not roll back the eight because the ninth had no surname. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, } from \"@medusajs/framework/workflows-sdk\"; import { ContainerRegistrationKeys, MedusaError } from \"@medusajs/framework/utils\"; import type { Link } from \"@medusajs/framework/modules-sdk\"; import { PERSON_MODULE } from \"../../modules/person\"; import { PARTNER_MODULE } from \"../../modules/partner\"; import { personCreateInputFromDraft, type PersonDraft, } from \"../../lib/people/id-card\"; export type ApproveIdExtractionBatchInput = { batch_id: string; /** Which items to approve. Omitted means every item that carries a usable draft — the \"accept the batch\" button. / item_ids?: string[] | null; /** Edits keyed by item id, applied over the draft before the person is built. This is where an operator's correction of \"Tarun\" to \"Tarun Debnath\" lands."
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}",
        "description": "Turning batch drafts into people — the human half of #1816. 🔴 This is a SEPARATE act on purpose. The reading workflow never creates a person, however confident the model sounded. In prod the same ID card read five times did not split the name identically (4x \"Tarun Debnath\", 1x \"Tarun\"); at one photo a time an operator catches that, at ten a time it seeds a roster with wrong names and nothing marks which ones. So the drafts sit until somebody says which are right. Partial success is the normal case and is reported per item, not thrown: approving eight of ten must not roll back the eight because the ninth had no surname. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, } from \"@medusajs/framework/workflows-sdk\"; import { ContainerRegistrationKeys, MedusaError } from \"@medusajs/framework/utils\"; import type { Link } from \"@medusajs/framework/modules-sdk\"; import { PERSON_MODULE } from \"../../modules/person\"; import { PARTNER_MODULE } from \"../../modules/partner\"; import { personCreateInputFromDraft, type PersonDraft, } from \"../../lib/people/id-card\"; export type ApproveIdExtractionBatchInput = { batch_id: string; /** Which items to approve. Omitted means every item that carries a usable draft — the \"accept the batch\" button. / item_ids?: string[] | null; /** Edits keyed by item id, applied over the draft before the person is built. This is where an operator's correction of \"Tarun\" to \"Tarun Debnath\" lands. / corrections?: Record<string, Partial<PersonDraft>> | null; /** Guard against approving someone else's batch. Filled from auth."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/approve-id-extraction-batch.ts"
  },
  "describe-product-image": {
    "fields": [
      {
        "name": "imageUrl",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.imageUrl }}"
      },
      {
        "name": "hint",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.hint }}"
      },
      {
        "name": "providerRole",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.providerRole }}"
      },
      {
        "name": "providerNameContains",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.providerNameContains }}"
      },
      {
        "name": "system_prompt",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.system_prompt }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/describe-product-image.ts"
  },
  "extract-and-create-inventory": {
    "fields": [
      {
        "name": "data",
        "type": "object",
        "required": true,
        "description": "Fields for the new record",
        "placeholder": "{{ $last }}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/ai/extract-and-create-inventory.ts"
  },
  "generate-design-ai-image": {
    "fields": [
      {
        "name": "customer_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.customer_id }}"
      },
      {
        "name": "design_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "mode",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.mode }}"
      },
      {
        "name": "badges",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.badges }}"
      },
      {
        "name": "materials_prompt",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.materials_prompt }}"
      },
      {
        "name": "reference_images",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.reference_images }}"
      },
      {
        "name": "canvas_snapshot",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.canvas_snapshot }}"
      },
      {
        "name": "preview_cache_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.preview_cache_key }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/generate-design-image.ts"
  },
  "id-extraction-batch-processing": {
    "fields": [
      {
        "name": "image_urls",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.image_urls }}",
        "description": "Batch ID-card extraction as a long-running, rate-limited, resumable job (#1816). Asked by the founder: *\"if we upload like 10 photos can it handle this — maybe when many photos are uploaded it should run the long-running workflow and return them through rate limit and failure aware, with iteration to create the data out of it.\"* Today `add_person_from_id_card` is one image, one synchronous request, behind Cloudflare's 100s edge limit. Ten photos is ten of those, each its own chance to hit what #1813 hit — a 504 at 60s with the provider still retrying for another eleven minutes — and no record of which ones landed. The shape here is `textile-folder-extraction`'s, which was built for exactly this problem with photographs of cloth: 1. Trigger returns `202` + `transaction_id`; an async step suspends. 2. A confirm route resumes it; processing continues in the background. 3. One photo at a time on a configurable interval, so the provider is never hammered. 4. Per-item errors recorded, with a retry that re-runs only what failed. ## Two things this does differently, both deliberate 🔴 **It produces DRAFTS, never people.** The same ID card read five times in prod did not split the name identically — 4x \"Tarun Debnath\", 1x \"Tarun\". One photo at a time, an operator sees that and fixes it. Ten at a time, it seeds a roster with wrong names and nothing marks which. Approval is a separate, human act; see `approveIdExtractionBatchItemsWorkflow`. 🔑 **The work-list is a question about state, not a loop position.** Items carry their own `status`, so the processing step asks the database what is still `pending`. A deploy kills a long in-process loop without warning and leaves `status` saying `running` (#1742) — deriving the work from rows is what makes a resume finish the batch instead of re-doing it. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep } from \"@medusajs/medusa/core-flows\"; import { PERSON_MODULE } from \"../../modules/person\"; import { createPartnerNotification } from \"../../lib/notifications/create-partner-notification\"; import { type IdNumberPolicy } from \"../../lib/people/id-card\"; import { providerGateKey, resolveMaxConcurrency, withProviderSlot, } from \"../../lib/ai/provider-gate\"; import { extractPersonFromIdWorkflow } from \"./extract-person-from-id\"; // ============================================ // Types // ============================================ export type IdExtractionBatchInput = { /** The photographs, in the order the caller submitted them."
      },
      {
        "name": "batch_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.batch_id }}",
        "description": "Batch ID-card extraction as a long-running, rate-limited, resumable job (#1816). Asked by the founder: *\"if we upload like 10 photos can it handle this — maybe when many photos are uploaded it should run the long-running workflow and return them through rate limit and failure aware, with iteration to create the data out of it.\"* Today `add_person_from_id_card` is one image, one synchronous request, behind Cloudflare's 100s edge limit. Ten photos is ten of those, each its own chance to hit what #1813 hit — a 504 at 60s with the provider still retrying for another eleven minutes — and no record of which ones landed. The shape here is `textile-folder-extraction`'s, which was built for exactly this problem with photographs of cloth: 1. Trigger returns `202` + `transaction_id`; an async step suspends. 2. A confirm route resumes it; processing continues in the background. 3. One photo at a time on a configurable interval, so the provider is never hammered. 4. Per-item errors recorded, with a retry that re-runs only what failed. ## Two things this does differently, both deliberate 🔴 **It produces DRAFTS, never people.** The same ID card read five times in prod did not split the name identically — 4x \"Tarun Debnath\", 1x \"Tarun\". One photo at a time, an operator sees that and fixes it. Ten at a time, it seeds a roster with wrong names and nothing marks which. Approval is a separate, human act; see `approveIdExtractionBatchItemsWorkflow`. 🔑 **The work-list is a question about state, not a loop position.** Items carry their own `status`, so the processing step asks the database what is still `pending`. A deploy kills a long in-process loop without warning and leaves `status` saying `running` (#1742) — deriving the work from rows is what makes a resume finish the batch instead of re-doing it. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep } from \"@medusajs/medusa/core-flows\"; import { PERSON_MODULE } from \"../../modules/person\"; import { createPartnerNotification } from \"../../lib/notifications/create-partner-notification\"; import { type IdNumberPolicy } from \"../../lib/people/id-card\"; import { providerGateKey, resolveMaxConcurrency, withProviderSlot, } from \"../../lib/ai/provider-gate\"; import { extractPersonFromIdWorkflow } from \"./extract-person-from-id\"; // ============================================ // Types // ============================================ export type IdExtractionBatchInput = { /** The photographs, in the order the caller submitted them. */ image_urls?: string[]; /** Set by the confirm/retry paths to work an existing batch."
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "id_number_policy",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.id_number_policy }}"
      },
      {
        "name": "person_type_ids",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.person_type_ids }}"
      },
      {
        "name": "interval_ms",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Batch ID-card extraction as a long-running, rate-limited, resumable job (#1816). Asked by the founder: *\"if we upload like 10 photos can it handle this — maybe when many photos are uploaded it should run the long-running workflow and return them through rate limit and failure aware, with iteration to create the data out of it.\"* Today `add_person_from_id_card` is one image, one synchronous request, behind Cloudflare's 100s edge limit. Ten photos is ten of those, each its own chance to hit what #1813 hit — a 504 at 60s with the provider still retrying for another eleven minutes — and no record of which ones landed. The shape here is `textile-folder-extraction`'s, which was built for exactly this problem with photographs of cloth: 1. Trigger returns `202` + `transaction_id`; an async step suspends. 2. A confirm route resumes it; processing continues in the background. 3. One photo at a time on a configurable interval, so the provider is never hammered. 4. Per-item errors recorded, with a retry that re-runs only what failed. ## Two things this does differently, both deliberate 🔴 **It produces DRAFTS, never people.** The same ID card read five times in prod did not split the name identically — 4x \"Tarun Debnath\", 1x \"Tarun\". One photo at a time, an operator sees that and fixes it. Ten at a time, it seeds a roster with wrong names and nothing marks which. Approval is a separate, human act; see `approveIdExtractionBatchItemsWorkflow`. 🔑 **The work-list is a question about state, not a loop position.** Items carry their own `status`, so the processing step asks the database what is still `pending`. A deploy kills a long in-process loop without warning and leaves `status` saying `running` (#1742) — deriving the work from rows is what makes a resume finish the batch instead of re-doing it. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep } from \"@medusajs/medusa/core-flows\"; import { PERSON_MODULE } from \"../../modules/person\"; import { createPartnerNotification } from \"../../lib/notifications/create-partner-notification\"; import { type IdNumberPolicy } from \"../../lib/people/id-card\"; import { providerGateKey, resolveMaxConcurrency, withProviderSlot, } from \"../../lib/ai/provider-gate\"; import { extractPersonFromIdWorkflow } from \"./extract-person-from-id\"; // ============================================ // Types // ============================================ export type IdExtractionBatchInput = { /** The photographs, in the order the caller submitted them. */ image_urls?: string[]; /** Set by the confirm/retry paths to work an existing batch. */ batch_id?: string; partner_id?: string | null; notes?: string | null; id_number_policy?: IdNumberPolicy; person_type_ids?: string[] | null; /** Milliseconds between photos. Default 20000."
      },
      {
        "name": "scope",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.scope }}",
        "description": "Batch ID-card extraction as a long-running, rate-limited, resumable job (#1816). Asked by the founder: *\"if we upload like 10 photos can it handle this — maybe when many photos are uploaded it should run the long-running workflow and return them through rate limit and failure aware, with iteration to create the data out of it.\"* Today `add_person_from_id_card` is one image, one synchronous request, behind Cloudflare's 100s edge limit. Ten photos is ten of those, each its own chance to hit what #1813 hit — a 504 at 60s with the provider still retrying for another eleven minutes — and no record of which ones landed. The shape here is `textile-folder-extraction`'s, which was built for exactly this problem with photographs of cloth: 1. Trigger returns `202` + `transaction_id`; an async step suspends. 2. A confirm route resumes it; processing continues in the background. 3. One photo at a time on a configurable interval, so the provider is never hammered. 4. Per-item errors recorded, with a retry that re-runs only what failed. ## Two things this does differently, both deliberate 🔴 **It produces DRAFTS, never people.** The same ID card read five times in prod did not split the name identically — 4x \"Tarun Debnath\", 1x \"Tarun\". One photo at a time, an operator sees that and fixes it. Ten at a time, it seeds a roster with wrong names and nothing marks which. Approval is a separate, human act; see `approveIdExtractionBatchItemsWorkflow`. 🔑 **The work-list is a question about state, not a loop position.** Items carry their own `status`, so the processing step asks the database what is still `pending`. A deploy kills a long in-process loop without warning and leaves `status` saying `running` (#1742) — deriving the work from rows is what makes a resume finish the batch instead of re-doing it. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep } from \"@medusajs/medusa/core-flows\"; import { PERSON_MODULE } from \"../../modules/person\"; import { createPartnerNotification } from \"../../lib/notifications/create-partner-notification\"; import { type IdNumberPolicy } from \"../../lib/people/id-card\"; import { providerGateKey, resolveMaxConcurrency, withProviderSlot, } from \"../../lib/ai/provider-gate\"; import { extractPersonFromIdWorkflow } from \"./extract-person-from-id\"; // ============================================ // Types // ============================================ export type IdExtractionBatchInput = { /** The photographs, in the order the caller submitted them. */ image_urls?: string[]; /** Set by the confirm/retry paths to work an existing batch. */ batch_id?: string; partner_id?: string | null; notes?: string | null; id_number_policy?: IdNumberPolicy; person_type_ids?: string[] | null; /** Milliseconds between photos. Default 20000. */ interval_ms?: number; /** Which items this run is for. - `\"pending\"` (default) — everything not yet read. Makes a re-run a RESUME. - `\"failed\"` — only the ones that failed, which is what the retry route wants."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/id-extraction-batch.ts"
  },
  "image-extraction-medusa": {
    "fields": [
      {
        "name": "image_url",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.image_url }}"
      },
      {
        "name": "entity_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.entity_type }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "threadId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.threadId }}"
      },
      {
        "name": "resourceId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.resourceId }}"
      },
      {
        "name": "defaults",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/image-extraction.ts"
  },
  "textile-folder-extraction-processing": {
    "fields": [
      {
        "name": "folder_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.folder_id }}"
      },
      {
        "name": "hints",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.hints }}"
      },
      {
        "name": "gender",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.gender }}"
      },
      {
        "name": "persist",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "interval_ms",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Medusa Long-Running Workflow for Folder-Wide Textile Feature Extraction Extracts features from EVERY image in a media folder as one long-running, rate-limited background job: 1. Trigger API returns transaction_id (202 status) 2. Async step suspends workflow until confirmed 3. Confirm API resumes workflow 4. Processing runs in the background, one photo at a time with a configurable interval (default: 1 photo per minute) so the AI provider is never rate limited. 5. Progress is mirrored to the folder's metadata (folder_extraction) so the admin UI can poll status while the job runs. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep, sendNotificationsStep } from \"@medusajs/medusa/core-flows\"; import { MEDIA_MODULE } from \"../../modules/media\"; import MediaService from \"../../modules/media/service\"; import { runTextileMastraExtraction, persistTextileExtractionResult, TextileProductExtractionOutput, } from \"./textile-product-extraction\"; import { pendingFolderExtractionMedia } from \"./lib/folder-extraction-resume\"; // ============================================ // Types // ============================================ export type TextileFolderExtractionInput = { folder_id: string; hints?: string[]; gender?: \"female\" | \"male\" | \"unisex\"; persist?: boolean; /** Milliseconds to wait between photos. Default 60000 (1 photo/minute)."
      },
      {
        "name": "media_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.media_ids }}",
        "description": "Medusa Long-Running Workflow for Folder-Wide Textile Feature Extraction Extracts features from EVERY image in a media folder as one long-running, rate-limited background job: 1. Trigger API returns transaction_id (202 status) 2. Async step suspends workflow until confirmed 3. Confirm API resumes workflow 4. Processing runs in the background, one photo at a time with a configurable interval (default: 1 photo per minute) so the AI provider is never rate limited. 5. Progress is mirrored to the folder's metadata (folder_extraction) so the admin UI can poll status while the job runs. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep, sendNotificationsStep } from \"@medusajs/medusa/core-flows\"; import { MEDIA_MODULE } from \"../../modules/media\"; import MediaService from \"../../modules/media/service\"; import { runTextileMastraExtraction, persistTextileExtractionResult, TextileProductExtractionOutput, } from \"./textile-product-extraction\"; import { pendingFolderExtractionMedia } from \"./lib/folder-extraction-resume\"; // ============================================ // Types // ============================================ export type TextileFolderExtractionInput = { folder_id: string; hints?: string[]; gender?: \"female\" | \"male\" | \"unisex\"; persist?: boolean; /** Milliseconds to wait between photos. Default 60000 (1 photo/minute). */ interval_ms?: number; /** When set, only these media files are extracted (instead of every image in the folder). Used by the \"retry failed\" endpoint to re-run just the files that failed a previous folder extraction."
      },
      {
        "name": "scope",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.scope }}",
        "description": "Medusa Long-Running Workflow for Folder-Wide Textile Feature Extraction Extracts features from EVERY image in a media folder as one long-running, rate-limited background job: 1. Trigger API returns transaction_id (202 status) 2. Async step suspends workflow until confirmed 3. Confirm API resumes workflow 4. Processing runs in the background, one photo at a time with a configurable interval (default: 1 photo per minute) so the AI provider is never rate limited. 5. Progress is mirrored to the folder's metadata (folder_extraction) so the admin UI can poll status while the job runs. / import { createStep, createWorkflow, StepResponse, WorkflowResponse, WorkflowData, transform, } from \"@medusajs/framework/workflows-sdk\"; import { MedusaError } from \"@medusajs/framework/utils\"; import { notifyOnFailureStep, sendNotificationsStep } from \"@medusajs/medusa/core-flows\"; import { MEDIA_MODULE } from \"../../modules/media\"; import MediaService from \"../../modules/media/service\"; import { runTextileMastraExtraction, persistTextileExtractionResult, TextileProductExtractionOutput, } from \"./textile-product-extraction\"; import { pendingFolderExtractionMedia } from \"./lib/folder-extraction-resume\"; // ============================================ // Types // ============================================ export type TextileFolderExtractionInput = { folder_id: string; hints?: string[]; gender?: \"female\" | \"male\" | \"unisex\"; persist?: boolean; /** Milliseconds to wait between photos. Default 60000 (1 photo/minute). */ interval_ms?: number; /** When set, only these media files are extracted (instead of every image in the folder). Used by the \"retry failed\" endpoint to re-run just the files that failed a previous folder extraction. / media_ids?: string[]; /** Which images in the folder this run is for (#1742). - `\"all\"` (default) — every image, the original behaviour. - `\"pending\"` — only images with no `textile_analysis` row yet, which is what makes a re-run a RESUME rather than a full re-do. This is the same question `getAllUngeocodedAddressesStep` asks for the geocode backfill: derive the work from state and running twice becomes harmless. ⚠️ Applied AFTER `media_ids`, so the two compose: an explicit list is narrowed to the ones still outstanding rather than overriding the scope."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/textile-folder-extraction.ts"
  },
  "textile-extraction-processing": {
    "fields": [
      {
        "name": "media_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.media_id }}"
      },
      {
        "name": "image_url",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.image_url }}"
      },
      {
        "name": "hints",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.hints }}"
      },
      {
        "name": "gender",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.gender }}"
      },
      {
        "name": "persist",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "threadId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.threadId }}"
      },
      {
        "name": "resourceId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.resourceId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/textile-product-extraction.ts"
  },
  "try-on-garment": {
    "fields": [
      {
        "name": "customer_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.customer_id }}"
      },
      {
        "name": "garment_image_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.garment_image_url }}"
      },
      {
        "name": "garment_image_base64",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.garment_image_base64 }}"
      },
      {
        "name": "face_image_base64",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.face_image_base64 }}"
      },
      {
        "name": "cloth_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.cloth_type }}"
      },
      {
        "name": "gender",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.gender }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/ai/try-on-garment.ts"
  },
  "create-analytics-event": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "event_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.event_type }}"
      },
      {
        "name": "event_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.event_name }}"
      },
      {
        "name": "pathname",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.pathname }}"
      },
      {
        "name": "referrer",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer }}"
      },
      {
        "name": "referrer_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer_source }}"
      },
      {
        "name": "visitor_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.user_agent }}"
      },
      {
        "name": "browser",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.browser }}"
      },
      {
        "name": "os",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.os }}"
      },
      {
        "name": "device_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.device_type }}"
      },
      {
        "name": "country",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.country }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "timestamp",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.timestamp }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/create-analytics-event.ts"
  },
  "delete-analytics-event": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/delete-analytics-event.ts"
  },
  "get-partner-storefront-digest": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "period",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.period }}"
      },
      {
        "name": "thresholds",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.thresholds }}"
      },
      {
        "name": "now",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.now }}",
        "description": "Compute a per-partner storefront analytics digest (#581 S1). Given a partner id + period it resolves the partner's website, runs the #569 overview for the current AND previous windows (for deltas), pulls the #559 breakdowns the digest surfaces, and assembles a digest JSON with rule-based suggestions (pure helpers in `partner-digest-lib.ts`). Per-partner scoping: a partner's analytics = their website's events. The website is resolved from the partner's `website_id` column (metadata fallback), then `storefront_domain` — mirroring `api/partners/storefront/helpers.ts#getPartnerWebsite`. / export type GetPartnerStorefrontDigestInput = { partner_id: string; period?: DigestPeriod; thresholds?: Partial<DigestThresholds>; /** ISO timestamp; injectable for deterministic runs. Defaults to now."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/get-partner-storefront-digest.ts"
  },
  "get-website-analytics-overview": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "days",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "from",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.from }}"
      },
      {
        "name": "to",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.to }}"
      },
      {
        "name": "utm_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_source }}"
      },
      {
        "name": "utm_medium",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_medium }}"
      },
      {
        "name": "utm_campaign",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_campaign }}"
      },
      {
        "name": "pathname",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pathname }}"
      },
      {
        "name": "qr_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.qr_key }}"
      },
      {
        "name": "qr_value",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.qr_value }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/get-website-analytics-overview.ts"
  },
  "list-analytics-event": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/list-analytics-event.ts"
  },
  "get-analytics-breakdown": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "dimension",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.dimension }}"
      },
      {
        "name": "start_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.start_date }}"
      },
      {
        "name": "end_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.end_date }}"
      },
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "Get Analytics Breakdown (#559 slice 3) Granular single-dimension breakdown of a website's events, with composable equality filters and an optional date range. Complements the high-level getAnalyticsStatsWorkflow which returns a fixed set of aggregates. / export type GetAnalyticsBreakdownInput = { website_id: string; dimension: BreakdownDimension; start_date?: Date; end_date?: Date; /** Composable equality filters keyed by filterable field name."
      },
      {
        "name": "limit",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-analytics-breakdown.ts"
  },
  "get-analytics-stats": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "start_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.start_date }}"
      },
      {
        "name": "end_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.end_date }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-analytics-stats.ts"
  },
  "get-analytics-timeseries": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "start_date",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.start_date }}"
      },
      {
        "name": "end_date",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.end_date }}"
      },
      {
        "name": "interval",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.interval }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-analytics-timeseries.ts"
  },
  "get-outbound-links": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "start_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.start_date }}"
      },
      {
        "name": "end_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.end_date }}"
      },
      {
        "name": "limit",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-outbound-links.ts"
  },
  "get-search-console-analytics": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "days",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "from",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.from }}"
      },
      {
        "name": "to",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.to }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-search-console-analytics.ts"
  },
  "get-session-pages": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "start_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.start_date }}"
      },
      {
        "name": "end_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.end_date }}"
      },
      {
        "name": "dimensions",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.dimensions }}",
        "description": "Get Session Pages Breakdown (#569 S2) Ranked aggregation of a website's analytics sessions by their entry and/or exit page, over an optional date range. Complements the events breakdown (getAnalyticsBreakdownWorkflow) which groups raw events; here we group sessions so the admin can see \"top landing pages\" and \"top exit pages\". There is NO website→analytics_session module link, so sessions are resolved directly off the custom_analytics service (mirrors get-website-analytics- overview's S1 session fetch). The fetch is best-effort: a missing service/method or query error degrades to empty (zeroed) breakdowns rather than throwing. / export type GetSessionPagesInput = { website_id: string; start_date?: Date; end_date?: Date; /** Which page dimensions to compute. Defaults to both entry + exit."
      },
      {
        "name": "limit",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/reports/get-session-pages.ts"
  },
  "get-website-sessions": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/analytics/reports/get-website-sessions.ts"
  },
  "track-analytics-event": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "event_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.event_type }}"
      },
      {
        "name": "event_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.event_name }}"
      },
      {
        "name": "pathname",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.pathname }}"
      },
      {
        "name": "referrer",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer }}"
      },
      {
        "name": "visitor_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "user_agent",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.user_agent }}"
      },
      {
        "name": "ip_address",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.ip_address }}"
      },
      {
        "name": "timestamp",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.timestamp }}"
      },
      {
        "name": "client_event_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.client_event_id }}"
      },
      {
        "name": "country",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.country }}"
      },
      {
        "name": "timezone",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.timezone }}"
      },
      {
        "name": "locale",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.locale }}"
      },
      {
        "name": "query_string",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.query_string }}"
      },
      {
        "name": "is_404",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "utm_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_source }}"
      },
      {
        "name": "utm_medium",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_medium }}"
      },
      {
        "name": "utm_campaign",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_campaign }}"
      },
      {
        "name": "utm_term",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_term }}"
      },
      {
        "name": "utm_content",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.utm_content }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/track-analytics-event.ts"
  },
  "update-analytics-event": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "event_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.event_type }}"
      },
      {
        "name": "event_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.event_name }}"
      },
      {
        "name": "pathname",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pathname }}"
      },
      {
        "name": "referrer",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer }}"
      },
      {
        "name": "referrer_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer_source }}"
      },
      {
        "name": "visitor_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.visitor_id }}"
      },
      {
        "name": "session_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.session_id }}"
      },
      {
        "name": "user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.user_agent }}"
      },
      {
        "name": "browser",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.browser }}"
      },
      {
        "name": "os",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.os }}"
      },
      {
        "name": "device_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.device_type }}"
      },
      {
        "name": "country",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.country }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "timestamp",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.timestamp }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/analytics/update-analytics-event.ts"
  },
  "create-person": {
    "fields": [
      {
        "name": "first_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.first_name }}"
      },
      {
        "name": "last_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.last_name }}"
      },
      {
        "name": "email",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.email }}"
      },
      {
        "name": "date_of_birth",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.date_of_birth }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/create-person.ts"
  },
  "create-crm-opportunity": {
    "fields": [
      {
        "name": "title",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "stage",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.stage }}"
      },
      {
        "name": "amount",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.amount }}"
      },
      {
        "name": "currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency }}"
      },
      {
        "name": "expected_close_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.expected_close_date }}"
      },
      {
        "name": "company_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.company_id }}"
      },
      {
        "name": "owner_person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.owner_person_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/crm/create-opportunity.ts"
  },
  "create-crm-task": {
    "fields": [
      {
        "name": "title",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "due_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.due_date }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "priority",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.priority }}"
      },
      {
        "name": "assignee_person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.assignee_person_id }}"
      },
      {
        "name": "related_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.related_type }}"
      },
      {
        "name": "related_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.related_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/crm/create-task.ts"
  },
  "log-crm-note": {
    "fields": [
      {
        "name": "body",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.body }}"
      },
      {
        "name": "author",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.author }}"
      },
      {
        "name": "related_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.related_type }}"
      },
      {
        "name": "related_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.related_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/crm/log-note.ts"
  },
  "delete-person": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/delete-person.ts"
  },
  "close-design-inquiry": {
    "fields": [
      {
        "name": "inquiry_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.inquiry_id }}"
      },
      {
        "name": "chosen_partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.chosen_partner_id }}",
        "description": "Close an inquiry and take the brief back (#1531). 🔴 The revocation is the point, not a tidy-up. Being asked a question granted a partner read access to an unreleased design; leaving that access in place means every partner ever shown a brief can still read it, forever. That is the #1496 shape — a quote that rendered on every partner's storefront because nothing revoked access — and it must not repeat on designs. Only links this flow created are withdrawn: a `prospect` link is one we granted for the asking, while any other role means the partner was already working on this design and must be left exactly as they were. / export type CloseDesignInquiryInput = { inquiry_id: string /** The partner who will make the sample. Their access is KEPT and promoted from prospect to maker — they are about to be sent work, and taking the brief away at the moment they win it would be absurd."
      },
      {
        "name": "chosen_role",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.chosen_role }}",
        "description": "Close an inquiry and take the brief back (#1531). 🔴 The revocation is the point, not a tidy-up. Being asked a question granted a partner read access to an unreleased design; leaving that access in place means every partner ever shown a brief can still read it, forever. That is the #1496 shape — a quote that rendered on every partner's storefront because nothing revoked access — and it must not repeat on designs. Only links this flow created are withdrawn: a `prospect` link is one we granted for the asking, while any other role means the partner was already working on this design and must be left exactly as they were. / export type CloseDesignInquiryInput = { inquiry_id: string /** The partner who will make the sample. Their access is KEPT and promoted from prospect to maker — they are about to be sent work, and taking the brief away at the moment they win it would be absurd. / chosen_partner_id?: string | null /** Role stamped on the chosen partner's link."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/design-inquiries/close-design-inquiry.ts"
  },
  "create-design-inquiry": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "partner_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.partner_ids }}"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "brief_note",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.brief_note }}"
      },
      {
        "name": "reference_media_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.reference_media_ids }}",
        "description": "Ask a set of partners what they can make for a design (#1531). One inquiry, many partners, one generated set of questions — so two partners' answers are comparable side by side, which is the thing a WhatsApp thread per partner could never give. / export type CreateDesignInquiryInput = { design_id: string partner_ids: string[] title?: string brief_note?: string /** media_file ids for the references being shown to every invited partner."
      },
      {
        "name": "categories",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.categories }}",
        "description": "Ask a set of partners what they can make for a design (#1531). One inquiry, many partners, one generated set of questions — so two partners' answers are comparable side by side, which is the thing a WhatsApp thread per partner could never give. / export type CreateDesignInquiryInput = { design_id: string partner_ids: string[] title?: string brief_note?: string /** media_file ids for the references being shown to every invited partner. */ reference_media_ids?: string[] /** Restrict the wizard to these spec categories. Absent = all of them."
      },
      {
        "name": "created_by",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.created_by }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/design-inquiries/create-design-inquiry.ts"
  },
  "cancel-partner-assignment": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "unlink",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/cancel-partner-assignment.ts"
  },
  "create-design-from-llm": {
    "fields": [
      {
        "name": "designPrompt",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designPrompt }}"
      },
      {
        "name": "existingValues",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/create-design-from-llm.ts"
  },
  "create-design": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "inspiration_sources",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "design_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.design_type }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "priority",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.priority }}"
      },
      {
        "name": "target_completion_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.target_completion_date }}"
      },
      {
        "name": "design_files",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "thumbnail_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.thumbnail_url }}"
      },
      {
        "name": "custom_sizes",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "color_palette",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "tags",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "estimated_cost",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "cost_currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.cost_currency }}"
      },
      {
        "name": "designer_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.designer_notes }}"
      },
      {
        "name": "feedback_history",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "media_files",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.media_files }}"
      },
      {
        "name": "moodboard",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "colors",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.colors }}"
      },
      {
        "name": "size_sets",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.size_sets }}"
      },
      {
        "name": "product_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.product_type }}",
        "description": "The garment this design is (#938) — \"trousers\", \"saree\". Normalised on write. When omitted, the workflow infers it; when given, it is treated as MANUAL and no model may overwrite it."
      },
      {
        "name": "concept_theme",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.concept_theme }}"
      },
      {
        "name": "aesthetic_keywords",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "origin_source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.origin_source }}"
      },
      {
        "name": "customer_id_for_link",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.customer_id_for_link }}"
      },
      {
        "name": "owner_partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.owner_partner_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/create-design.ts"
  },
  "create-draft-order-from-designs": {
    "fields": [
      {
        "name": "customer_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.customer_id }}",
        "description": "Who the order is for — or `null` for a draft with no buyer attached yet (#1817). Most designs carry no customer link, and a cart is perfectly able to acquire one later, at checkout or when the order is claimed."
      },
      {
        "name": "design_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.design_ids }}"
      },
      {
        "name": "currency_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency_code }}"
      },
      {
        "name": "price_overrides",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "override_currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.override_currency }}",
        "description": "Who the order is for — or `null` for a draft with no buyer attached yet (#1817). Most designs carry no customer link, and a cart is perfectly able to acquire one later, at checkout or when the order is claimed. / customer_id: string | null design_ids: string[] currency_code?: string price_overrides?: Record<string, number> /** Currency of price_overrides (e.g. \"inr\"). Defaults to store default."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/create-draft-order-from-designs.ts"
  },
  "create-product-from-design": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "estimated_cost",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "customer_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.customer_id }}",
        "description": "Create Product from Design Workflow Creates a purchasable product/variant from a customer's design. Strategy: 1. If design has a linked product → create variant on that product 2. If no linked product → create new product + variant The variant is linked to the design via design-variant-link, enabling production run creation when the order is placed. / type CreateProductFromDesignInput = { design_id: string; estimated_cost: number; /** Stamped onto the design↔variant link as provenance. Optional: the quote path mints a variant for work that has no buyer yet, which is the whole point of quoting it."
      },
      {
        "name": "currency_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency_code }}"
      },
      {
        "name": "made_to_order",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Create Product from Design Workflow Creates a purchasable product/variant from a customer's design. Strategy: 1. If design has a linked product → create variant on that product 2. If no linked product → create new product + variant The variant is linked to the design via design-variant-link, enabling production run creation when the order is placed. / type CreateProductFromDesignInput = { design_id: string; estimated_cost: number; /** Stamped onto the design↔variant link as provenance. Optional: the quote path mints a variant for work that has no buyer yet, which is the whole point of quoting it. / customer_id?: string; currency_code?: string; /** Make this a MADE-TO-ORDER variant: something that will be produced after it is bought, rather than something sitting on a shelf. Two things change, and both are required for the variant to be quotable: - `status: \"published\"` instead of `\"draft\"`. A draft product's variant is not purchasable, so an accepted quote would build a cart that cannot be completed — and acceptance is the step where that would first be noticed, by the buyer. - `manage_inventory: false`. The whole premise is that the production run is in the FUTURE, so stock is zero and always will be until the run happens. A managed variant at zero stock refuses the order. Off by default: the approve route creates a catalogue product that is reviewed before it goes live, and that behaviour is unchanged."
      },
      {
        "name": "unit_price",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.unit_price }}",
        "description": "Create Product from Design Workflow Creates a purchasable product/variant from a customer's design. Strategy: 1. If design has a linked product → create variant on that product 2. If no linked product → create new product + variant The variant is linked to the design via design-variant-link, enabling production run creation when the order is placed. / type CreateProductFromDesignInput = { design_id: string; estimated_cost: number; /** Stamped onto the design↔variant link as provenance. Optional: the quote path mints a variant for work that has no buyer yet, which is the whole point of quoting it. / customer_id?: string; currency_code?: string; /** Make this a MADE-TO-ORDER variant: something that will be produced after it is bought, rather than something sitting on a shelf. Two things change, and both are required for the variant to be quotable: - `status: \"published\"` instead of `\"draft\"`. A draft product's variant is not purchasable, so an accepted quote would build a cart that cannot be completed — and acceptance is the step where that would first be noticed, by the buyer. - `manage_inventory: false`. The whole premise is that the production run is in the FUTURE, so stock is zero and always will be until the run happens. A managed variant at zero stock refuses the order. Off by default: the approve route creates a catalogue product that is reviewed before it goes live, and that behaviour is unchanged. / made_to_order?: boolean; /** The price to list, in major units and already in `currency_code`. Overrides `estimated_cost` when given. Both are now written verbatim — see `resolveListedPrice` for what changed and why."
      },
      {
        "name": "sales_channel_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.sales_channel_id }}",
        "description": "Create Product from Design Workflow Creates a purchasable product/variant from a customer's design. Strategy: 1. If design has a linked product → create variant on that product 2. If no linked product → create new product + variant The variant is linked to the design via design-variant-link, enabling production run creation when the order is placed. / type CreateProductFromDesignInput = { design_id: string; estimated_cost: number; /** Stamped onto the design↔variant link as provenance. Optional: the quote path mints a variant for work that has no buyer yet, which is the whole point of quoting it. / customer_id?: string; currency_code?: string; /** Make this a MADE-TO-ORDER variant: something that will be produced after it is bought, rather than something sitting on a shelf. Two things change, and both are required for the variant to be quotable: - `status: \"published\"` instead of `\"draft\"`. A draft product's variant is not purchasable, so an accepted quote would build a cart that cannot be completed — and acceptance is the step where that would first be noticed, by the buyer. - `manage_inventory: false`. The whole premise is that the production run is in the FUTURE, so stock is zero and always will be until the run happens. A managed variant at zero stock refuses the order. Off by default: the approve route creates a catalogue product that is reviewed before it goes live, and that behaviour is unchanged. / made_to_order?: boolean; /** The price to list, in major units and already in `currency_code`. Overrides `estimated_cost` when given. Both are now written verbatim — see `resolveListedPrice` for what changed and why. / unit_price?: number | null; /** The sales channel the new product belongs in — the catalogue of whoever is quoting or selling it. 🔴 Until this existed the branch below read `listStores({})[0]` and used THAT store's default channel. On a platform with 15 stores that is whichever row Postgres returned first, which in practice was always the core \"Default Sales Channel\" — so every made-to-order design product ever minted landed in a catalogue belonging to nobody who was quoting it. All 12 on production did. `assertVariantsInStore` then refused the mint, and the readiness preflight refused before that, which is how a whole feature could be shipped, tested and never once produce a quotable design. Omitted, the store default is still the fallback: the approve path creates a catalogue product for the core store and that behaviour is unchanged."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/create-product-from-design.ts"
  },
  "create-tasks-from-templates": {
    "fields": [
      {
        "name": "data",
        "type": "object",
        "required": true,
        "description": "Fields for the new record",
        "placeholder": "{{ $last }}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/designs/create-tasks-from-templates.ts"
  },
  "delete-design-task": {
    "fields": [
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/delete-design-task.ts"
  },
  "delete-design": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/delete-design.ts"
  },
  "estimate-design-cost": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "inventory_item_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.inventory_item_ids }}"
      },
      {
        "name": "production_cost_override",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.production_cost_override }}",
        "description": "How much the returned figure is worth believing. 🔴 `none` is not a weaker estimate — it is the ABSENCE of one, and it is the only value for which `total_estimated` is null. The estimator used to report \"I found nothing\" as `total_estimated: 0` with `confidence: \"estimated\"`, as though it had looked and got an answer. `0` is not a small number here; it is a claim that the work is free, and every downstream reader believed it — including the storefront checkout, which turned it into a customer-facing price of zero. #1564 / type ConfidenceLevel = \"exact\" | \"estimated\" | \"guesstimate\" | \"none\"; /** Which rung of the production-cost waterfall produced the figure. Surfaced so a reader can tell a settled price from a borrowed one without re-deriving the precedence. `sample_run` in particular is a real partner rate for a PROTOTYPE, which usually costs more per unit than a production batch — useful, but never to be presented as the production price. #1568 / export type ProductionCostSource = | \"partner_entered\" | \"actual_run\" /** A sample run's MEASURED consumption, stored on the design as `cost_breakdown.source === \"sample_consumption\"`. Distinct from `sample_run` and genuinely strong: it is what the sample actually used, not what the partner quoted for it — which is why that path alone keeps confidence \"exact\". / | \"sample_consumption\" | \"sample_run\" | \"admin_estimate\" | \"similar_designs\" /** Comparable HISTORY priced this design because nothing else could: what designs of this type have actually settled at, and what products previously created from designs actually sell for. 🔴 Opt-in per caller (`allow_historical_basis`), and never a settled price. `similar_designs` above is deliberately NOT a pricing signal — see `hasPricingSignal` — because pricing one design off another's number is the silent substitution #1554 was. This rung is that same evidence used DELIBERATELY, by a caller that has no bill of materials to work from and has asked for a starting figure anyway, and it is labelled and capped at \"guesstimate\" exactly like the sample-run fallback (#1568). The storefront checkout and draft-order paths do not pass the flag, so their behaviour is unchanged (#1564). / | \"historical_comparables\" | \"default_percent\"; export type MaterialCostItem = { inventory_item_id?: string; component_design_id?: string; name: string; cost: number; quantity: number; cost_source: \"order_history\" | \"unit_cost\" | \"component_design\" | \"consumption_log\" | \"estimated\" | \"default\"; /** Per-material commission (line cost × platform_fee_percent). Set when a fee applies. */ commission?: number; }; /** One piece of evidence about what this KIND of work has cost before. `unit_amount` is always per finished unit, in the design's cost currency — the caller normalises, because a total divided by the wrong quantity is the #1554 family and must not be re-derived in three places. / export type HistoricalComparable = { /** Design id or product id, so a reader can go and look at the evidence. */ id: string; name: string | null; unit_amount: number; source: \"design_estimate\" | \"product_price\"; }; type EstimateCostInput = { design_id: string; inventory_item_ids?: string[]; /** Partner-entered production cost per finished unit. When provided it is the authoritative production figure — it overrides every derived estimate (including the DEFAULT_PRODUCTION_PERCENT fallback). Partner recalc route."
      },
      {
        "name": "platform_fee_percent",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "How much the returned figure is worth believing. 🔴 `none` is not a weaker estimate — it is the ABSENCE of one, and it is the only value for which `total_estimated` is null. The estimator used to report \"I found nothing\" as `total_estimated: 0` with `confidence: \"estimated\"`, as though it had looked and got an answer. `0` is not a small number here; it is a claim that the work is free, and every downstream reader believed it — including the storefront checkout, which turned it into a customer-facing price of zero. #1564 / type ConfidenceLevel = \"exact\" | \"estimated\" | \"guesstimate\" | \"none\"; /** Which rung of the production-cost waterfall produced the figure. Surfaced so a reader can tell a settled price from a borrowed one without re-deriving the precedence. `sample_run` in particular is a real partner rate for a PROTOTYPE, which usually costs more per unit than a production batch — useful, but never to be presented as the production price. #1568 / export type ProductionCostSource = | \"partner_entered\" | \"actual_run\" /** A sample run's MEASURED consumption, stored on the design as `cost_breakdown.source === \"sample_consumption\"`. Distinct from `sample_run` and genuinely strong: it is what the sample actually used, not what the partner quoted for it — which is why that path alone keeps confidence \"exact\". / | \"sample_consumption\" | \"sample_run\" | \"admin_estimate\" | \"similar_designs\" /** Comparable HISTORY priced this design because nothing else could: what designs of this type have actually settled at, and what products previously created from designs actually sell for. 🔴 Opt-in per caller (`allow_historical_basis`), and never a settled price. `similar_designs` above is deliberately NOT a pricing signal — see `hasPricingSignal` — because pricing one design off another's number is the silent substitution #1554 was. This rung is that same evidence used DELIBERATELY, by a caller that has no bill of materials to work from and has asked for a starting figure anyway, and it is labelled and capped at \"guesstimate\" exactly like the sample-run fallback (#1568). The storefront checkout and draft-order paths do not pass the flag, so their behaviour is unchanged (#1564). / | \"historical_comparables\" | \"default_percent\"; export type MaterialCostItem = { inventory_item_id?: string; component_design_id?: string; name: string; cost: number; quantity: number; cost_source: \"order_history\" | \"unit_cost\" | \"component_design\" | \"consumption_log\" | \"estimated\" | \"default\"; /** Per-material commission (line cost × platform_fee_percent). Set when a fee applies. */ commission?: number; }; /** One piece of evidence about what this KIND of work has cost before. `unit_amount` is always per finished unit, in the design's cost currency — the caller normalises, because a total divided by the wrong quantity is the #1554 family and must not be re-derived in three places. / export type HistoricalComparable = { /** Design id or product id, so a reader can go and look at the evidence. */ id: string; name: string | null; unit_amount: number; source: \"design_estimate\" | \"product_price\"; }; type EstimateCostInput = { design_id: string; inventory_item_ids?: string[]; /** Partner-entered production cost per finished unit. When provided it is the authoritative production figure — it overrides every derived estimate (including the DEFAULT_PRODUCTION_PERCENT fallback). Partner recalc route. / production_cost_override?: number | null; /** JYT platform commission as a percentage of material cost. Opt-in per caller — defaults to 0 so store/admin/draft-order flows are unaffected; only the partner recalc route passes DEFAULT_PLATFORM_FEE_PERCENT."
      },
      {
        "name": "default_material_cost",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "How much the returned figure is worth believing. 🔴 `none` is not a weaker estimate — it is the ABSENCE of one, and it is the only value for which `total_estimated` is null. The estimator used to report \"I found nothing\" as `total_estimated: 0` with `confidence: \"estimated\"`, as though it had looked and got an answer. `0` is not a small number here; it is a claim that the work is free, and every downstream reader believed it — including the storefront checkout, which turned it into a customer-facing price of zero. #1564 / type ConfidenceLevel = \"exact\" | \"estimated\" | \"guesstimate\" | \"none\"; /** Which rung of the production-cost waterfall produced the figure. Surfaced so a reader can tell a settled price from a borrowed one without re-deriving the precedence. `sample_run` in particular is a real partner rate for a PROTOTYPE, which usually costs more per unit than a production batch — useful, but never to be presented as the production price. #1568 / export type ProductionCostSource = | \"partner_entered\" | \"actual_run\" /** A sample run's MEASURED consumption, stored on the design as `cost_breakdown.source === \"sample_consumption\"`. Distinct from `sample_run` and genuinely strong: it is what the sample actually used, not what the partner quoted for it — which is why that path alone keeps confidence \"exact\". / | \"sample_consumption\" | \"sample_run\" | \"admin_estimate\" | \"similar_designs\" /** Comparable HISTORY priced this design because nothing else could: what designs of this type have actually settled at, and what products previously created from designs actually sell for. 🔴 Opt-in per caller (`allow_historical_basis`), and never a settled price. `similar_designs` above is deliberately NOT a pricing signal — see `hasPricingSignal` — because pricing one design off another's number is the silent substitution #1554 was. This rung is that same evidence used DELIBERATELY, by a caller that has no bill of materials to work from and has asked for a starting figure anyway, and it is labelled and capped at \"guesstimate\" exactly like the sample-run fallback (#1568). The storefront checkout and draft-order paths do not pass the flag, so their behaviour is unchanged (#1564). / | \"historical_comparables\" | \"default_percent\"; export type MaterialCostItem = { inventory_item_id?: string; component_design_id?: string; name: string; cost: number; quantity: number; cost_source: \"order_history\" | \"unit_cost\" | \"component_design\" | \"consumption_log\" | \"estimated\" | \"default\"; /** Per-material commission (line cost × platform_fee_percent). Set when a fee applies. */ commission?: number; }; /** One piece of evidence about what this KIND of work has cost before. `unit_amount` is always per finished unit, in the design's cost currency — the caller normalises, because a total divided by the wrong quantity is the #1554 family and must not be re-derived in three places. / export type HistoricalComparable = { /** Design id or product id, so a reader can go and look at the evidence. */ id: string; name: string | null; unit_amount: number; source: \"design_estimate\" | \"product_price\"; }; type EstimateCostInput = { design_id: string; inventory_item_ids?: string[]; /** Partner-entered production cost per finished unit. When provided it is the authoritative production figure — it overrides every derived estimate (including the DEFAULT_PRODUCTION_PERCENT fallback). Partner recalc route. / production_cost_override?: number | null; /** JYT platform commission as a percentage of material cost. Opt-in per caller — defaults to 0 so store/admin/draft-order flows are unaffected; only the partner recalc route passes DEFAULT_PLATFORM_FEE_PERCENT. / platform_fee_percent?: number; /** Fallback per-unit cost for a material with no resolved cost. Opt-in per caller — defaults to 0 (off) so store/admin/draft-order flows are unaffected; the partner recalc route passes DEFAULT_MATERIAL_COST."
      },
      {
        "name": "allow_historical_basis",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "How much the returned figure is worth believing. 🔴 `none` is not a weaker estimate — it is the ABSENCE of one, and it is the only value for which `total_estimated` is null. The estimator used to report \"I found nothing\" as `total_estimated: 0` with `confidence: \"estimated\"`, as though it had looked and got an answer. `0` is not a small number here; it is a claim that the work is free, and every downstream reader believed it — including the storefront checkout, which turned it into a customer-facing price of zero. #1564 / type ConfidenceLevel = \"exact\" | \"estimated\" | \"guesstimate\" | \"none\"; /** Which rung of the production-cost waterfall produced the figure. Surfaced so a reader can tell a settled price from a borrowed one without re-deriving the precedence. `sample_run` in particular is a real partner rate for a PROTOTYPE, which usually costs more per unit than a production batch — useful, but never to be presented as the production price. #1568 / export type ProductionCostSource = | \"partner_entered\" | \"actual_run\" /** A sample run's MEASURED consumption, stored on the design as `cost_breakdown.source === \"sample_consumption\"`. Distinct from `sample_run` and genuinely strong: it is what the sample actually used, not what the partner quoted for it — which is why that path alone keeps confidence \"exact\". / | \"sample_consumption\" | \"sample_run\" | \"admin_estimate\" | \"similar_designs\" /** Comparable HISTORY priced this design because nothing else could: what designs of this type have actually settled at, and what products previously created from designs actually sell for. 🔴 Opt-in per caller (`allow_historical_basis`), and never a settled price. `similar_designs` above is deliberately NOT a pricing signal — see `hasPricingSignal` — because pricing one design off another's number is the silent substitution #1554 was. This rung is that same evidence used DELIBERATELY, by a caller that has no bill of materials to work from and has asked for a starting figure anyway, and it is labelled and capped at \"guesstimate\" exactly like the sample-run fallback (#1568). The storefront checkout and draft-order paths do not pass the flag, so their behaviour is unchanged (#1564). / | \"historical_comparables\" | \"default_percent\"; export type MaterialCostItem = { inventory_item_id?: string; component_design_id?: string; name: string; cost: number; quantity: number; cost_source: \"order_history\" | \"unit_cost\" | \"component_design\" | \"consumption_log\" | \"estimated\" | \"default\"; /** Per-material commission (line cost × platform_fee_percent). Set when a fee applies. */ commission?: number; }; /** One piece of evidence about what this KIND of work has cost before. `unit_amount` is always per finished unit, in the design's cost currency — the caller normalises, because a total divided by the wrong quantity is the #1554 family and must not be re-derived in three places. / export type HistoricalComparable = { /** Design id or product id, so a reader can go and look at the evidence. */ id: string; name: string | null; unit_amount: number; source: \"design_estimate\" | \"product_price\"; }; type EstimateCostInput = { design_id: string; inventory_item_ids?: string[]; /** Partner-entered production cost per finished unit. When provided it is the authoritative production figure — it overrides every derived estimate (including the DEFAULT_PRODUCTION_PERCENT fallback). Partner recalc route. / production_cost_override?: number | null; /** JYT platform commission as a percentage of material cost. Opt-in per caller — defaults to 0 so store/admin/draft-order flows are unaffected; only the partner recalc route passes DEFAULT_PLATFORM_FEE_PERCENT. / platform_fee_percent?: number; /** Fallback per-unit cost for a material with no resolved cost. Opt-in per caller — defaults to 0 (off) so store/admin/draft-order flows are unaffected; the partner recalc route passes DEFAULT_MATERIAL_COST. / default_material_cost?: number; /** Let comparable history price the design when nothing else can. Opt-in per caller — defaults to false, so every existing caller is unaffected. The quote wizard passes it, because a custom design whose production run is in the future has no run, no sample and often no BOM, and \"no answer\" is not a useful thing to show a partner about to send a quote."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/estimate-design-cost.ts"
  },
  "get-design-task": {
    "fields": [
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/get-design-task.ts"
  },
  "get-design-tasks": {
    "fields": [
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/get-design-tasks.ts"
  },
  "infer-design-product-type": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "force",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Infer and store a design's garment type (#938). The type is the key that makes a production spec derivable, so it has to exist before a draft product can be built from a design (#939). Most designs will never have one typed by hand, hence this. ## Two rules this workflow exists to enforce 1. 🔴 **A human's type is never overwritten by a model.** `mayInferOver` refuses when `product_type_source` is `manual`, unless a person explicitly asks to re-infer. Without this a designer corrects \"trousers\" to \"palazzo\" and finds it back the next time anything touches the design — with the production spec, and therefore the cost, having moved underneath them. 2. 🔴 **Inference must never fail the thing that triggered it.** A design that cannot be created because a model was unreachable is a far worse outcome than a design with no type yet. Every failure path here returns a `skipped` result; none of them throws. / /** Below this the model is guessing, and a wrong type moves a real cost. */ export const MIN_PRODUCT_TYPE_CONFIDENCE = 0.6 export type InferDesignProductTypeInput = { design_id: string /** Re-infer even over a manually typed value. Set only from an explicit \"re-infer\" action — that is a person choosing, not a model overruling one."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/infer-design-product-type.ts"
  },
  "list-partner-designs": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partnerId }}"
      },
      {
        "name": "q",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.q }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "bucket",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.bucket }}"
      },
      {
        "name": "offset",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "limit",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "locale",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.locale }}",
        "description": "Forwarded to `query.graph` for translated fields; the route reads it off the request."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/list-partner-designs.ts"
  },
  "list-single-design": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/list-single-design.ts"
  },
  "link-design-partner-workflow": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "partner_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.partner_ids }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/partner/link-design-to-partner.ts"
  },
  "promote-design-to-product": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/promote-design-to-product.ts"
  },
  "revise-design": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "revision_notes",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.revision_notes }}"
      },
      {
        "name": "overrides",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/revise-design.ts"
  },
  "update-design-task": {
    "fields": [
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/update-design-task.ts"
  },
  "update-design": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "inspiration_sources",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "design_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.design_type }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "priority",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.priority }}"
      },
      {
        "name": "media_files",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "target_completion_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.target_completion_date }}"
      },
      {
        "name": "design_files",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "thumbnail_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.thumbnail_url }}"
      },
      {
        "name": "custom_sizes",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "color_palette",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "tags",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "estimated_cost",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "cost_currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.cost_currency }}"
      },
      {
        "name": "designer_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.designer_notes }}"
      },
      {
        "name": "feedback_history",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "moodboard",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "colors",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.colors }}"
      },
      {
        "name": "size_sets",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.size_sets }}"
      },
      {
        "name": "product_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.product_type }}",
        "description": "#938. Setting this through the API is a HUMAN naming the garment, so it is stamped `manual` and inference will not overwrite it afterwards. Pass null to clear the type and hand it back to inference."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/designs/update-design.ts"
  },
  "create-email-template": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "template_key",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.template_key }}"
      },
      {
        "name": "subject",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.subject }}"
      },
      {
        "name": "html_content",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.html_content }}"
      },
      {
        "name": "variables",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "template_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.template_type }}"
      },
      {
        "name": "is_active",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "to",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.to }}"
      },
      {
        "name": "from",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.from }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/email_templates/create-email-template.ts"
  },
  "delete-email-template": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/email_templates/delete-email-template.ts"
  },
  "list-email-template": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/email_templates/list-email-template.ts"
  },
  "update-email-template": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "to",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.to }}"
      },
      {
        "name": "from",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.from }}"
      },
      {
        "name": "template_key",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.template_key }}"
      },
      {
        "name": "subject",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.subject }}"
      },
      {
        "name": "html_content",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.html_content }}"
      },
      {
        "name": "variables",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "template_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.template_type }}"
      },
      {
        "name": "is_active",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/email_templates/update-email-template.ts"
  },
  "batch-sync-products-to-etsy": {
    "fields": [
      {
        "name": "product_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.product_ids }}"
      },
      {
        "name": "etsy_account_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.etsy_account_id }}"
      },
      {
        "name": "sync_job_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.sync_job_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/etsy_sync/workflows/batch-sync-products.ts"
  },
  "create-feedback-with-ai-v2-link": {
    "fields": [
      {
        "name": "rating",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.rating }}"
      },
      {
        "name": "comment",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.comment }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "submitted_by",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.submitted_by }}"
      },
      {
        "name": "submitted_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.submitted_at }}"
      },
      {
        "name": "reviewed_by",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.reviewed_by }}"
      },
      {
        "name": "reviewed_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.reviewed_at }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/create-feedback-with-ai-v2-link.ts"
  },
  "create-feedback-with-link": {
    "fields": [
      {
        "name": "rating",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.rating }}"
      },
      {
        "name": "comment",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.comment }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "submitted_by",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.submitted_by }}"
      },
      {
        "name": "submitted_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.submitted_at }}"
      },
      {
        "name": "reviewed_by",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.reviewed_by }}"
      },
      {
        "name": "reviewed_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.reviewed_at }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/create-feedback-with-link.ts"
  },
  "create-feedback": {
    "fields": [
      {
        "name": "rating",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.rating }}"
      },
      {
        "name": "comment",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.comment }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "submitted_by",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.submitted_by }}"
      },
      {
        "name": "submitted_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.submitted_at }}"
      },
      {
        "name": "reviewed_by",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.reviewed_by }}"
      },
      {
        "name": "reviewed_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.reviewed_at }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/create-feedback.ts"
  },
  "delete-feedback": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/delete-feedback.ts"
  },
  "list-feedback": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/list-feedback.ts"
  },
  "update-feedback": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/feedback/update-feedback.ts"
  },
  "create-form": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "label",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.label }}"
      },
      {
        "name": "required",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "placeholder",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.placeholder }}"
      },
      {
        "name": "help_text",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.help_text }}"
      },
      {
        "name": "options",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "validation",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/create-form.ts"
  },
  "delete-form": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/delete-form.ts"
  },
  "import-tour-bookings": {
    "fields": [
      {
        "name": "form_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.form_id }}"
      },
      {
        "name": "bookings",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.bookings }}"
      },
      {
        "name": "token_ttl_days",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/import-tour-bookings.ts"
  },
  "list-form-responses": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/list-form-responses.ts"
  },
  "list-forms": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/list-forms.ts"
  },
  "set-form-fields": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/set-form-fields.ts"
  },
  "submit-form-response": {
    "fields": [
      {
        "name": "domain",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.domain }}"
      },
      {
        "name": "handle",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.handle }}"
      },
      {
        "name": "email",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.email }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "page_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.page_url }}"
      },
      {
        "name": "referrer",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.referrer }}"
      },
      {
        "name": "ip",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.ip }}"
      },
      {
        "name": "user_agent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.user_agent }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/submit-form-response.ts"
  },
  "update-form": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "domain",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.domain }}"
      },
      {
        "name": "handle",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.handle }}"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "submit_label",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.submit_label }}"
      },
      {
        "name": "success_message",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.success_message }}"
      },
      {
        "name": "settings",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/update-form.ts"
  },
  "verify-form-response": {
    "fields": [
      {
        "name": "response_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.response_id }}"
      },
      {
        "name": "code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.code }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/forms/verify-form-response.ts"
  },
  "fanout-prices-from-source": {
    "fields": [
      {
        "name": "source_price_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.source_price_id }}",
        "description": "Workflow: fanout-prices-from-source When a partner sets a manual price on a variant, this workflow creates auto-converted price rows in the partner's other supported currencies using FX rates from the fx_rates module. Algorithm: 1. Resolve the source price. 2. Recursion guard: query for the link `price ↔ fx_price_meta`. If the source already has an `fx_price_meta` row, fanout was the thing that created it — skip, otherwise the price.created event would trigger an infinite re-emit loop. 3. Resolve price_set → variant → product → first sales_channel → store. Skip if the chain breaks. 4. Read store.supported_currencies — these are the currencies the partner is willing to display prices in. 5. For each currency in supported_currencies that the price_set doesn't already have a row for, compute the converted amount via FX and call `addPrices` to attach a new price row to the price_set. 6. For each newly created price, create an `fx_price_meta` row (base_currency, base_amount, fx_rate, source_price_id) and a Medusa link between `price.id` and `fx_price_meta.id`. The link is the discriminator the recursion guard, UI badge, strip-on-edit, and daily re-rate all read. Why a separate fx_price_meta table: Medusa's `Price` model has no `metadata` column (verified). An earlier draft tried to stash the FX audit fields in `metadata` and it silently dropped — discovered when the recursion guard never fired. See apps/docs/notes/FX_AUTO_CONVERSION.md. Failure semantics: Per-currency errors during rate lookup are logged + counted but never abort the whole fanout. If `addPrices` itself fails the step throws so the workflow reports failure to the subscriber. / /** Identity of a price WITHIN a price set: currency plus its quantity bounds. Bounds arrive as BigNumberValue — a string from `query.graph`, a number when we set it ourselves — so `50` and `\"50\"` must collapse to the same key or a tier is re-created on every fanout. `null`/`undefined` (an unbounded price) normalises to the empty string, which is distinct from any numeric bound. / export const tierKey = (currency: string, min: any, max: any): string => { const norm = (v: any) => v === null || v === undefined || v === \"\" ? \"\" : String(Number(v)) return `${String(currency).toLowerCase()}|${norm(min)}|${norm(max)}` } export type FanoutPricesInput = { /** Source price id (the one the partner just set)."
      },
      {
        "name": "store_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.store_id }}",
        "description": "Workflow: fanout-prices-from-source When a partner sets a manual price on a variant, this workflow creates auto-converted price rows in the partner's other supported currencies using FX rates from the fx_rates module. Algorithm: 1. Resolve the source price. 2. Recursion guard: query for the link `price ↔ fx_price_meta`. If the source already has an `fx_price_meta` row, fanout was the thing that created it — skip, otherwise the price.created event would trigger an infinite re-emit loop. 3. Resolve price_set → variant → product → first sales_channel → store. Skip if the chain breaks. 4. Read store.supported_currencies — these are the currencies the partner is willing to display prices in. 5. For each currency in supported_currencies that the price_set doesn't already have a row for, compute the converted amount via FX and call `addPrices` to attach a new price row to the price_set. 6. For each newly created price, create an `fx_price_meta` row (base_currency, base_amount, fx_rate, source_price_id) and a Medusa link between `price.id` and `fx_price_meta.id`. The link is the discriminator the recursion guard, UI badge, strip-on-edit, and daily re-rate all read. Why a separate fx_price_meta table: Medusa's `Price` model has no `metadata` column (verified). An earlier draft tried to stash the FX audit fields in `metadata` and it silently dropped — discovered when the recursion guard never fired. See apps/docs/notes/FX_AUTO_CONVERSION.md. Failure semantics: Per-currency errors during rate lookup are logged + counted but never abort the whole fanout. If `addPrices` itself fails the step throws so the workflow reports failure to the subscriber. / /** Identity of a price WITHIN a price set: currency plus its quantity bounds. Bounds arrive as BigNumberValue — a string from `query.graph`, a number when we set it ourselves — so `50` and `\"50\"` must collapse to the same key or a tier is re-created on every fanout. `null`/`undefined` (an unbounded price) normalises to the empty string, which is distinct from any numeric bound. / export const tierKey = (currency: string, min: any, max: any): string => { const norm = (v: any) => v === null || v === undefined || v === \"\" ? \"\" : String(Number(v)) return `${String(currency).toLowerCase()}|${norm(min)}|${norm(max)}` } export type FanoutPricesInput = { /** Source price id (the one the partner just set). */ source_price_id: string /** Optional: the store whose supported_currencies drive the fanout. Pass this from contexts where you already know the store (partner routes, scripts). When omitted, the workflow derives it by walking price_set → variant → product → sales_channel and looking up the store where `default_sales_channel_id` matches — useful as a fallback but slower and dependent on the product being linked to a sales_channel that's the default for exactly one store."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/fx/fanout-prices.ts"
  },
  "refresh-fx-rates": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/fx/refresh-fx-rates.ts"
  },
  "rerate-auto-converted-prices": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/fx/rerate-auto-converted-prices.ts"
  },
  "bulk-sync-products-to-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/bulk-sync-products-to-google.ts"
  },
  "commit-import-mappings": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/commit-import-mappings.ts"
  },
  "import-existing-products-from-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/import-existing-products-from-google.ts"
  },
  "preview-import-from-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/preview-import-from-google.ts"
  },
  "sync-product-to-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/sync-product-to-google.ts"
  },
  "takeover-product-from-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/takeover-product-from-google.ts"
  },
  "unsync-product-from-google": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/google_merchant/workflows/unsync-product-from-google.ts"
  },
  "normalize-create-social-post-input-workflow": {
    "fields": [
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "caption",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.caption }}"
      },
      {
        "name": "media_attachments",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/hooks/socials.ts"
  },
  "resolve-default-facebook-page-id-workflow": {
    "fields": [
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "caption",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.caption }}"
      },
      {
        "name": "media_attachments",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/hooks/socials.ts"
  },
  "sync-inbound-emails": {
    "fields": [
      {
        "name": "count",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inbound-emails/sync-inbound-emails.ts"
  },
  "create-payment-and-link": {
    "fields": [
      {
        "name": "personIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.personIds }}"
      },
      {
        "name": "partnerIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.partnerIds }}"
      },
      {
        "name": "inventoryOrderIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.inventoryOrderIds }}"
      },
      {
        "name": "paymentSubmissionIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.paymentSubmissionIds }}",
        "description": "The payout(s) this payment SETTLES (#1710). 🔴 The fact that ends the \"one fact, two homes\" class. Until this existed a payment could name a person, a partner or an inventory order — never the submission it was actually paying off — so the partner ledger read `recorded: 0` beside `outstanding: 28,200` on an order against which INR 20,000 had demonstrably moved. That is the reading that pays someone twice. ⚠️ The link it writes is `paym_subm_paym_subm_inte_paym_inte_paym-9812b09f`. It has existed and been migrated since the link was declared — the belief that Postgres's 63-byte identifier limit stopped it from being created was wrong (Medusa abbreviates each segment to four characters and appends a hash). The table was empty because nothing wrote it, not because it was missing."
      },
      {
        "name": "attachments",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.attachments }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/create-payment-and-link.ts"
  },
  "create-payment-details": {
    "fields": [
      {
        "name": "amount",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "payment_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.payment_type }}"
      },
      {
        "name": "payment_date",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.payment_date }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "paid_to_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.paid_to_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/create-payment-details.ts"
  },
  "create-payment": {
    "fields": [
      {
        "name": "amount",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "payment_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.payment_type }}"
      },
      {
        "name": "payment_date",
        "type": "date",
        "required": true,
        "placeholder": "{{ $trigger.payment_date }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "paid_to_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.paid_to_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/create-payment.ts"
  },
  "delete-payment-details": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/delete-payment-details.ts"
  },
  "delete-payment": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/delete-payment.ts"
  },
  "list-payment-details": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/list-payment-details.ts"
  },
  "list-payment": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/list-payment.ts"
  },
  "update-payment-details": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "account_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.account_name }}"
      },
      {
        "name": "account_number",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.account_number }}"
      },
      {
        "name": "bank_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.bank_name }}"
      },
      {
        "name": "ifsc_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.ifsc_code }}"
      },
      {
        "name": "wallet_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.wallet_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/update-payment-details.ts"
  },
  "update-payment": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "amount",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "payment_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.payment_type }}"
      },
      {
        "name": "payment_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.payment_date }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "paid_to_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.paid_to_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/internal_payments/update-payment.ts"
  },
  "list-partner-inventory-items": {
    "fields": [
      {
        "name": "locationId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.locationId }}",
        "description": "The partner's stock location — the whole scoping rule for this surface."
      },
      {
        "name": "q",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.q }}"
      },
      {
        "name": "offset",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "limit",
        "type": "number",
        "required": true,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory/list-partner-inventory-items.ts"
  },
  "split-inventory-item": {
    "fields": [
      {
        "name": "sourceInventoryItemId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.sourceInventoryItemId }}"
      },
      {
        "name": "quantity",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "newTitle",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.newTitle }}"
      },
      {
        "name": "locationId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.locationId }}",
        "description": "When set, split is taken from this location only instead of proportionally across all."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory/split-inventory-item.ts"
  },
  "create-inventory-order-shipment": {
    "fields": [
      {
        "name": "orderId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.orderId }}"
      },
      {
        "name": "carrier",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.carrier }}",
        "description": "Generate a real carrier shipment (forward → AWB → label) for a partner inventory-order completion and persist the carrier refs onto the inventory order (#772). Opt-in: invoked only when the partner asked to generate a shipment; the legacy free-text `partner_tracking_number` path is untouched otherwise. Pickup bootstrap: the explicit pickup input or the order's from_location is ensured to be a registered carrier pickup via `registerShiprocketPickup` (metadata warehouse key used as-is when present; else lists first, then registers from the location's address). There is NO fallback to another registered pickup — all parties share one Shiprocket account, so that would ship from someone else's warehouse. A clean MedusaError (not a 500) is thrown when no pickup can be resolved so the UI shows an actionable message. / export type CreateInventoryOrderShipmentInput = { orderId: string /** Defaults to \"shiprocket\"."
      },
      {
        "name": "pickupStockLocationId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pickupStockLocationId }}",
        "description": "Generate a real carrier shipment (forward → AWB → label) for a partner inventory-order completion and persist the carrier refs onto the inventory order (#772). Opt-in: invoked only when the partner asked to generate a shipment; the legacy free-text `partner_tracking_number` path is untouched otherwise. Pickup bootstrap: the explicit pickup input or the order's from_location is ensured to be a registered carrier pickup via `registerShiprocketPickup` (metadata warehouse key used as-is when present; else lists first, then registers from the location's address). There is NO fallback to another registered pickup — all parties share one Shiprocket account, so that would ship from someone else's warehouse. A clean MedusaError (not a 500) is thrown when no pickup can be resolved so the UI shows an actionable message. / export type CreateInventoryOrderShipmentInput = { orderId: string /** Defaults to \"shiprocket\". */ carrier?: string /** Source stock location to ship from; auto-registered as a pickup if new."
      },
      {
        "name": "weightGrams",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "dimensionsCm",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.dimensionsCm }}"
      },
      {
        "name": "preferredCourierId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.preferredCourierId }}"
      },
      {
        "name": "deliveredQuantities",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "Generate a real carrier shipment (forward → AWB → label) for a partner inventory-order completion and persist the carrier refs onto the inventory order (#772). Opt-in: invoked only when the partner asked to generate a shipment; the legacy free-text `partner_tracking_number` path is untouched otherwise. Pickup bootstrap: the explicit pickup input or the order's from_location is ensured to be a registered carrier pickup via `registerShiprocketPickup` (metadata warehouse key used as-is when present; else lists first, then registers from the location's address). There is NO fallback to another registered pickup — all parties share one Shiprocket account, so that would ship from someone else's warehouse. A clean MedusaError (not a 500) is thrown when no pickup can be resolved so the UI shows an actionable message. / export type CreateInventoryOrderShipmentInput = { orderId: string /** Defaults to \"shiprocket\". */ carrier?: string /** Source stock location to ship from; auto-registered as a pickup if new. */ pickupStockLocationId?: string weightGrams?: number dimensionsCm?: Dimensions preferredCourierId?: string | number /** Delivered quantities keyed by order_line_id (restricts shipment items)."
      },
      {
        "name": "actingEmail",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.actingEmail }}",
        "description": "Generate a real carrier shipment (forward → AWB → label) for a partner inventory-order completion and persist the carrier refs onto the inventory order (#772). Opt-in: invoked only when the partner asked to generate a shipment; the legacy free-text `partner_tracking_number` path is untouched otherwise. Pickup bootstrap: the explicit pickup input or the order's from_location is ensured to be a registered carrier pickup via `registerShiprocketPickup` (metadata warehouse key used as-is when present; else lists first, then registers from the location's address). There is NO fallback to another registered pickup — all parties share one Shiprocket account, so that would ship from someone else's warehouse. A clean MedusaError (not a 500) is thrown when no pickup can be resolved so the UI shows an actionable message. / export type CreateInventoryOrderShipmentInput = { orderId: string /** Defaults to \"shiprocket\". */ carrier?: string /** Source stock location to ship from; auto-registered as a pickup if new. */ pickupStockLocationId?: string weightGrams?: number dimensionsCm?: Dimensions preferredCourierId?: string | number /** Delivered quantities keyed by order_line_id (restricts shipment items). */ deliveredQuantities?: Record<string, number> /** Acting user's email recorded on a freshly-registered pickup (#427)."
      },
      {
        "name": "pickupDate",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pickupDate }}",
        "description": "Generate a real carrier shipment (forward → AWB → label) for a partner inventory-order completion and persist the carrier refs onto the inventory order (#772). Opt-in: invoked only when the partner asked to generate a shipment; the legacy free-text `partner_tracking_number` path is untouched otherwise. Pickup bootstrap: the explicit pickup input or the order's from_location is ensured to be a registered carrier pickup via `registerShiprocketPickup` (metadata warehouse key used as-is when present; else lists first, then registers from the location's address). There is NO fallback to another registered pickup — all parties share one Shiprocket account, so that would ship from someone else's warehouse. A clean MedusaError (not a 500) is thrown when no pickup can be resolved so the UI shows an actionable message. / export type CreateInventoryOrderShipmentInput = { orderId: string /** Defaults to \"shiprocket\". */ carrier?: string /** Source stock location to ship from; auto-registered as a pickup if new. */ pickupStockLocationId?: string weightGrams?: number dimensionsCm?: Dimensions preferredCourierId?: string | number /** Delivered quantities keyed by order_line_id (restricts shipment items). */ deliveredQuantities?: Record<string, number> /** Acting user's email recorded on a freshly-registered pickup (#427). */ actingEmail?: string /** Requested carrier pickup date (\"YYYY-MM-DD\"). When set, a pickup is scheduled for that date after the shipment is created; otherwise Shiprocket picks the earliest slot."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/create-inventory-order-shipment.ts"
  },
  "create-inventory-order-tasks-from-templates": {
    "fields": [
      {
        "name": "data",
        "type": "object",
        "required": true,
        "description": "Fields for the new record",
        "placeholder": "{{ $last }}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/inventory_orders/create-tasks-from-templates.ts"
  },
  "delete-inventory-order-task": {
    "fields": [
      {
        "name": "inventoryOrderId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.inventoryOrderId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/delete-inventory-order-task.ts"
  },
  "get-inventory-order-task": {
    "fields": [
      {
        "name": "inventoryOrderId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.inventoryOrderId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/get-inventory-order-task.ts"
  },
  "get-inventory-order-tasks": {
    "fields": [
      {
        "name": "inventoryOrderId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.inventoryOrderId }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/get-inventory-order-tasks.ts"
  },
  "list-partner-inventory-orders": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partnerId }}"
      },
      {
        "name": "q",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.q }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "offset",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "limit",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "locale",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.locale }}",
        "description": "Forwarded to `query.graph` for translated fields; the route reads it off the request."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/list-partner-inventory-orders.ts"
  },
  "sync-inventory-shipment-tracking": {
    "fields": [
      {
        "name": "tracking",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.tracking }}",
        "description": "Shipment-level counterpart of the order status-changed event (#888). Fired only on a real shipment status transition (webhook retries are deduped by the forward-only guard), so visual flows can notify partners about pickup / transit milestones without listening to raw carrier payloads. / export const INVENTORY_SHIPMENT_STATUS_CHANGED_EVENT = \"inventory_orders.inventory-shipment.status-changed\" export type SyncInventoryShipmentTrackingInput = { /** Normalized carrier push (from `normalizeShiprocketWebhook` or a future carrier)."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/sync-inventory-shipment-tracking.ts"
  },
  "update-inventory-order-task": {
    "fields": [
      {
        "name": "inventoryOrderId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.inventoryOrderId }}"
      },
      {
        "name": "taskId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.taskId }}"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "priority",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.priority }}"
      },
      {
        "name": "due_date",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.due_date }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/update-inventory-order-task.ts"
  },
  "update-inventory-order": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "expectedCurrentStatus",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.expectedCurrentStatus }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/inventory_orders/update-inventory-order.ts"
  },
  "create-investor-admin": {
    "fields": [
      {
        "name": "authIdentityId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.authIdentityId }}"
      },
      {
        "name": "tempPassword",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.tempPassword }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/investor/create-investor-admin.ts"
  },
  "reset-investor-admin-password": {
    "fields": [
      {
        "name": "authIdentityId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.authIdentityId }}"
      },
      {
        "name": "tempPassword",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.tempPassword }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/investor/create-investor-admin.ts"
  },
  "create-investor-admin-with-registration": {
    "fields": [
      {
        "name": "authIdentityId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.authIdentityId }}"
      },
      {
        "name": "tempPassword",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.tempPassword }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/investor/create-investor-admin.ts"
  },
  "generate-investor-agreement": {
    "fields": [
      {
        "name": "investor_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.investor_id }}"
      },
      {
        "name": "funding_round_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.funding_round_id }}"
      },
      {
        "name": "cap_table_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.cap_table_id }}"
      },
      {
        "name": "instrument_group",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.instrument_group }}"
      },
      {
        "name": "instrument_label",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.instrument_label }}",
        "description": "Human label surfaced in the email/agreement (e.g. \"SAFE\")."
      },
      {
        "name": "amount",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "currency_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency_code }}"
      },
      {
        "name": "number_of_shares",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.number_of_shares }}"
      },
      {
        "name": "share_price",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.share_price }}"
      },
      {
        "name": "principal",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.principal }}"
      },
      {
        "name": "valuation_cap",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.valuation_cap }}"
      },
      {
        "name": "discount_rate",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.discount_rate }}"
      },
      {
        "name": "safe_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.safe_type }}"
      },
      {
        "name": "stake_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.stake_id }}"
      },
      {
        "name": "convertible_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.convertible_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/investor/generate-investor-agreement.ts"
  },
  "generate-ideas-email": {
    "fields": [
      {
        "name": "oneGoal",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.oneGoal }}",
        "description": "generate-ideas-email.ts — #659 slice 2, PR-2. The \"AI tactical-ideas email\" generate workflow: read today's ground-truth numbers from `marketing_metric_snapshot`, ask the LLM for 3-5 tactical moves (referring to numbers ONLY by {TOKEN} placeholder), run the two-layer hallucination guard (`ideas-email-guard-lib.ts`), regenerate ONCE on failure, then persist a `marketing_ideas_log` row BEFORE anything is ever sent (report §4.3: \"every job writes its result to Postgres before sending\"). Testability: the AI call is an INJECTABLE function (`AiGenerateFn`) — the integration test passes a stub returning fixed `{TOKEN}` copy, so CI NEVER calls a live LLM. The Medusa workflow wrapper injects the real OpenRouter call (mirrors the verified pattern in `src/workflows/ad-planning/sentiment/analyze-sentiment.ts:44-71`). This PR does NOT send the email — `sent` stays false; the send workflow (PR-3) flips it. The job that schedules this (PR-4) stays disabled until the One Goal is picked. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { GroundTruth, GroundTruthValue } from \"./ideas-email-guard-lib\" import { runGuard } from \"./ideas-email-guard-lib\" import { buildIdeasPrompt, STRICTER_SUFFIX } from \"./ideas-email-prompt-lib\" /** An injectable LLM call: prompt in, raw text out (or \"\" on failure). */ export type AiGenerateFn = (prompt: string) => Promise<string> export type GenerateIdeasEmailOptions = { /** Injected for tests; defaults to the role-resolved platform generator (ai_newsletter_drafter → configured platform, else free models). */ aiGenerate?: AiGenerateFn /** The \"One Goal\" string; never invented in code (report §3.6)."
      },
      {
        "name": "businessDescription",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.businessDescription }}",
        "description": "generate-ideas-email.ts — #659 slice 2, PR-2. The \"AI tactical-ideas email\" generate workflow: read today's ground-truth numbers from `marketing_metric_snapshot`, ask the LLM for 3-5 tactical moves (referring to numbers ONLY by {TOKEN} placeholder), run the two-layer hallucination guard (`ideas-email-guard-lib.ts`), regenerate ONCE on failure, then persist a `marketing_ideas_log` row BEFORE anything is ever sent (report §4.3: \"every job writes its result to Postgres before sending\"). Testability: the AI call is an INJECTABLE function (`AiGenerateFn`) — the integration test passes a stub returning fixed `{TOKEN}` copy, so CI NEVER calls a live LLM. The Medusa workflow wrapper injects the real OpenRouter call (mirrors the verified pattern in `src/workflows/ad-planning/sentiment/analyze-sentiment.ts:44-71`). This PR does NOT send the email — `sent` stays false; the send workflow (PR-3) flips it. The job that schedules this (PR-4) stays disabled until the One Goal is picked. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { GroundTruth, GroundTruthValue } from \"./ideas-email-guard-lib\" import { runGuard } from \"./ideas-email-guard-lib\" import { buildIdeasPrompt, STRICTER_SUFFIX } from \"./ideas-email-prompt-lib\" /** An injectable LLM call: prompt in, raw text out (or \"\" on failure). */ export type AiGenerateFn = (prompt: string) => Promise<string> export type GenerateIdeasEmailOptions = { /** Injected for tests; defaults to the role-resolved platform generator (ai_newsletter_drafter → configured platform, else free models). */ aiGenerate?: AiGenerateFn /** The \"One Goal\" string; never invented in code (report §3.6). */ oneGoal?: string /** Business description fed to the prompt."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/marketing/generate-ideas-email.ts"
  },
  "generate-newsletter-draft": {
    "fields": [
      {
        "name": "topic",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.topic }}",
        "description": "generate-newsletter-draft.ts — #659 slice (report §12.4). The \"AI newsletter draft generator\": ask the LLM for a customer-facing newsletter (subject / preheader / intro / sections / CTA) and persist it to the EXISTING `marketing_draft` model as an OPERATOR-REVIEW draft. Sending is NOT done here — drafts are reviewed/edited in the (separate) Newsletter editor UI and later enqueued through the existing `src/jobs/process-email-queue.ts`. Reuses the verified generate-ideas-email wiring: - the AI call is an INJECTABLE `AiGenerateFn` (prompt in, text out) so CI never calls a live LLM; the Medusa workflow wrapper injects the real OpenRouter call (mirrors generate-ideas-email.ts:152-168). Unlike the tactical-ideas email this is CUSTOMER-FACING, so it does NOT read internal metric snapshots — we never want raw platform GMV leaking into a brand newsletter. The model is told to avoid inventing specific numbers. Pure, unit-tested helpers: `buildNewsletterPrompt`, `parseNewsletterPayload`, `coerceNewsletterPayload`. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { AiGenerateFn } from \"./generate-ideas-email\" /** Role this generator resolves a platform for (free-model fallback). */ const NEWSLETTER_DRAFT_ROLE = \"ai_newsletter_drafter\" const DEFAULT_BUSINESS_DESCRIPTION = \"JYT is a textile-production commerce platform: it runs an admin storefront \" + \"and onboards partner brands who sell their own products through per-partner \" + \"storefronts.\" /** v1 newsletter voice rules — kept pure (the workflow may pass an override). */ export const NEWSLETTER_VOICE_RULES = [ \"Warm and brand-forward, but never hype. Sound like a real person, not an ad.\", \"Lead with value for the reader; keep paragraphs short and scannable.\", \"Do NOT invent specific numbers, percentages, prices, discounts, or dates.\", \"End with one clear, low-pressure call to action.\", ].join(\"\\n- \") // IST = UTC+5:30. Return the IST calendar date (YYYY-MM-DD) for a UTC instant. function istDateString(d: Date): string { const ist = new Date(d.getTime() + 5.5 * 60 * 60 * 1000) return ist.toISOString().slice(0, 10) } export type NewsletterSection = { heading: string body: string } export type NewsletterPayload = { subject: string preheader: string intro: string sections: NewsletterSection[] cta: string /** true when the model output could not be parsed as JSON and we fell back. */ parse_error?: boolean } export type GenerateNewsletterDraftOptions = { /** Injected for tests; defaults to the real OpenRouter call. */ aiGenerate?: AiGenerateFn /** Operator topic/angle for this newsletter (free text)."
      },
      {
        "name": "businessDescription",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.businessDescription }}",
        "description": "generate-newsletter-draft.ts — #659 slice (report §12.4). The \"AI newsletter draft generator\": ask the LLM for a customer-facing newsletter (subject / preheader / intro / sections / CTA) and persist it to the EXISTING `marketing_draft` model as an OPERATOR-REVIEW draft. Sending is NOT done here — drafts are reviewed/edited in the (separate) Newsletter editor UI and later enqueued through the existing `src/jobs/process-email-queue.ts`. Reuses the verified generate-ideas-email wiring: - the AI call is an INJECTABLE `AiGenerateFn` (prompt in, text out) so CI never calls a live LLM; the Medusa workflow wrapper injects the real OpenRouter call (mirrors generate-ideas-email.ts:152-168). Unlike the tactical-ideas email this is CUSTOMER-FACING, so it does NOT read internal metric snapshots — we never want raw platform GMV leaking into a brand newsletter. The model is told to avoid inventing specific numbers. Pure, unit-tested helpers: `buildNewsletterPrompt`, `parseNewsletterPayload`, `coerceNewsletterPayload`. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { AiGenerateFn } from \"./generate-ideas-email\" /** Role this generator resolves a platform for (free-model fallback). */ const NEWSLETTER_DRAFT_ROLE = \"ai_newsletter_drafter\" const DEFAULT_BUSINESS_DESCRIPTION = \"JYT is a textile-production commerce platform: it runs an admin storefront \" + \"and onboards partner brands who sell their own products through per-partner \" + \"storefronts.\" /** v1 newsletter voice rules — kept pure (the workflow may pass an override). */ export const NEWSLETTER_VOICE_RULES = [ \"Warm and brand-forward, but never hype. Sound like a real person, not an ad.\", \"Lead with value for the reader; keep paragraphs short and scannable.\", \"Do NOT invent specific numbers, percentages, prices, discounts, or dates.\", \"End with one clear, low-pressure call to action.\", ].join(\"\\n- \") // IST = UTC+5:30. Return the IST calendar date (YYYY-MM-DD) for a UTC instant. function istDateString(d: Date): string { const ist = new Date(d.getTime() + 5.5 * 60 * 60 * 1000) return ist.toISOString().slice(0, 10) } export type NewsletterSection = { heading: string body: string } export type NewsletterPayload = { subject: string preheader: string intro: string sections: NewsletterSection[] cta: string /** true when the model output could not be parsed as JSON and we fell back. */ parse_error?: boolean } export type GenerateNewsletterDraftOptions = { /** Injected for tests; defaults to the real OpenRouter call. */ aiGenerate?: AiGenerateFn /** Operator topic/angle for this newsletter (free text). */ topic?: string /** Business description fed to the prompt."
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}",
        "description": "generate-newsletter-draft.ts — #659 slice (report §12.4). The \"AI newsletter draft generator\": ask the LLM for a customer-facing newsletter (subject / preheader / intro / sections / CTA) and persist it to the EXISTING `marketing_draft` model as an OPERATOR-REVIEW draft. Sending is NOT done here — drafts are reviewed/edited in the (separate) Newsletter editor UI and later enqueued through the existing `src/jobs/process-email-queue.ts`. Reuses the verified generate-ideas-email wiring: - the AI call is an INJECTABLE `AiGenerateFn` (prompt in, text out) so CI never calls a live LLM; the Medusa workflow wrapper injects the real OpenRouter call (mirrors generate-ideas-email.ts:152-168). Unlike the tactical-ideas email this is CUSTOMER-FACING, so it does NOT read internal metric snapshots — we never want raw platform GMV leaking into a brand newsletter. The model is told to avoid inventing specific numbers. Pure, unit-tested helpers: `buildNewsletterPrompt`, `parseNewsletterPayload`, `coerceNewsletterPayload`. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { AiGenerateFn } from \"./generate-ideas-email\" /** Role this generator resolves a platform for (free-model fallback). */ const NEWSLETTER_DRAFT_ROLE = \"ai_newsletter_drafter\" const DEFAULT_BUSINESS_DESCRIPTION = \"JYT is a textile-production commerce platform: it runs an admin storefront \" + \"and onboards partner brands who sell their own products through per-partner \" + \"storefronts.\" /** v1 newsletter voice rules — kept pure (the workflow may pass an override). */ export const NEWSLETTER_VOICE_RULES = [ \"Warm and brand-forward, but never hype. Sound like a real person, not an ad.\", \"Lead with value for the reader; keep paragraphs short and scannable.\", \"Do NOT invent specific numbers, percentages, prices, discounts, or dates.\", \"End with one clear, low-pressure call to action.\", ].join(\"\\n- \") // IST = UTC+5:30. Return the IST calendar date (YYYY-MM-DD) for a UTC instant. function istDateString(d: Date): string { const ist = new Date(d.getTime() + 5.5 * 60 * 60 * 1000) return ist.toISOString().slice(0, 10) } export type NewsletterSection = { heading: string body: string } export type NewsletterPayload = { subject: string preheader: string intro: string sections: NewsletterSection[] cta: string /** true when the model output could not be parsed as JSON and we fell back. */ parse_error?: boolean } export type GenerateNewsletterDraftOptions = { /** Injected for tests; defaults to the real OpenRouter call. */ aiGenerate?: AiGenerateFn /** Operator topic/angle for this newsletter (free text). */ topic?: string /** Business description fed to the prompt. */ businessDescription?: string /** Voice override. */ voiceRules?: string /** Human label for the draft row; defaults to `newsletter-<IST date>`."
      },
      {
        "name": "markReady",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "generate-newsletter-draft.ts — #659 slice (report §12.4). The \"AI newsletter draft generator\": ask the LLM for a customer-facing newsletter (subject / preheader / intro / sections / CTA) and persist it to the EXISTING `marketing_draft` model as an OPERATOR-REVIEW draft. Sending is NOT done here — drafts are reviewed/edited in the (separate) Newsletter editor UI and later enqueued through the existing `src/jobs/process-email-queue.ts`. Reuses the verified generate-ideas-email wiring: - the AI call is an INJECTABLE `AiGenerateFn` (prompt in, text out) so CI never calls a live LLM; the Medusa workflow wrapper injects the real OpenRouter call (mirrors generate-ideas-email.ts:152-168). Unlike the tactical-ideas email this is CUSTOMER-FACING, so it does NOT read internal metric snapshots — we never want raw platform GMV leaking into a brand newsletter. The model is told to avoid inventing specific numbers. Pure, unit-tested helpers: `buildNewsletterPrompt`, `parseNewsletterPayload`, `coerceNewsletterPayload`. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, } from \"@medusajs/framework/workflows-sdk\" import { makeRoleAiGenerate } from \"../../mastra/services/ai-platforms\" import { MARKETING_MODULE } from \"../../modules/marketing\" import type { AiGenerateFn } from \"./generate-ideas-email\" /** Role this generator resolves a platform for (free-model fallback). */ const NEWSLETTER_DRAFT_ROLE = \"ai_newsletter_drafter\" const DEFAULT_BUSINESS_DESCRIPTION = \"JYT is a textile-production commerce platform: it runs an admin storefront \" + \"and onboards partner brands who sell their own products through per-partner \" + \"storefronts.\" /** v1 newsletter voice rules — kept pure (the workflow may pass an override). */ export const NEWSLETTER_VOICE_RULES = [ \"Warm and brand-forward, but never hype. Sound like a real person, not an ad.\", \"Lead with value for the reader; keep paragraphs short and scannable.\", \"Do NOT invent specific numbers, percentages, prices, discounts, or dates.\", \"End with one clear, low-pressure call to action.\", ].join(\"\\n- \") // IST = UTC+5:30. Return the IST calendar date (YYYY-MM-DD) for a UTC instant. function istDateString(d: Date): string { const ist = new Date(d.getTime() + 5.5 * 60 * 60 * 1000) return ist.toISOString().slice(0, 10) } export type NewsletterSection = { heading: string body: string } export type NewsletterPayload = { subject: string preheader: string intro: string sections: NewsletterSection[] cta: string /** true when the model output could not be parsed as JSON and we fell back. */ parse_error?: boolean } export type GenerateNewsletterDraftOptions = { /** Injected for tests; defaults to the real OpenRouter call. */ aiGenerate?: AiGenerateFn /** Operator topic/angle for this newsletter (free text). */ topic?: string /** Business description fed to the prompt. */ businessDescription?: string /** Voice override. */ voiceRules?: string /** Human label for the draft row; defaults to `newsletter-<IST date>`. */ name?: string /** When true, persist as status=\"approved\" (ready); else status=\"draft\"."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/marketing/generate-newsletter-draft.ts"
  },
  "log-outreach": {
    "fields": [
      {
        "name": "recipient_email",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.recipient_email }}"
      },
      {
        "name": "recipient_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.recipient_name }}"
      },
      {
        "name": "company",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.company }}"
      },
      {
        "name": "campaign",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.campaign }}"
      },
      {
        "name": "channel",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.channel }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "external_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.external_id }}"
      },
      {
        "name": "sent_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.sent_at }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/marketing/log-outreach.ts"
  },
  "create-album-media": {
    "fields": [
      {
        "name": "album_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.album_id }}"
      },
      {
        "name": "media_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.media_id }}"
      },
      {
        "name": "sort_order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/create-album-media.ts"
  },
  "create-album": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "slug",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.slug }}"
      },
      {
        "name": "cover_media_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.cover_media_id }}"
      },
      {
        "name": "is_public",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "sort_order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/create-album.ts"
  },
  "create-folder": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "slug",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.slug }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "parent_folder_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.parent_folder_id }}"
      },
      {
        "name": "path",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.path }}"
      },
      {
        "name": "level",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "default_sort_order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "default_is_public",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/create-folder.ts"
  },
  "create-media-file": {
    "fields": [
      {
        "name": "file_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.file_name }}"
      },
      {
        "name": "original_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.original_name }}"
      },
      {
        "name": "file_path",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.file_path }}"
      },
      {
        "name": "file_size",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "file_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.file_type }}"
      },
      {
        "name": "mime_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.mime_type }}"
      },
      {
        "name": "extension",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.extension }}"
      },
      {
        "name": "alt_text",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.alt_text }}"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "folder_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.folder_id }}"
      },
      {
        "name": "is_public",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "tags",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/create-media-file.ts"
  },
  "batch-create-products-from-media": {
    "fields": [
      {
        "name": "folder_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.folder_name }}"
      },
      {
        "name": "folder_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.folder_id }}"
      },
      {
        "name": "media_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.media_ids }}"
      },
      {
        "name": "batch_size",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "auto_publish",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/create-products-from-media/workflows/batch-create-products.ts"
  },
  "delete-album-media": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/delete-album-media.ts"
  },
  "delete-album": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/delete-album.ts"
  },
  "delete-folder": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/delete-folder.ts"
  },
  "delete-media-file": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/delete-media-file.ts"
  },
  "finalize-s3-media": {
    "fields": [
      {
        "name": "existingFolderId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.existingFolderId }}"
      },
      {
        "name": "existingAlbumIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.existingAlbumIds }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/finalize-s3-media.ts"
  },
  "get-media-dictionaries": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/media/get-dictionaries.ts"
  },
  "get-folder-detail": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/get-folder-detail.ts"
  },
  "get-folder": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/get-folder.ts"
  },
  "list-album-media": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/list-album-media.ts"
  },
  "list-album": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/list-album.ts"
  },
  "list-all-medias": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/list-all-medias.ts"
  },
  "list-folder": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/list-folder.ts"
  },
  "list-media-file": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/list-media-file.ts"
  },
  "update-album-media": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/update-album-media.ts"
  },
  "update-album": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/update-album.ts"
  },
  "update-folder": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "path",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.path }}"
      },
      {
        "name": "level",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "sort_order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "is_public",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "parent_folder_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.parent_folder_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/update-folder.ts"
  },
  "update-media-file": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/update-media-file.ts"
  },
  "upload-and-organize-media": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "parent_folder_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.parent_folder_id }}"
      },
      {
        "name": "is_public",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/media/upload-and-organize-media.ts"
  },
  "fetch-meta-ads-overview": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/meta-ads/fetch-overview/index.ts"
  },
  "list-partner-orders": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.partnerId }}"
      },
      {
        "name": "salesChannelId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.salesChannelId }}"
      },
      {
        "name": "kind",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.kind }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.fields }}"
      },
      {
        "name": "baseFilters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "order",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "skip",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "take",
        "type": "number",
        "required": true,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/orders/list-partner-orders.ts"
  },
  "sync-order-shipment-tracking": {
    "fields": [
      {
        "name": "tracking",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.tracking }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/orders/sync-order-shipment-tracking.ts"
  },
  "associate-partner-person-types": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partnerId }}"
      },
      {
        "name": "personTypeIds",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.personTypeIds }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/associate-partner-person-types.ts"
  },
  "batch-partner-variants": {
    "fields": [
      {
        "name": "storeId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.storeId }}"
      },
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "store",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "create",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.create }}"
      },
      {
        "name": "update",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.update }}"
      },
      {
        "name": "delete",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.delete }}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}",
        "description": "#1380 / #1370 — the partner variants/batch write, as one workflow. Companion to `create-partner-product`: same shape, same gating discipline, so the two partner write paths are described the same way. ⚠️ READ THIS BEFORE TREATING IT AS A LATENCY FIX. It is not one. The cost on this route lives inside `refetchBatchVariants`, and that call is carried over here unchanged and still runs on the request scope immediately after that scope performed the write: [variants/batch] total=16364ms auth=26ms batchWorkflow=358ms inventoryLevels=0ms responseQueries=15977ms fanoutEmit=3ms The write is 358ms; the re-read was 15977ms. Moving the same query into a step does not make it cheaper. What this DOES buy is the ability to skip work that never needed to run, and a phase map that says `skipped` instead of `0ms` so a skipped branch can never again be misread as a free one. Three theories about the re-read are already dead — the option graph, core's `refetchBatchVariants` being the wrong helper, and inline subscribers. Do not add a fourth here. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { batchProductVariantsWorkflow } from \"@medusajs/medusa/core-flows\" import { refetchBatchVariants } from \"@medusajs/medusa/api/admin/products/helpers\" import { collectVariantPriceIds } from \"../fx/fanout-variant-prices\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestPartnerPriceFanoutStep } from \"./create-partner-product\" export type BatchPartnerVariantsInput = { storeId: string productId: string store: { id?: string; default_sales_channel_id?: string | null } create?: any[] update?: any[] delete?: string[] /** Response field set, straight from `req.queryConfig.fields` the way core does it. Omitted → `BATCH_VARIANT_FIELDS`. ⚠️ Narrowing this is a real trade: the FX fanout is driven by `collectVariantPriceIds`, which reads the `prices` the enrichment built. Drop the price fields and the fanout goes quiet — without erroring."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/batch-partner-variants.ts"
  },
  "connect-partner-whatsapp": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "phone",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.phone }}",
        "description": "Already-normalized digits-only phone with country code."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/connect-partner-whatsapp.ts"
  },
  "create-partner-admin": {
    "fields": [
      {
        "name": "authIdentityId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.authIdentityId }}"
      },
      {
        "name": "tempPassword",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.tempPassword }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/create-partner-admin.ts"
  },
  "create-partner-admin-with-registration": {
    "fields": [
      {
        "name": "authIdentityId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.authIdentityId }}"
      },
      {
        "name": "tempPassword",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.tempPassword }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/create-partner-admin.ts"
  },
  "create-partner-product": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.partnerId }}",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate."
      },
      {
        "name": "storeId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.storeId }}"
      },
      {
        "name": "product",
        "type": "object",
        "required": true,
        "placeholder": "{}",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate. / partnerId?: string | null storeId: string /** Raw product payload. Route validators stay the source of truth for shape."
      },
      {
        "name": "requireSalesChannel",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate. / partnerId?: string | null storeId: string /** Raw product payload. Route validators stay the source of truth for shape. */ product: Record<string, any> /** Legacy `POST /partners/products` refuses a store with no default sales channel (documented 400). The store-scoped route has always just skipped the injection instead. Preserved per-caller rather than unified, because changing either one is a wire-visible change to a live client."
      },
      {
        "name": "applyArtisanGate",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate. / partnerId?: string | null storeId: string /** Raw product payload. Route validators stay the source of truth for shape. */ product: Record<string, any> /** Legacy `POST /partners/products` refuses a store with no default sales channel (documented 400). The store-scoped route has always just skipped the injection instead. Preserved per-caller rather than unified, because changing either one is a wire-visible change to a live client. / requireSalesChannel?: boolean /** Run the #859 artisan proposal gate (status → `proposed`, ownership link, `partner_product.proposed`). Default true. The admin and discover-copy paths have never applied it and turning it on for them would silently change what those routes publish."
      },
      {
        "name": "salesChannelIds",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.salesChannelIds }}",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate. / partnerId?: string | null storeId: string /** Raw product payload. Route validators stay the source of truth for shape. */ product: Record<string, any> /** Legacy `POST /partners/products` refuses a store with no default sales channel (documented 400). The store-scoped route has always just skipped the injection instead. Preserved per-caller rather than unified, because changing either one is a wire-visible change to a live client. / requireSalesChannel?: boolean /** Run the #859 artisan proposal gate (status → `proposed`, ownership link, `partner_product.proposed`). Default true. The admin and discover-copy paths have never applied it and turning it on for them would silently change what those routes publish. / applyArtisanGate?: boolean /** Channels to attach. Defaults to the store's default sales channel, which is what every partner-facing route has always forced. Discover-copy resolves its own channel, so it passes one explicitly rather than being overridden."
      },
      {
        "name": "seedInventoryLevels",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "#1380 / #1370 — ONE workflow behind every partner product create. Five paths reach `createProductsWorkflow`. That duplication is not cosmetic: it shipped the FX-fanout gap that became #1370 Open 1, and then hid the 504 from measurement because only one of the two live routes was instrumented. This workflow is the single place the post-create work is described, so a fix lands once instead of four times. The branching is the point. Measured on prod (2026-08-20, `80d8e10e0`): [partners/products]        total=11398ms ... inventoryLevels=7999ms [partners/stores/products] total=4399ms  ... inventoryLevels=4091ms 70% and 93% of those requests were spent in `ensureInventoryLevelsForVariants` — on payloads that are entirely `manage_inventory: false`, where the helper runs two `query.graph` calls and then returns having written nothing. The `when()` gates below mean that work only happens when there is actually something to seed. `createProductsWorkflow` already returns `manage_inventory` per variant, so the decision needs no extra read. / import { createWorkflow, createStep, StepResponse, WorkflowResponse, when, transform, } from \"@medusajs/framework/workflows-sdk\" import { MedusaError, Modules } from \"@medusajs/framework/utils\" import { createProductsWorkflow } from \"@medusajs/medusa/core-flows\" import { ensureInventoryLevelsForVariants } from \"../../api/partners/helpers\" import { requestVariantPriceFanout } from \"../fx/fanout-variant-prices\" import { isCoreChannelListingPartner, recordArtisanProposal, } from \"../../api/partners/products/lib/artisan-proposal\" export type CreatePartnerProductInput = { /** Owning partner. Optional because the admin \"create on behalf of\" route resolves an owner but does not run the partner-facing proposal gate. / partnerId?: string | null storeId: string /** Raw product payload. Route validators stay the source of truth for shape. */ product: Record<string, any> /** Legacy `POST /partners/products` refuses a store with no default sales channel (documented 400). The store-scoped route has always just skipped the injection instead. Preserved per-caller rather than unified, because changing either one is a wire-visible change to a live client. / requireSalesChannel?: boolean /** Run the #859 artisan proposal gate (status → `proposed`, ownership link, `partner_product.proposed`). Default true. The admin and discover-copy paths have never applied it and turning it on for them would silently change what those routes publish. / applyArtisanGate?: boolean /** Channels to attach. Defaults to the store's default sales channel, which is what every partner-facing route has always forced. Discover-copy resolves its own channel, so it passes one explicitly rather than being overridden. / salesChannelIds?: string[] /** Seed 0-quantity `inventory_level` rows for managed variants. Default true. Quick-create opts out because it writes a REAL quantity at the store's default location straight after, and seeding first would race it to the same (item, location) pair."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/create-partner-product.ts"
  },
  "list-partner-people": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partnerId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner/list-partner-people.ts"
  },
  "accept-quote": {
    "fields": [
      {
        "name": "quote_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.quote_id }}"
      },
      {
        "name": "allow_tax_divergence",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Accepting a quote (#1439 S11). Turns a frozen quote into a cart the buyer can pay a deposit on. Four things have to be true when this finishes, and each one has a step that refuses rather than a comment that hopes: 1. **The cart belongs to the buyer the quote was priced for.** The minted price list is ruled on that buyer's customer group, so a cart with no customer prices every line at BASE and the entire quote silently evaporates. `customer_id` is therefore set from the quote at creation time — before the first line is added — never from anything the caller sends. 2. **The cart charges the freight the quote froze.** See `createQuoteFreightOptionStep`: core will not accept an amount from a caller, so the frozen number is minted as a flat shipping option scoped to this one quote. 3. **The cart's numbers equal the quote's numbers.** Asserted, not assumed. A 200 from cart creation proves a cart exists; it says nothing about what it costs. Every wrong number this epic has produced was produced successfully. 4. **What is still owed is written down.** A `payment_schedule` row, keyed on the cart, splitting the total into a deposit and a balance. Idempotent by the quote's `accepted_cart_id`: a buyer who double-submits gets the same cart back, not a second one priced against the same price list. / export type AcceptQuoteInput = { quote_id: string /** Where the goods go. Optional — a quote already carries a destination country, postal code and city, which is what the freight was rated against. Anything passed here is merged over that, so a buyer can supply street-level detail at acceptance without being able to move the shipment to a country the quote was never priced for. / shipping_address?: { first_name?: string | null last_name?: string | null company?: string | null address_1?: string | null address_2?: string | null city?: string | null postal_code?: string | null province?: string | null phone?: string | null } | null /** Proceed even though the cart's tax does not match the tax the quote froze. 🔴 Off by default, and it should stay that way. Quote tax is resolved against the SELLER's jurisdiction (#1447 — quoting an Indian export at 19% German VAT lost deals silently), while a cart is taxed by core against the cart's own region. When those disagree, the buyer is about to be charged something other than what they were promised, and the honest response is to stop. The escape hatch exists so an operator can take a deal through knowingly, and it is recorded on the schedule when they do."
      },
      {
        "name": "dialled_lines",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.dialled_lines }}",
        "description": "Accepting a quote (#1439 S11). Turns a frozen quote into a cart the buyer can pay a deposit on. Four things have to be true when this finishes, and each one has a step that refuses rather than a comment that hopes: 1. **The cart belongs to the buyer the quote was priced for.** The minted price list is ruled on that buyer's customer group, so a cart with no customer prices every line at BASE and the entire quote silently evaporates. `customer_id` is therefore set from the quote at creation time — before the first line is added — never from anything the caller sends. 2. **The cart charges the freight the quote froze.** See `createQuoteFreightOptionStep`: core will not accept an amount from a caller, so the frozen number is minted as a flat shipping option scoped to this one quote. 3. **The cart's numbers equal the quote's numbers.** Asserted, not assumed. A 200 from cart creation proves a cart exists; it says nothing about what it costs. Every wrong number this epic has produced was produced successfully. 4. **What is still owed is written down.** A `payment_schedule` row, keyed on the cart, splitting the total into a deposit and a balance. Idempotent by the quote's `accepted_cart_id`: a buyer who double-submits gets the same cart back, not a second one priced against the same price list. / export type AcceptQuoteInput = { quote_id: string /** Where the goods go. Optional — a quote already carries a destination country, postal code and city, which is what the freight was rated against. Anything passed here is merged over that, so a buyer can supply street-level detail at acceptance without being able to move the shipment to a country the quote was never priced for. / shipping_address?: { first_name?: string | null last_name?: string | null company?: string | null address_1?: string | null address_2?: string | null city?: string | null postal_code?: string | null province?: string | null phone?: string | null } | null /** Proceed even though the cart's tax does not match the tax the quote froze. 🔴 Off by default, and it should stay that way. Quote tax is resolved against the SELLER's jurisdiction (#1447 — quoting an Indian export at 19% German VAT lost deals silently), while a cart is taxed by core against the cart's own region. When those disagree, the buyer is about to be charged something other than what they were promised, and the honest response is to stop. The escape hatch exists so an operator can take a deal through knowingly, and it is recorded on the schedule when they do. / allow_tax_divergence?: boolean /** The basket as the buyer actually dialled it (#1439 S13). The quote page has always let a buyer move quantities — `GET /store/b2b/quotes/:token?lines=` re-prices the whole view through them — but acceptance ignored this entirely and built the cart from the QUOTED quantities. A buyer who dialled 40 up from 29, saw the new total, and pressed accept got a cart for 29. Nothing anywhere said so. 🔑 Only quantities move. A variant not already on the quote is refused: the price list was minted for THIS basket, and a line nobody quoted has no frozen price to stand behind."
      },
      {
        "name": "now",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.now }}",
        "description": "Accepting a quote (#1439 S11). Turns a frozen quote into a cart the buyer can pay a deposit on. Four things have to be true when this finishes, and each one has a step that refuses rather than a comment that hopes: 1. **The cart belongs to the buyer the quote was priced for.** The minted price list is ruled on that buyer's customer group, so a cart with no customer prices every line at BASE and the entire quote silently evaporates. `customer_id` is therefore set from the quote at creation time — before the first line is added — never from anything the caller sends. 2. **The cart charges the freight the quote froze.** See `createQuoteFreightOptionStep`: core will not accept an amount from a caller, so the frozen number is minted as a flat shipping option scoped to this one quote. 3. **The cart's numbers equal the quote's numbers.** Asserted, not assumed. A 200 from cart creation proves a cart exists; it says nothing about what it costs. Every wrong number this epic has produced was produced successfully. 4. **What is still owed is written down.** A `payment_schedule` row, keyed on the cart, splitting the total into a deposit and a balance. Idempotent by the quote's `accepted_cart_id`: a buyer who double-submits gets the same cart back, not a second one priced against the same price list. / export type AcceptQuoteInput = { quote_id: string /** Where the goods go. Optional — a quote already carries a destination country, postal code and city, which is what the freight was rated against. Anything passed here is merged over that, so a buyer can supply street-level detail at acceptance without being able to move the shipment to a country the quote was never priced for. / shipping_address?: { first_name?: string | null last_name?: string | null company?: string | null address_1?: string | null address_2?: string | null city?: string | null postal_code?: string | null province?: string | null phone?: string | null } | null /** Proceed even though the cart's tax does not match the tax the quote froze. 🔴 Off by default, and it should stay that way. Quote tax is resolved against the SELLER's jurisdiction (#1447 — quoting an Indian export at 19% German VAT lost deals silently), while a cart is taxed by core against the cart's own region. When those disagree, the buyer is about to be charged something other than what they were promised, and the honest response is to stop. The escape hatch exists so an operator can take a deal through knowingly, and it is recorded on the schedule when they do. / allow_tax_divergence?: boolean /** The basket as the buyer actually dialled it (#1439 S13). The quote page has always let a buyer move quantities — `GET /store/b2b/quotes/:token?lines=` re-prices the whole view through them — but acceptance ignored this entirely and built the cart from the QUOTED quantities. A buyer who dialled 40 up from 29, saw the new total, and pressed accept got a cart for 29. Nothing anywhere said so. 🔑 Only quantities move. A variant not already on the quote is refused: the price list was minted for THIS basket, and a line nobody quoted has no frozen price to stand behind. / dialled_lines?: Array<{ variant_id: string; quantity: number }> | null /** Injected so acceptance is deterministic under test."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner-quote/accept-quote.ts"
  },
  "ensure-design-quote-variant": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "currency_code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.currency_code }}",
        "description": "Make a CUSTOM design quotable, without inventing a second pricing path. ## The problem this solves A design could only be quoted once a product existed behind it, because `resolveDesignVariants` resolves a design to the VARIANT it is sold through and everything downstream — tiers, the minted price list, freight weight, the accepted cart — is keyed on that variant. A design whose production run is in the FUTURE has no product, so the picker greyed it out and told the partner to go and create one, which is a real step they have to do by hand for work they have not sold yet. ## Why minting a variant rather than quoting the design directly The obvious alternative is a quote line that carries the design's estimated cost and no variant. It is the wrong shape: - `planQuotePrices` DROPS any line with no `variant_id`, deliberately, so the quote would mint a price list that prices nothing; - `accept-quote` builds a real cart from `variant_id` line items, so there would be nothing to accept; - and it would be the second way to arrive at a number the buyer pays, which is exactly what `design-lines.ts` refuses. So the estimate becomes the PRICE OF A VARIANT, once, here — and from that point on a custom design is quoted by the same machinery as everything else. ## What it refuses 🔴 A design that cannot be priced does NOT get a variant. Minting one at a price of zero, or at \"we could not tell\", would put an active price row in front of a buyer — the #1564 failure with an extra step. The refusal carries the estimator's own words so the partner learns what is missing. / export type EnsureDesignQuoteVariantInput = { design_id: string /** The currency the quote is denominated in."
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}",
        "description": "Make a CUSTOM design quotable, without inventing a second pricing path. ## The problem this solves A design could only be quoted once a product existed behind it, because `resolveDesignVariants` resolves a design to the VARIANT it is sold through and everything downstream — tiers, the minted price list, freight weight, the accepted cart — is keyed on that variant. A design whose production run is in the FUTURE has no product, so the picker greyed it out and told the partner to go and create one, which is a real step they have to do by hand for work they have not sold yet. ## Why minting a variant rather than quoting the design directly The obvious alternative is a quote line that carries the design's estimated cost and no variant. It is the wrong shape: - `planQuotePrices` DROPS any line with no `variant_id`, deliberately, so the quote would mint a price list that prices nothing; - `accept-quote` builds a real cart from `variant_id` line items, so there would be nothing to accept; - and it would be the second way to arrive at a number the buyer pays, which is exactly what `design-lines.ts` refuses. So the estimate becomes the PRICE OF A VARIANT, once, here — and from that point on a custom design is quoted by the same machinery as everything else. ## What it refuses 🔴 A design that cannot be priced does NOT get a variant. Minting one at a price of zero, or at \"we could not tell\", would put an active price row in front of a buyer — the #1564 failure with an extra step. The refusal carries the estimator's own words so the partner learns what is missing. / export type EnsureDesignQuoteVariantInput = { design_id: string /** The currency the quote is denominated in. */ currency_code: string /** Scopes visibility. Omit on the admin surface, as the picker does."
      },
      {
        "name": "markup_percent",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Make a CUSTOM design quotable, without inventing a second pricing path. ## The problem this solves A design could only be quoted once a product existed behind it, because `resolveDesignVariants` resolves a design to the VARIANT it is sold through and everything downstream — tiers, the minted price list, freight weight, the accepted cart — is keyed on that variant. A design whose production run is in the FUTURE has no product, so the picker greyed it out and told the partner to go and create one, which is a real step they have to do by hand for work they have not sold yet. ## Why minting a variant rather than quoting the design directly The obvious alternative is a quote line that carries the design's estimated cost and no variant. It is the wrong shape: - `planQuotePrices` DROPS any line with no `variant_id`, deliberately, so the quote would mint a price list that prices nothing; - `accept-quote` builds a real cart from `variant_id` line items, so there would be nothing to accept; - and it would be the second way to arrive at a number the buyer pays, which is exactly what `design-lines.ts` refuses. So the estimate becomes the PRICE OF A VARIANT, once, here — and from that point on a custom design is quoted by the same machinery as everything else. ## What it refuses 🔴 A design that cannot be priced does NOT get a variant. Minting one at a price of zero, or at \"we could not tell\", would put an active price row in front of a buyer — the #1564 failure with an extra step. The refusal carries the estimator's own words so the partner learns what is missing. / export type EnsureDesignQuoteVariantInput = { design_id: string /** The currency the quote is denominated in. */ currency_code: string /** Scopes visibility. Omit on the admin surface, as the picker does. */ partner_id?: string | null /** Override the 20% uplift. The wizard does not; ops might."
      },
      {
        "name": "catalogue_sales_channel_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.catalogue_sales_channel_id }}",
        "description": "Make a CUSTOM design quotable, without inventing a second pricing path. ## The problem this solves A design could only be quoted once a product existed behind it, because `resolveDesignVariants` resolves a design to the VARIANT it is sold through and everything downstream — tiers, the minted price list, freight weight, the accepted cart — is keyed on that variant. A design whose production run is in the FUTURE has no product, so the picker greyed it out and told the partner to go and create one, which is a real step they have to do by hand for work they have not sold yet. ## Why minting a variant rather than quoting the design directly The obvious alternative is a quote line that carries the design's estimated cost and no variant. It is the wrong shape: - `planQuotePrices` DROPS any line with no `variant_id`, deliberately, so the quote would mint a price list that prices nothing; - `accept-quote` builds a real cart from `variant_id` line items, so there would be nothing to accept; - and it would be the second way to arrive at a number the buyer pays, which is exactly what `design-lines.ts` refuses. So the estimate becomes the PRICE OF A VARIANT, once, here — and from that point on a custom design is quoted by the same machinery as everything else. ## What it refuses 🔴 A design that cannot be priced does NOT get a variant. Minting one at a price of zero, or at \"we could not tell\", would put an active price row in front of a buyer — the #1564 failure with an extra step. The refusal carries the estimator's own words so the partner learns what is missing. / export type EnsureDesignQuoteVariantInput = { design_id: string /** The currency the quote is denominated in. */ currency_code: string /** Scopes visibility. Omit on the admin surface, as the picker does. */ partner_id?: string | null /** Override the 20% uplift. The wizard does not; ops might. */ markup_percent?: number /** The catalogue the minted product belongs in — the quoting partner's default sales channel. 🔑 Deliberately NOT `partner_id`. That field scopes VISIBILITY, and on the admin surface it is passed as null on purpose: a design is not owned by anyone before a production run, so an admin quotes any design for any partner. Whose catalogue the product lands in is a different question with a different answer, and folding the two together would either re-scope the picker or put the product in nobody's catalogue — which is exactly what happened while this input did not exist."
      },
      {
        "name": "dry_run",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Make a CUSTOM design quotable, without inventing a second pricing path. ## The problem this solves A design could only be quoted once a product existed behind it, because `resolveDesignVariants` resolves a design to the VARIANT it is sold through and everything downstream — tiers, the minted price list, freight weight, the accepted cart — is keyed on that variant. A design whose production run is in the FUTURE has no product, so the picker greyed it out and told the partner to go and create one, which is a real step they have to do by hand for work they have not sold yet. ## Why minting a variant rather than quoting the design directly The obvious alternative is a quote line that carries the design's estimated cost and no variant. It is the wrong shape: - `planQuotePrices` DROPS any line with no `variant_id`, deliberately, so the quote would mint a price list that prices nothing; - `accept-quote` builds a real cart from `variant_id` line items, so there would be nothing to accept; - and it would be the second way to arrive at a number the buyer pays, which is exactly what `design-lines.ts` refuses. So the estimate becomes the PRICE OF A VARIANT, once, here — and from that point on a custom design is quoted by the same machinery as everything else. ## What it refuses 🔴 A design that cannot be priced does NOT get a variant. Minting one at a price of zero, or at \"we could not tell\", would put an active price row in front of a buyer — the #1564 failure with an extra step. The refusal carries the estimator's own words so the partner learns what is missing. / export type EnsureDesignQuoteVariantInput = { design_id: string /** The currency the quote is denominated in. */ currency_code: string /** Scopes visibility. Omit on the admin surface, as the picker does. */ partner_id?: string | null /** Override the 20% uplift. The wizard does not; ops might. */ markup_percent?: number /** The catalogue the minted product belongs in — the quoting partner's default sales channel. 🔑 Deliberately NOT `partner_id`. That field scopes VISIBILITY, and on the admin surface it is passed as null on purpose: a design is not owned by anyone before a production run, so an admin quotes any design for any partner. Whose catalogue the product lands in is a different question with a different answer, and folding the two together would either re-scope the picker or put the product in nobody's catalogue — which is exactly what happened while this input did not exist. / catalogue_sales_channel_id?: string | null /** Answer the question without creating anything. 🔑 The readiness preflight uses this. A preflight that MINTS a product as a side effect of being asked \"would this work\" leaves real catalogue rows behind every time a partner opens the wizard and changes their mind — and the whole reason readiness exists is that it is safe to ask."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner-quote/ensure-design-quote-variant.ts"
  },
  "mint-partner-quote": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "store",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "buyer_email",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.buyer_email }}"
      },
      {
        "name": "recipient_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.recipient_name }}"
      },
      {
        "name": "recipient_company",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.recipient_company }}"
      },
      {
        "name": "buyer_tax_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.buyer_tax_id }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified."
      },
      {
        "name": "buyer_tax_id_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.buyer_tax_id_type }}"
      },
      {
        "name": "partner_note",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.partner_note }}"
      },
      {
        "name": "destination_country_code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.destination_country_code }}"
      },
      {
        "name": "destination_postal_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.destination_postal_code }}"
      },
      {
        "name": "destination_city",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.destination_city }}"
      },
      {
        "name": "currency_code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.currency_code }}"
      },
      {
        "name": "region_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.region_id }}"
      },
      {
        "name": "carrier",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.carrier }}"
      },
      {
        "name": "duties_prepaid",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447)."
      },
      {
        "name": "duty_total",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.duty_total }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price."
      },
      {
        "name": "duty_basis",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.duty_basis }}"
      },
      {
        "name": "duty_rate_percent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.duty_rate_percent }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty."
      },
      {
        "name": "import_tax_rate_percent",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.import_tax_rate_percent }}"
      },
      {
        "name": "import_tax_total",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.import_tax_total }}"
      },
      {
        "name": "ddp_fee_total",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.ddp_fee_total }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty. / duty_rate_percent?: number | null import_tax_rate_percent?: number | null import_tax_total?: number | null /** The carrier's fee for advancing duty and tax. Always an amount."
      },
      {
        "name": "freight_override_amount",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.freight_override_amount }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty. / duty_rate_percent?: number | null import_tax_rate_percent?: number | null import_tax_total?: number | null /** The carrier's fee for advancing duty and tax. Always an amount. */ ddp_fee_total?: number | null /** Freight named by hand, in the QUOTE currency (#1439 S12). Replaces whatever the picker found, and satisfies the \"no freight option\" refusal — which is what makes a cross-border lane quotable at all today, since the carrier answers \"no serviceable couriers available for given weight\" and the stored option is flat at any weight."
      },
      {
        "name": "freight_basis",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.freight_basis }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty. / duty_rate_percent?: number | null import_tax_rate_percent?: number | null import_tax_total?: number | null /** The carrier's fee for advancing duty and tax. Always an amount. */ ddp_fee_total?: number | null /** Freight named by hand, in the QUOTE currency (#1439 S12). Replaces whatever the picker found, and satisfies the \"no freight option\" refusal — which is what makes a cross-border lane quotable at all today, since the carrier answers \"no serviceable couriers available for given weight\" and the stored option is flat at any weight. / freight_override_amount?: number | null /** Who quoted it and on what basis. Evidence, like `duty_basis`."
      },
      {
        "name": "ttl_days",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "deposit_pct",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.deposit_pct }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty. / duty_rate_percent?: number | null import_tax_rate_percent?: number | null import_tax_total?: number | null /** The carrier's fee for advancing duty and tax. Always an amount. */ ddp_fee_total?: number | null /** Freight named by hand, in the QUOTE currency (#1439 S12). Replaces whatever the picker found, and satisfies the \"no freight option\" refusal — which is what makes a cross-border lane quotable at all today, since the carrier answers \"no serviceable couriers available for given weight\" and the stored option is flat at any weight. / freight_override_amount?: number | null /** Who quoted it and on what basis. Evidence, like `duty_basis`. */ freight_basis?: string | null ttl_days?: number /** The deposit share of this deal, 0-100 (#1439 S11). Omit — or pass null — to fall through to the partner's house terms and then the platform's 30%. `0` is a real answer and is NOT treated as absent."
      },
      {
        "name": "created_by",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.created_by }}"
      },
      {
        "name": "now",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.now }}",
        "description": "The buyer's own registration, as stated. Recorded, never verified. */ buyer_tax_id?: string | null buyer_tax_id_type?: string | null partner_note?: string | null lines: Array<{ variant_id: string /** The design this line was picked as (#1486). Already resolved to `variant_id` by the route — carried here only so the frozen line can record which design the partner chose. It prices nothing. / design_id?: string | null quantity: number position?: number note?: string | null /** 0-100, off the live catalog price at this line's quantity (#1439 S7). */ discount_percent?: number | null /** A flat unit price, in the PARTNER STORE's default currency. */ override_unit_amount?: number | null /** The operator's own unit weight in grams, for a line the catalogue cannot weigh — a design quoted before its garment was ever weighed, or one of the 183 variants with no weight at either level. Prices this quote's freight only. It is frozen onto the line as `quoted_unit_weight_grams` with `quoted_weight_source: \"manual\"`, so the document records that a human supplied the figure, and it is never written back to the variant. / unit_weight_grams?: number | null }> destination_country_code: string destination_postal_code?: string | null destination_city?: string | null currency_code: string region_id?: string | null carrier?: string /** Quote as DDP — we pay the destination duty and import tax (#1447). */ duties_prepaid?: boolean /** The duty amount we are undertaking to pay, in the QUOTE currency, and the note saying how it was arrived at (#1447). Entered by hand because nothing can derive it yet: HS codes are incomplete and Shiprocket's tariff endpoint is gated on CSB-5 KYC. The validator refuses `duties_prepaid` without an amount, so no quote can promise duty cover and add nothing to the price. / duty_total?: number | null duty_basis?: string | null /** The rate form, and the normal one. The amounts are computed by `buildQuoteView` against the basket it actually priced — duty on goods + freight, import tax on goods + freight + duty. / duty_rate_percent?: number | null import_tax_rate_percent?: number | null import_tax_total?: number | null /** The carrier's fee for advancing duty and tax. Always an amount. */ ddp_fee_total?: number | null /** Freight named by hand, in the QUOTE currency (#1439 S12). Replaces whatever the picker found, and satisfies the \"no freight option\" refusal — which is what makes a cross-border lane quotable at all today, since the carrier answers \"no serviceable couriers available for given weight\" and the stored option is flat at any weight. / freight_override_amount?: number | null /** Who quoted it and on what basis. Evidence, like `duty_basis`. */ freight_basis?: string | null ttl_days?: number /** The deposit share of this deal, 0-100 (#1439 S11). Omit — or pass null — to fall through to the partner's house terms and then the platform's 30%. `0` is a real answer and is NOT treated as absent. / deposit_pct?: number | null created_by?: string | null /** Injected so the whole mint is deterministic under test."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner-quote/mint-quote.ts"
  },
  "cancel-partner-subscription": {
    "fields": [
      {
        "name": "subscription_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.subscription_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner-subscription/cancel-subscription.ts"
  },
  "create-partner-subscription": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "plan_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.plan_id }}"
      },
      {
        "name": "payment_provider",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.payment_provider }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partner-subscription/create-subscription.ts"
  },
  "seed-partner-plans": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/partner-subscription/seed-plans.ts"
  },
  "attach-storefront-domain": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "vercel_project_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.vercel_project_id }}",
        "description": "Roadmap #17 (issue #346) — a partner's custom domain should work in both its www and apex forms. The submitted host is the canonical one; the counterpart is attached to the same Vercel project as a permanent redirect, and BOTH are registered as `website_domain` aliases so the storefront's host-based website lookup resolves either form (the gap that left sharlho.com showing the generic \"Store\" template). / export type DomainPair = { /** The host the partner asked for — canonical, serves traffic. */ primary: string /** The www/apex twin — redirects to primary. Null for deeper subdomains. */ counterpart: string | null } /** apex → www twin; www → apex twin; anything deeper (shop.example.com) has no twin. Assumes an already-cleaned lowercase host. / export function deriveDomainPair(domain: string): DomainPair { const parts = domain.split(\".\") if (parts[0] === \"www\" && parts.length === 3) { return { primary: domain, counterpart: parts.slice(1).join(\".\") } } if (parts.length === 2) { return { primary: domain, counterpart: `www.${domain}` } } return { primary: domain, counterpart: null } } /** Read a partner's connected custom domain. Prefers the typed `custom_domain` column; falls back to the legacy `metadata.custom_domain` so a reader that runs during the deploy window (migration done, old rows) still resolves. / export function partnerCustomDomain(partner: any): string | undefined { const col = partner?.custom_domain if (typeof col === \"string\" && col) return col const meta = partner?.metadata?.custom_domain return typeof meta === \"string\" && meta ? meta : undefined } export function partnerCustomDomainVerified(partner: any): boolean { if (partner?.custom_domain_verified === true) return true // Only trust the legacy metadata flag when the column hasn't taken over yet. if (partner?.custom_domain) return partner.custom_domain_verified === true return partner?.metadata?.custom_domain_verified === true } export type AttachStorefrontDomainInput = { partner_id: string /** @deprecated kept for callers; the provider + project ref are now resolved from the partner."
      },
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "domain",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.domain }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partners/attach-storefront-domain.ts"
  },
  "delete-partner": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "force",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Delete even though orders are still in flight. See the planner's docs."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partners/delete-partner.ts"
  },
  "get-partner-storefront-status": {
    "fields": [
      {
        "name": "partner",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partner }}",
        "description": "The provider 404'd for a project we still hold a reference to — the refs are stale. The partner route clears them; the admin mirror only reports it. / stale_project?: boolean } export type GetPartnerStorefrontStatusWorkflowInput = { /** The full partner record — read for its provider/project/domain columns."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partners/get-partner-storefront-status.ts"
  },
  "list-partners": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "fields",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      },
      {
        "name": "offset",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "limit",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "order",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partners/list-partners.ts"
  },
  "resolve-partner-website": {
    "fields": [
      {
        "name": "partner",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partner }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/partners/resolve-partner-website.ts"
  },
  "set-default-payment-method": {
    "fields": [
      {
        "name": "payment_method_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.payment_method_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_methods/set-default-payment-method.ts"
  },
  "create-payment-report": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "period_start",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.period_start }}"
      },
      {
        "name": "period_end",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.period_end }}"
      },
      {
        "name": "entity_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.entity_type }}"
      },
      {
        "name": "entity_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.entity_id }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "payment_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.payment_type }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_reports/create-payment_reports.ts"
  },
  "delete-payment-report": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_reports/delete-payment_report.ts"
  },
  "list-payment-reports": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_reports/list-payment_reports.ts"
  },
  "settle-payment-reconciliation": {
    "fields": [
      {
        "name": "reconciliation_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.reconciliation_id }}"
      },
      {
        "name": "settled_by",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.settled_by }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_reports/settle-payment-reconciliation.ts"
  },
  "update-payment-report": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_reports/update-payment_report.ts"
  },
  "create-payment-submission": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "design_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.design_ids }}"
      },
      {
        "name": "task_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.task_ids }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "documents",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.documents }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below."
      },
      {
        "name": "quantities",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1."
      },
      {
        "name": "unit_amounts",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost."
      },
      {
        "name": "cost_overrides",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity."
      },
      {
        "name": "task_cost_overrides",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task."
      },
      {
        "name": "rate_breakdown",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task. */ task_cost_overrides?: Record<string, number> /** Per-piece prices within one design's line, keyed by design id (#1596). ⚠️ Typed only — never read from `metadata`. It has no legacy caller, so it never touches the untyped channel at all."
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task. */ task_cost_overrides?: Record<string, number> /** Per-piece prices within one design's line, keyed by design id (#1596). ⚠️ Typed only — never read from `metadata`. It has no legacy caller, so it never touches the untyped channel at all. / rate_breakdown?: Record<string, RateSlice[]> /** The status the submission lands in. \"Pending\" (the default) is a partner saying \"pay me for this\". \"Draft\" is the system pre-filling one FOR the partner — see `require_design_status` — which they then review and submit."
      },
      {
        "name": "require_design_status",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task. */ task_cost_overrides?: Record<string, number> /** Per-piece prices within one design's line, keyed by design id (#1596). ⚠️ Typed only — never read from `metadata`. It has no legacy caller, so it never touches the untyped channel at all. / rate_breakdown?: Record<string, RateSlice[]> /** The status the submission lands in. \"Pending\" (the default) is a partner saying \"pay me for this\". \"Draft\" is the system pre-filling one FOR the partner — see `require_design_status` — which they then review and submit. / status?: \"Draft\" | \"Pending\" /** Whether the design's own status must be Approved / Commerce_Ready. Default true: a partner submitting by hand is asserting the design work is finished, and the design status is the check on that assertion. The run-completion auto-draft passes false, because there the proof of finished work is the COMPLETED PRODUCTION RUN itself — and completion sets the design to Technical_Review, so a status check would reject every single auto-draft. Only the auto path may set this; the partner-facing route never does."
      },
      {
        "name": "production_run_ids",
        "type": "object",
        "required": false,
        "placeholder": "{}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task. */ task_cost_overrides?: Record<string, number> /** Per-piece prices within one design's line, keyed by design id (#1596). ⚠️ Typed only — never read from `metadata`. It has no legacy caller, so it never touches the untyped channel at all. / rate_breakdown?: Record<string, RateSlice[]> /** The status the submission lands in. \"Pending\" (the default) is a partner saying \"pay me for this\". \"Draft\" is the system pre-filling one FOR the partner — see `require_design_status` — which they then review and submit. / status?: \"Draft\" | \"Pending\" /** Whether the design's own status must be Approved / Commerce_Ready. Default true: a partner submitting by hand is asserting the design work is finished, and the design status is the check on that assertion. The run-completion auto-draft passes false, because there the proof of finished work is the COMPLETED PRODUCTION RUN itself — and completion sets the design to Technical_Review, so a status check would reject every single auto-draft. Only the auto path may set this; the partner-facing route never does. / require_design_status?: boolean /** Which completed production runs each design line pays for, keyed by design id. Optional — a hand-picked design line has no run behind it and stays exactly as it is today. 🔴 A typed input, never `metadata`. This is what stops the same finished run being paid for twice, and a guard that reads an untyped blob is one spelling mistake away from reading nothing at all (#1557)."
      },
      {
        "name": "currency",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.currency }}",
        "description": "⚠️ Free-form, and NOT where the money lives any more. The four fields below used to reach this workflow only through here, and every route validates `metadata` as `z.record(z.string(), z.any())` — which accepts anything. `design_quantities` and `design_quantites` both validated cleanly, and the typo fell through to \"absent means 1\" and billed a per-unit rate once (#1554 by spelling mistake, #1557). Reading them off `metadata` is kept ONLY as a fallback for callers that still post that way; the typed fields win outright. See `moneyOf` below. / metadata?: Record<string, any> /** What the caller is asking to be paid, as typed inputs rather than blob keys. 🔑 These are the contract now. Each corresponds to a `metadata.design_*` key the workflow used to read, and an explicit field REPLACES the whole corresponding map rather than merging key-by-key — a caller that sends `quantities` must not still be overridden by a stale blob it never wrote. / /** Units billed per design. Absent means 1. */ quantities?: Record<string, number> /** Agreed rate per unit, per design. Beats the design's stored cost. */ unit_amounts?: Record<string, number> /** Typed line TOTAL per design. Wins outright; never multiplied by quantity. */ cost_overrides?: Record<string, number> /** Typed line total per task. */ task_cost_overrides?: Record<string, number> /** Per-piece prices within one design's line, keyed by design id (#1596). ⚠️ Typed only — never read from `metadata`. It has no legacy caller, so it never touches the untyped channel at all. / rate_breakdown?: Record<string, RateSlice[]> /** The status the submission lands in. \"Pending\" (the default) is a partner saying \"pay me for this\". \"Draft\" is the system pre-filling one FOR the partner — see `require_design_status` — which they then review and submit. / status?: \"Draft\" | \"Pending\" /** Whether the design's own status must be Approved / Commerce_Ready. Default true: a partner submitting by hand is asserting the design work is finished, and the design status is the check on that assertion. The run-completion auto-draft passes false, because there the proof of finished work is the COMPLETED PRODUCTION RUN itself — and completion sets the design to Technical_Review, so a status check would reject every single auto-draft. Only the auto path may set this; the partner-facing route never does. / require_design_status?: boolean /** Which completed production runs each design line pays for, keyed by design id. Optional — a hand-picked design line has no run behind it and stays exactly as it is today. 🔴 A typed input, never `metadata`. This is what stops the same finished run being paid for twice, and a guard that reads an untyped blob is one spelling mistake away from reading nothing at all (#1557). / production_run_ids?: Record<string, string[]> /** What the payout is denominated in. Absent means the partner's own `currency_code`, and `inr` if they have none — see `lib/submission-currency`, which owns that precedence. ⚠️ This is the currency of the SUBMISSION, not of its sources. A line priced in another currency is converted into this one at an FX rate that is recorded on the line, never applied silently."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_submissions/create-payment-submission.ts"
  },
  "delete-payment-submission": {
    "fields": [
      {
        "name": "submission_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.submission_id }}"
      },
      {
        "name": "expected_partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.expected_partner_id }}",
        "description": "Remove a Draft payment submission (#1604). ## Draft only, and that restriction is the safety A Draft is machine-written: `auto-draft-payment-submission` mints one on every completed run, and when the sweep produces a bad one — the wrong design, a duplicate of a redo, a run whose price was later corrected — there was no way to remove it. They accumulate: seven of production's fifteen submissions are Drafts. Everything else is a decision somebody made. A Pending claim is a partner asking to be paid and its exit is `review`; an Approved or Paid submission is the record of money that moved, and deleting that record does not un-move it. So this refuses anything but a Draft rather than soft-deleting more widely and hoping the audit trail survives. 🔑 Soft delete, not a hard one — the run ids a draft claimed are evidence, and `softDelete` keeps the row queryable for anyone reconstructing why a design looked billed on a Tuesday. / export type DeletePaymentSubmissionInput = { submission_id: string /** When present, the submission must belong to this partner."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_submissions/delete-payment-submission.ts"
  },
  "review-payment-submission": {
    "fields": [
      {
        "name": "submission_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.submission_id }}"
      },
      {
        "name": "action",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.action }}"
      },
      {
        "name": "reviewed_by",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.reviewed_by }}"
      },
      {
        "name": "rejection_reason",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.rejection_reason }}"
      },
      {
        "name": "amount_override",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "payment_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.payment_type }}"
      },
      {
        "name": "paid_to_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.paid_to_id }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_submissions/review-payment-submission.ts"
  },
  "submit-payment-submission": {
    "fields": [
      {
        "name": "submission_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.submission_id }}"
      },
      {
        "name": "expected_partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.expected_partner_id }}",
        "description": "Turn a Draft payment submission into a real claim — Draft → Pending, in place. ## Why this exists (#1604, and the reason #1605 is a workaround) `auto-draft-payment-submission` writes a Draft on every completed run: the design, the partner, the rate, the quantity AND the run ids, all recorded. That draft is deliberately not a claim on anyone — the partner reviews it and submits when they're ready. Except there was no way to submit it. The only routes were create, review and two GETs, so \"submitting\" meant creating a SECOND submission by hand — which could not name the runs (the Draft already holds a live claim on them, so the run-level guard refuses), and therefore had to name none. #1602 closed the runless re-bill hole and, with it, the only path a drafted design had; #1605 reopened it by exempting Draft priors from that guard. That exemption is safe but it is not the answer: it means the strongest evidence we hold — the run ids the subscriber knew for certain — is thrown away every time a partner bills drafted work, and the line that reaches review reads `run_provenance: not_recorded`. Converting the Draft in place keeps the evidence and makes the runless path unnecessary. ## What it re-checks, and why it must A Draft can be minutes or months old. Between drafting and submitting, the same design or the same runs may have been billed by another submission. So every claim guard `create-payment-submission` runs is re-run here against the submission's OWN lines — with this submission excluded from the priors, since a claim cannot conflict with itself. 🔴 The same obligation lands on any future PATCH of `production_run_ids`. An edit route that skips these checks is an open window beside a locked door. / export type SubmitPaymentSubmissionInput = { submission_id: string /** When present, the submission must belong to this partner. The partner route passes it; the admin route does not, because an admin submitting on a partner's behalf is a legitimate (and, with 7 drafts stuck on production, necessary) operation."
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}",
        "description": "Turn a Draft payment submission into a real claim — Draft → Pending, in place. ## Why this exists (#1604, and the reason #1605 is a workaround) `auto-draft-payment-submission` writes a Draft on every completed run: the design, the partner, the rate, the quantity AND the run ids, all recorded. That draft is deliberately not a claim on anyone — the partner reviews it and submits when they're ready. Except there was no way to submit it. The only routes were create, review and two GETs, so \"submitting\" meant creating a SECOND submission by hand — which could not name the runs (the Draft already holds a live claim on them, so the run-level guard refuses), and therefore had to name none. #1602 closed the runless re-bill hole and, with it, the only path a drafted design had; #1605 reopened it by exempting Draft priors from that guard. That exemption is safe but it is not the answer: it means the strongest evidence we hold — the run ids the subscriber knew for certain — is thrown away every time a partner bills drafted work, and the line that reaches review reads `run_provenance: not_recorded`. Converting the Draft in place keeps the evidence and makes the runless path unnecessary. ## What it re-checks, and why it must A Draft can be minutes or months old. Between drafting and submitting, the same design or the same runs may have been billed by another submission. So every claim guard `create-payment-submission` runs is re-run here against the submission's OWN lines — with this submission excluded from the priors, since a claim cannot conflict with itself. 🔴 The same obligation lands on any future PATCH of `production_run_ids`. An edit route that skips these checks is an open window beside a locked door. / export type SubmitPaymentSubmissionInput = { submission_id: string /** When present, the submission must belong to this partner. The partner route passes it; the admin route does not, because an admin submitting on a partner's behalf is a legitimate (and, with 7 drafts stuck on production, necessary) operation. / expected_partner_id?: string /** Optional partner note, recorded at the moment of submission."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_submissions/submit-payment-submission.ts"
  },
  "update-payment-submission-item": {
    "fields": [
      {
        "name": "submission_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.submission_id }}"
      },
      {
        "name": "item_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.item_id }}"
      },
      {
        "name": "quantity",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Correct one line on a payment submission (#1604). ## The gap this closes Until now a submission had exactly two exits — approve or reject, wholesale. Nothing could change a line. That is not hypothetical: `audit-partner-payout-quantity` refuses to write by design (\"the correction is a payment decision rather than a data repair\") and tells an operator to correct the line; there was no route to correct the line. On production two lines sit in exactly that state, reported as `unmatched` and untouchable. ## 🔴 The guards are the point, not the edit `production_run_ids` is the column that decides whether the same work can be billed twice. An edit route that writes it without re-running the claim checks is an open window beside a locked door: submit naming no runs        → refused by the guard submit clean, then PATCH runs in → completely unguarded So every check `create-payment-submission` runs over claimed runs runs here too, with this submission excluded from the priors. ## Status contract Copied verbatim from `audit-partner-payout-quantity`: writes are honoured only on Draft or Pending. Nothing Approved, Under_Review or Paid is ever edited — the money there has moved or been committed, and rewriting the row would make our record disagree with what was actually paid without putting a rupee in anyone's hand. / export type UpdatePaymentSubmissionItemInput = { submission_id: string item_id: string /** Units this line pays for."
      },
      {
        "name": "unit_amount",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Correct one line on a payment submission (#1604). ## The gap this closes Until now a submission had exactly two exits — approve or reject, wholesale. Nothing could change a line. That is not hypothetical: `audit-partner-payout-quantity` refuses to write by design (\"the correction is a payment decision rather than a data repair\") and tells an operator to correct the line; there was no route to correct the line. On production two lines sit in exactly that state, reported as `unmatched` and untouchable. ## 🔴 The guards are the point, not the edit `production_run_ids` is the column that decides whether the same work can be billed twice. An edit route that writes it without re-running the claim checks is an open window beside a locked door: submit naming no runs        → refused by the guard submit clean, then PATCH runs in → completely unguarded So every check `create-payment-submission` runs over claimed runs runs here too, with this submission excluded from the priors. ## Status contract Copied verbatim from `audit-partner-payout-quantity`: writes are honoured only on Draft or Pending. Nothing Approved, Under_Review or Paid is ever edited — the money there has moved or been committed, and rewriting the row would make our record disagree with what was actually paid without putting a rupee in anyone's hand. / export type UpdatePaymentSubmissionItemInput = { submission_id: string item_id: string /** Units this line pays for. */ quantity?: number /** The agreed rate per unit. `amount` becomes quantity x this."
      },
      {
        "name": "amount",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "Correct one line on a payment submission (#1604). ## The gap this closes Until now a submission had exactly two exits — approve or reject, wholesale. Nothing could change a line. That is not hypothetical: `audit-partner-payout-quantity` refuses to write by design (\"the correction is a payment decision rather than a data repair\") and tells an operator to correct the line; there was no route to correct the line. On production two lines sit in exactly that state, reported as `unmatched` and untouchable. ## 🔴 The guards are the point, not the edit `production_run_ids` is the column that decides whether the same work can be billed twice. An edit route that writes it without re-running the claim checks is an open window beside a locked door: submit naming no runs        → refused by the guard submit clean, then PATCH runs in → completely unguarded So every check `create-payment-submission` runs over claimed runs runs here too, with this submission excluded from the priors. ## Status contract Copied verbatim from `audit-partner-payout-quantity`: writes are honoured only on Draft or Pending. Nothing Approved, Under_Review or Paid is ever edited — the money there has moved or been committed, and rewriting the row would make our record disagree with what was actually paid without putting a rupee in anyone's hand. / export type UpdatePaymentSubmissionItemInput = { submission_id: string item_id: string /** Units this line pays for. */ quantity?: number /** The agreed rate per unit. `amount` becomes quantity x this. */ unit_amount?: number /** An explicit line TOTAL. Wins over `unit_amount` exactly as a cost override does at create, and clears `unit_amount` for the same reason: there is no recorded rate behind a typed total, and dividing it back out invents one."
      },
      {
        "name": "production_run_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.production_run_ids }}",
        "description": "Correct one line on a payment submission (#1604). ## The gap this closes Until now a submission had exactly two exits — approve or reject, wholesale. Nothing could change a line. That is not hypothetical: `audit-partner-payout-quantity` refuses to write by design (\"the correction is a payment decision rather than a data repair\") and tells an operator to correct the line; there was no route to correct the line. On production two lines sit in exactly that state, reported as `unmatched` and untouchable. ## 🔴 The guards are the point, not the edit `production_run_ids` is the column that decides whether the same work can be billed twice. An edit route that writes it without re-running the claim checks is an open window beside a locked door: submit naming no runs        → refused by the guard submit clean, then PATCH runs in → completely unguarded So every check `create-payment-submission` runs over claimed runs runs here too, with this submission excluded from the priors. ## Status contract Copied verbatim from `audit-partner-payout-quantity`: writes are honoured only on Draft or Pending. Nothing Approved, Under_Review or Paid is ever edited — the money there has moved or been committed, and rewriting the row would make our record disagree with what was actually paid without putting a rupee in anyone's hand. / export type UpdatePaymentSubmissionItemInput = { submission_id: string item_id: string /** Units this line pays for. */ quantity?: number /** The agreed rate per unit. `amount` becomes quantity x this. */ unit_amount?: number /** An explicit line TOTAL. Wins over `unit_amount` exactly as a cost override does at create, and clears `unit_amount` for the same reason: there is no recorded rate behind a typed total, and dividing it back out invents one. / amount?: number /** The runs this line pays for. `[]` is a real value meaning \"this line names no runs\" — which is why it is checked against the runless guard rather than waved through as an absence."
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/payment_submissions/update-payment-submission-item.ts"
  },
  "create-person-type": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/person_type/create-person_type.ts"
  },
  "delete-person-type": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/person_type/delete-person_type.ts"
  },
  "list-person-type": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/person_type/list-person_type.ts"
  },
  "update-person-type": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/person_type/update-person_type.ts"
  },
  "associate-person-types": {
    "fields": [
      {
        "name": "personId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.personId }}"
      },
      {
        "name": "typeIds",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.typeIds }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/associate-person-types.ts"
  },
  "batch-geocode-addresses": {
    "fields": [
      {
        "name": "addresses",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.addresses }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/batch-geocode-addresses.ts"
  },
  "create-address": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "street",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.street }}"
      },
      {
        "name": "city",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.city }}"
      },
      {
        "name": "state",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.state }}"
      },
      {
        "name": "postal_code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.postal_code }}"
      },
      {
        "name": "country",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.country }}"
      },
      {
        "name": "latitude",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "longitude",
        "type": "number",
        "required": true,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/create-address.ts"
  },
  "create-contact": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "value",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.value }}"
      },
      {
        "name": "is_primary",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/create-contact.ts"
  },
  "create-person-sub": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "subscription_type",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.subscription_type }}"
      },
      {
        "name": "network",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.network }}"
      },
      {
        "name": "subscription_status",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.subscription_status }}"
      },
      {
        "name": "email_subscribed",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.email_subscribed }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/create-person-subs.ts"
  },
  "create-person-tags": {
    "fields": [
      {
        "name": "name",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/create-person-tags.ts"
  },
  "delete-address": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/delete-address.ts"
  },
  "delete-person-tags": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/delete-person-tags.ts"
  },
  "geocode-address-workflow": {
    "fields": [
      {
        "name": "person_address_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_address_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/geocode-address.ts"
  },
  "list-and-count-persons-with-filter": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "withDeleted",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "fields",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.fields }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/list-and-count-with-filter/list-and-count-with-filter.ts"
  },
  "list-public-persons": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "withDeleted",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/list-public-persons.ts"
  },
  "retrieve-addresses": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/retrieve-addresses.ts"
  },
  "retrieve-contacts": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/retrieve-contacts.ts"
  },
  "retrieve-person-tags": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/retrieve-person-tags.ts"
  },
  "update-address": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/update-address.ts"
  },
  "update-contact": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/update-contact.ts"
  },
  "update-person-tags": {
    "fields": [
      {
        "name": "person_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.person_id }}"
      },
      {
        "name": "name",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/persons/update-person-tags.ts"
  },
  "accept-production-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/accept-production-run.ts"
  },
  "admin-accept-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/admin-accept-production-run.ts"
  },
  "admin-complete-production-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "override_note",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.override_note }}",
        "description": "Admin: complete a production run on the partner's behalf, from a message. Why this exists as its own workflow rather than a few writes in the route. `completeProductionRunWorkflow` gates on `assertCanCompleteWork`, which requires BOTH `status ∈ complete_work_from` (prod: `[\"in_progress\"]`) and a `finished_at`. In the WhatsApp-driven flow the partner never formally accepted, started or finished the run in the partner app — they said so in a message — so the run is typically still `sent_to_partner` with no lifecycle timestamps at all. Something has to bring it to a completable state first. The order of operations IS the safety property here. Stamping the timestamps and then letting the gate run turns a rejection into a corruption: the run is left permanently claiming it was started and finished at a moment it wasn't, and is pre-armed to slip past the `finished_at` gate on some later call. So: 1. `evaluateAdminCompletionOverride` decides — from the policy, before any write — whether this run may be overridden at all, and what the override would have to change. It is PURE, so the decision is unit-testable without a DB. 2. The override is applied in a step that captures the previous values and restores them in its compensation. 3. `completeProductionRunWorkflow` runs as a nested step. If it throws for any reason, the engine compensates step 2 and the run goes back to exactly the state it was in. The promotable set is derived from the policy rather than hardcoded, for the same reason `partner-run-steps` defers to it: allowed transitions live in ONE place. A run may be overridden from a status that could legitimately reach `in_progress` in a single step (`accept_from`) or that is already completable (`complete_work_from`). Anything else — a draft, an unapproved run, one parked for reassignment — is refused with no write at all. / import { MedusaError } from \"@medusajs/framework/utils\" import { StepResponse, WorkflowResponse, createStep, createWorkflow, } from \"@medusajs/framework/workflows-sdk\" import { PRODUCTION_RUNS_MODULE } from \"../../modules/production_runs\" import type ProductionRunService from \"../../modules/production_runs/service\" import { PRODUCTION_POLICY_MODULE } from \"../../modules/production_policy\" import type ProductionPolicyService from \"../../modules/production_policy/service\" import { completeProductionRunWorkflow, type CompleteProductionRunInput, } from \"./complete-production-run\" export type AdminCompleteProductionRunInput = CompleteProductionRunInput & { /** Free-form partner message this completion was raised from, if any."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/admin-complete-production-run.ts"
  },
  "admin-finish-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/admin-finish-production-run.ts"
  },
  "admin-start-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/admin-start-production-run.ts"
  },
  "approve-production-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "assignments",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.assignments }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/approve-production-run.ts"
  },
  "assign-production-run-partner": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "note",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.note }}",
        "description": "#1228 — the manual half of #1093's reassignment story. #1093 built the automatic path (reminders → cap → park in `awaiting_reassignment`, partner unassigned, tasks cancelled) but nothing that moves a parked run back OUT. This workflow is that missing half: an admin points the run at a partner — the same one who let it go stale, or a different one — and it lands back on `approved`, ready for the ordinary dispatch flow. Deliberately stops at `approved` rather than dispatching itself. Dispatch is what collects template names, and template names are what seed the partner's tasks; short-circuiting it would hand a partner a run with no work items. / export type AssignProductionRunPartnerSource = \"manual\" | \"reminder_retry\" export type AssignProductionRunPartnerInput = { production_run_id: string partner_id: string /** Free-text admin note, recorded on the activity feed."
      },
      {
        "name": "source",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.source }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/assign-production-run-partner.ts"
  },
  "attach-media-to-production-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "media_url",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.media_url }}"
      },
      {
        "name": "media_mime_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.media_mime_type }}"
      },
      {
        "name": "filename",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.filename }}"
      },
      {
        "name": "message_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.message_id }}"
      },
      {
        "name": "conversation_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.conversation_id }}"
      },
      {
        "name": "actor_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.actor_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/attach-media-to-production-run.ts"
  },
  "complete-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/complete-production-run.ts"
  },
  "complete-provenance-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "produced_quantity",
        "type": "number",
        "required": false,
        "placeholder": "0"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/complete-provenance-run.ts"
  },
  "create-production-run-transfer": {
    "fields": [
      {
        "name": "productionRunId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productionRunId }}"
      },
      {
        "name": "toLocationId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.toLocationId }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location."
      },
      {
        "name": "fromLocationId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.fromLocationId }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below)."
      },
      {
        "name": "reason",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.reason }}"
      },
      {
        "name": "quantity",
        "type": "number",
        "required": false,
        "placeholder": "0",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below). */ fromLocationId?: string reason?: \"finishing\" | \"qc\" | \"packaging\" | \"stock\" | \"customer\" | \"other\" /** Units moving in this hop; defaults to what the run produced."
      },
      {
        "name": "carrier",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.carrier }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below). */ fromLocationId?: string reason?: \"finishing\" | \"qc\" | \"packaging\" | \"stock\" | \"customer\" | \"other\" /** Units moving in this hop; defaults to what the run produced. */ quantity?: number /** Book with this carrier. Omitted → a `draft` transfer with no carrier booking (a self-driven hop), which is a legitimate movement, not an error."
      },
      {
        "name": "weightGrams",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "dimensionsCm",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.dimensionsCm }}"
      },
      {
        "name": "preferredCourierId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.preferredCourierId }}"
      },
      {
        "name": "actingEmail",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.actingEmail }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below). */ fromLocationId?: string reason?: \"finishing\" | \"qc\" | \"packaging\" | \"stock\" | \"customer\" | \"other\" /** Units moving in this hop; defaults to what the run produced. */ quantity?: number /** Book with this carrier. Omitted → a `draft` transfer with no carrier booking (a self-driven hop), which is a legitimate movement, not an error. / carrier?: string weightGrams?: number dimensionsCm?: Dimensions & { breadth?: number } preferredCourierId?: string | number /** Acting user's email recorded on a freshly-registered pickup (#427)."
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "replacesTransferId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.replacesTransferId }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below). */ fromLocationId?: string reason?: \"finishing\" | \"qc\" | \"packaging\" | \"stock\" | \"customer\" | \"other\" /** Units moving in this hop; defaults to what the run produced. */ quantity?: number /** Book with this carrier. Omitted → a `draft` transfer with no carrier booking (a self-driven hop), which is a legitimate movement, not an error. / carrier?: string weightGrams?: number dimensionsCm?: Dimensions & { breadth?: number } preferredCourierId?: string | number /** Acting user's email recorded on a freshly-registered pickup (#427). */ actingEmail?: string notes?: string /** The cancelled hop this one replaces (#891 follow-up). 🔑 A re-booking is a NEW row, never an edit of the old one. The original carries an AWB the carrier really issued and really cancelled; rewriting it would erase the fact that the first attempt happened, and the transfer list is the only record of what physically moved. So both rows survive and point at each other — `replaces_transfer_id` here, `replaced_by_transfer_id` there. 🔴 Only a CANCELLED transfer can be replaced. Replacing a live one would leave two open movements for the same goods, and the second would silently double-count the moment inventory learns to follow a transfer (S3)."
      },
      {
        "name": "audience",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.audience }}",
        "description": "#891 — move a production run's output from where it was made to wherever it needs to go next (a finishing partner, a QC/packaging warehouse, or stock). Reuses the shipment machinery wholesale: `resolveShippingProvider` (so both Shiprocket and Delhivery work here on day one), `ensureCarrierPickup` for the origin, `resolveInventoryDestinationAddress` for the destination, and an `inventory_shipment` row for the carrier refs so the existing tracking webhook needs no new routing. What's new is only the `goods_transfer` row recording the movement's intent. A carrier booking is OPTIONAL. A hop between two of our own locations is frequently someone driving a van, and that is still a real transfer whose inventory consequences matter — so `carrier: undefined` records a `draft` transfer with no AWB rather than refusing the movement. / export type CreateProductionRunTransferInput = { productionRunId: string /** Destination stock location. */ toLocationId: string /** Origin; defaults to the run's own goods location (see below). */ fromLocationId?: string reason?: \"finishing\" | \"qc\" | \"packaging\" | \"stock\" | \"customer\" | \"other\" /** Units moving in this hop; defaults to what the run produced. */ quantity?: number /** Book with this carrier. Omitted → a `draft` transfer with no carrier booking (a self-driven hop), which is a legitimate movement, not an error. / carrier?: string weightGrams?: number dimensionsCm?: Dimensions & { breadth?: number } preferredCourierId?: string | number /** Acting user's email recorded on a freshly-registered pickup (#427). */ actingEmail?: string notes?: string /** The cancelled hop this one replaces (#891 follow-up). 🔑 A re-booking is a NEW row, never an edit of the old one. The original carries an AWB the carrier really issued and really cancelled; rewriting it would erase the fact that the first attempt happened, and the transfer list is the only record of what physically moved. So both rows survive and point at each other — `replaces_transfer_id` here, `replaced_by_transfer_id` there. 🔴 Only a CANCELLED transfer can be replaced. Replacing a live one would leave two open movements for the same goods, and the second would silently double-count the moment inventory learns to follow a transfer (S3). / replacesTransferId?: string /** Who reads the error — carrier-account failures are not a partner's doing."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/create-production-run-transfer.ts"
  },
  "create-production-run": {
    "fields": [
      {
        "name": "design_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "quantity",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.quantity }}",
        "description": "The agreed quantity. Absent = 1, as always; explicit `null` = there is NO agreed amount, an open-ended run that opts out of the payment ceiling (#1676). The two are not interchangeable here."
      },
      {
        "name": "produced_quantity",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "run_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.run_type }}"
      },
      {
        "name": "product_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.product_id }}"
      },
      {
        "name": "variant_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.variant_id }}"
      },
      {
        "name": "order_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.order_id }}"
      },
      {
        "name": "order_line_item_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.order_line_item_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "task_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.task_ids }}"
      },
      {
        "name": "materials",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.materials }}",
        "description": "The agreed quantity. Absent = 1, as always; explicit `null` = there is NO agreed amount, an open-ended run that opts out of the payment ceiling (#1676). The two are not interchangeable here. / quantity?: number | null // #1112 — for runs born already-`completed` (retail fulfillment), stamp the // produced yield up front instead of leaving it for the lifecycle to fill. produced_quantity?: number run_type?: \"production\" | \"sample\" product_id?: string variant_id?: string order_id?: string order_line_item_id?: string metadata?: Record<string, any> task_ids?: string[] /** The per-assignment material allocation: WHICH of the design's inventory items this run is actually sent out with, and how much of each. Omit to keep the historical behaviour — the whole design BOM stays available to the partner. Must be a subset of the design's linked inventory items."
      },
      {
        "name": "execution_mode",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.execution_mode }}"
      },
      {
        "name": "sub_partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.sub_partner_id }}"
      },
      {
        "name": "skip_unified_projection",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/create-production-run.ts"
  },
  "emit-production-run-reminder": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}",
        "description": "#1279 — a run parked in `awaiting_reassignment`. Unlike every other kind this one has NO partner (parking nulls `partner_id` and keeps `previous_partner_id` for audit), so it is not a nag — it is an admin escalation. It exists because parking used to be terminal in practice: the reminder flow read only `sent_to_partner`/`in_progress`, so the moment the cap parked a run, the machinery that parked it could no longer see it. Six runs sat that way until a human noticed, the oldest for 4.5 months. / | \"awaiting_reassignment\" export type EmitProductionRunReminderInput = { production_run_id: string /** Null only for `awaiting_reassignment` — a parked run has no partner."
      },
      {
        "name": "design_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "reminder_kind",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.reminder_kind }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/emit-production-run-reminder.ts"
  },
  "finish-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/finish-production-run.ts"
  },
  "list-partner-production-runs": {
    "fields": [
      {
        "name": "partnerId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.partnerId }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "role",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.role }}"
      },
      {
        "name": "run_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.run_type }}"
      },
      {
        "name": "design_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.design_id }}"
      },
      {
        "name": "offset",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "limit",
        "type": "number",
        "required": true,
        "placeholder": "0"
      },
      {
        "name": "locale",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.locale }}",
        "description": "Forwarded to `query.graph` for translated fields; the route reads it off the request."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/list-partner-production-runs.ts"
  },
  "signal-lifecycle-step-success": {
    "fields": [
      {
        "name": "transaction_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.transaction_id }}"
      },
      {
        "name": "step_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.step_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/production-run-steps.ts"
  },
  "reassign-production-run": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "reason",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.reason }}",
        "description": "#1093 — move a run into the admin reassignment queue. Triggered when the assigned partner never accepted (2 reminders sent, then capped) or explicitly declined. The run is NOT cancelled — the work still needs doing — it is unassigned and parked in `awaiting_reassignment` so an admin can re-dispatch it to a new partner (reuses dispatch-production-run). Deliberately does NOT mirror the order as cancelled (unlike the old decline path): the customer's order is unaffected by one partner stepping away. / export type ReassignSource = \"reminder_cap\" | \"decline\" export type ReassignProductionRunInput = { production_run_id: string /** Raw reason code (e.g. decline reason, or \"reminder_cap\")."
      },
      {
        "name": "source",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.source }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.partner_id }}",
        "description": "#1093 — move a run into the admin reassignment queue. Triggered when the assigned partner never accepted (2 reminders sent, then capped) or explicitly declined. The run is NOT cancelled — the work still needs doing — it is unassigned and parked in `awaiting_reassignment` so an admin can re-dispatch it to a new partner (reuses dispatch-production-run). Deliberately does NOT mirror the order as cancelled (unlike the old decline path): the customer's order is unaffected by one partner stepping away. / export type ReassignSource = \"reminder_cap\" | \"decline\" export type ReassignProductionRunInput = { production_run_id: string /** Raw reason code (e.g. decline reason, or \"reminder_cap\"). */ reason: string source: ReassignSource /** The partner being unassigned (for the emitted events / audit)."
      },
      {
        "name": "composed_reason",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.composed_reason }}",
        "description": "#1093 — move a run into the admin reassignment queue. Triggered when the assigned partner never accepted (2 reminders sent, then capped) or explicitly declined. The run is NOT cancelled — the work still needs doing — it is unassigned and parked in `awaiting_reassignment` so an admin can re-dispatch it to a new partner (reuses dispatch-production-run). Deliberately does NOT mirror the order as cancelled (unlike the old decline path): the customer's order is unaffected by one partner stepping away. / export type ReassignSource = \"reminder_cap\" | \"decline\" export type ReassignProductionRunInput = { production_run_id: string /** Raw reason code (e.g. decline reason, or \"reminder_cap\"). */ reason: string source: ReassignSource /** The partner being unassigned (for the emitted events / audit). */ partner_id?: string | null /** Attribution-prefixed reason string stored on the run for the admin feed."
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/reassign-production-run.ts"
  },
  "recreate-production-run": {
    "fields": [
      {
        "name": "designs",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.designs }}"
      },
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "run_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.run_type }}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/recreate-production-run.ts"
  },
  "send-production-run-to-production": {
    "fields": [
      {
        "name": "production_run_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.production_run_id }}"
      },
      {
        "name": "template_names",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.template_names }}",
        "description": "Templates BY NAME. Kept because most names are unambiguous and every existing caller uses them, but a name is not an identity: prod carries two rows called \"Stitching\" that differ only by category (Pre Production vs Production), i.e. two different stages of the process wearing one label. A name that matches more than one row is now REJECTED rather than resolved — see `resolveDispatchTemplateIdsStep`."
      },
      {
        "name": "template_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.template_ids }}",
        "description": "Templates BY NAME. Kept because most names are unambiguous and every existing caller uses them, but a name is not an identity: prod carries two rows called \"Stitching\" that differ only by category (Pre Production vs Production), i.e. two different stages of the process wearing one label. A name that matches more than one row is now REJECTED rather than resolved — see `resolveDispatchTemplateIdsStep`. / template_names?: string[] /** Templates BY ID — preferred. The only way to say which \"Stitching\" you mean. Takes precedence over `template_names` when both are given."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/production-runs/send-production-run-to-production.ts"
  },
  "start-production-run": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/production-runs/start-production-run.ts"
  },
  "generate-product-description-from-image": {
    "fields": [
      {
        "name": "imageUrl",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.imageUrl }}"
      },
      {
        "name": "hint",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.hint }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/gen-ai-desc.ts"
  },
  "link-product-with-design": {
    "fields": [
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/link-unlink-products-with-designs.ts"
  },
  "unlink-product-from-design": {
    "fields": [
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/link-unlink-products-with-designs.ts"
  },
  "link-multiple-products-with-design": {
    "fields": [
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "designId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.designId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/link-unlink-products-with-designs.ts"
  },
  "link-product-with-person": {
    "fields": [
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "personId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.personId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/link-unlink-products-with-people.ts"
  },
  "unlink-product-from-person": {
    "fields": [
      {
        "name": "productId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.productId }}"
      },
      {
        "name": "personId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.personId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/link-unlink-products-with-people.ts"
  },
  "upsert-artisan-product-detail": {
    "fields": [
      {
        "name": "made_to_order",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      },
      {
        "name": "lead_time_days",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.lead_time_days }}"
      },
      {
        "name": "lead_time_label",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.lead_time_label }}"
      },
      {
        "name": "min_order_quantity",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.min_order_quantity }}"
      },
      {
        "name": "maker_story",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.maker_story }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/upsert-artisan-product-detail.ts"
  },
  "upsert-product-spec": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "hex_code",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.hex_code }}"
      },
      {
        "name": "usage_notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.usage_notes }}"
      },
      {
        "name": "order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "available",
        "type": "boolean",
        "required": false,
        "placeholder": "false"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/products/upsert-product-spec.ts"
  },
  "create-raw-material": {
    "fields": [
      {
        "name": "inventoryId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.inventoryId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/raw-materials/create-raw-material.ts"
  },
  "list-raw-materials-categories": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/raw-materials/list-raw-material-category.ts"
  },
  "resolve-group-color-inventory-items": {
    "fields": [
      {
        "name": "lines",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.lines }}"
      },
      {
        "name": "stock_location_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.stock_location_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/raw_material_groups/resolve-group-color-items.ts"
  },
  "propagate-region-to-partners": {
    "fields": [
      {
        "name": "region_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.region_id }}",
        "description": "Workflow: propagate-region-to-partners When admin creates (or updates) a region, every active partner needs the same plumbing the 0A backfill did manually: 1. `partner_region` link from the partner to the region. 2. `store.supported_currencies` extended with the region's currency_code if missing. 3. (Optional) FX fanout against every existing variant price on the partner's store so converted price rows materialize in the new currency. Called by: - `src/subscribers/region-propagate.ts` on `region.created`. - `POST /admin/regions/:id/share-to-all` for manual re-runs. Companion of `src/scripts/backfill-all-admin-regions-to-partners.ts` and friends — those are the one-off batch path for retrofitting the existing platform; this workflow is the structural fix so new regions self-propagate. Idempotent: skips link pairs that already exist, skips currencies already in supported_currencies, and the FX workflow has its own recursion guard on auto-derived prices. Safe to re-run. / export type PropagateRegionInput = { /** Region whose state should propagate to partners."
      },
      {
        "name": "partner_ids",
        "type": "array",
        "required": false,
        "placeholder": "{{ $last.partner_ids }}",
        "description": "Workflow: propagate-region-to-partners When admin creates (or updates) a region, every active partner needs the same plumbing the 0A backfill did manually: 1. `partner_region` link from the partner to the region. 2. `store.supported_currencies` extended with the region's currency_code if missing. 3. (Optional) FX fanout against every existing variant price on the partner's store so converted price rows materialize in the new currency. Called by: - `src/subscribers/region-propagate.ts` on `region.created`. - `POST /admin/regions/:id/share-to-all` for manual re-runs. Companion of `src/scripts/backfill-all-admin-regions-to-partners.ts` and friends — those are the one-off batch path for retrofitting the existing platform; this workflow is the structural fix so new regions self-propagate. Idempotent: skips link pairs that already exist, skips currencies already in supported_currencies, and the FX workflow has its own recursion guard on auto-derived prices. Safe to re-run. / export type PropagateRegionInput = { /** Region whose state should propagate to partners. */ region_id: string /** Optional: scope to a subset of partners. Default = every partner with at least one store."
      },
      {
        "name": "trigger_fanout",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Workflow: propagate-region-to-partners When admin creates (or updates) a region, every active partner needs the same plumbing the 0A backfill did manually: 1. `partner_region` link from the partner to the region. 2. `store.supported_currencies` extended with the region's currency_code if missing. 3. (Optional) FX fanout against every existing variant price on the partner's store so converted price rows materialize in the new currency. Called by: - `src/subscribers/region-propagate.ts` on `region.created`. - `POST /admin/regions/:id/share-to-all` for manual re-runs. Companion of `src/scripts/backfill-all-admin-regions-to-partners.ts` and friends — those are the one-off batch path for retrofitting the existing platform; this workflow is the structural fix so new regions self-propagate. Idempotent: skips link pairs that already exist, skips currencies already in supported_currencies, and the FX workflow has its own recursion guard on auto-derived prices. Safe to re-run. / export type PropagateRegionInput = { /** Region whose state should propagate to partners. */ region_id: string /** Optional: scope to a subset of partners. Default = every partner with at least one store. / partner_ids?: string[] /** When true, also kick off FX fanout against every existing variant price on the affected stores so converted rows materialize for the new currency immediately. Default false (cheap path: partners' next variant save will fan out automatically)."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/regions/propagate-region-to-partners.ts"
  },
  "create--social-platform": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "category",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.category }}"
      },
      {
        "name": "auth_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.auth_type }}"
      },
      {
        "name": "icon_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.icon_url }}"
      },
      {
        "name": "base_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.base_url }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "api_config",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/create-social-platform.ts"
  },
  "create-social-post": {
    "fields": [
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "post_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.post_url }}"
      },
      {
        "name": "caption",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.caption }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "scheduled_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.scheduled_at }}"
      },
      {
        "name": "posted_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.posted_at }}"
      },
      {
        "name": "insights",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "media_attachments",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "error_message",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.error_message }}"
      },
      {
        "name": "related_item_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.related_item_type }}"
      },
      {
        "name": "related_item_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.related_item_id }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/create-social-post.ts"
  },
  "delete--social-platform": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/delete-social-platform.ts"
  },
  "delete-social-post": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/delete-social-post.ts"
  },
  "exchange-token-workflow": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "platform",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.platform }}"
      },
      {
        "name": "code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.code }}"
      },
      {
        "name": "codeVerifier",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.codeVerifier }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/exchange-token.ts"
  },
  "extract-hashtags-mentions-workflow": {
    "fields": [
      {
        "name": "caption",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.caption }}"
      },
      {
        "name": "platform_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.platform_name }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/extract-hashtags-mentions.ts"
  },
  "list--social-platform": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/list-social-platform.ts"
  },
  "list-social-post": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/list-social-post.ts"
  },
  "oauth-callback-workflow": {
    "fields": [
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "platform",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.platform }}"
      },
      {
        "name": "code",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.code }}"
      },
      {
        "name": "state",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.state }}"
      },
      {
        "name": "redirect_uri",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.redirect_uri }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/oauth-callback.ts"
  },
  "publish-social-post-workflow": {
    "fields": [
      {
        "name": "post_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.post_id }}"
      },
      {
        "name": "page_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.page_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/publish-post.ts"
  },
  "publish-social-post-unified": {
    "fields": [
      {
        "name": "post_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.post_id }}"
      },
      {
        "name": "override_page_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.override_page_id }}"
      },
      {
        "name": "override_ig_user_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.override_ig_user_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/publish-social-post-unified.ts"
  },
  "publish-to-both-platforms-series": {
    "fields": [
      {
        "name": "pageId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pageId }}"
      },
      {
        "name": "igUserId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.igUserId }}"
      },
      {
        "name": "userAccessToken",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.userAccessToken }}"
      },
      {
        "name": "publishTarget",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.publishTarget }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/publish-to-both-platforms.ts"
  },
  "publish-to-both-platforms-unified": {
    "fields": [
      {
        "name": "pageId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.pageId }}"
      },
      {
        "name": "igUserId",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.igUserId }}"
      },
      {
        "name": "userAccessToken",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.userAccessToken }}"
      },
      {
        "name": "publishTarget",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.publishTarget }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/publish-to-both-platforms.ts"
  },
  "preview-campaign": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name"
      },
      {
        "name": "product_ids",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.product_ids }}",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name */ name: string /** Product IDs to preview"
      },
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name */ name: string /** Product IDs to preview */ product_ids: string[] /** Platform ID"
      },
      {
        "name": "content_rule",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.content_rule }}",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name */ name: string /** Product IDs to preview */ product_ids: string[] /** Platform ID */ platform_id: string /** Content rule to apply"
      },
      {
        "name": "interval_hours",
        "type": "number",
        "required": true,
        "placeholder": "0",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name */ name: string /** Product IDs to preview */ product_ids: string[] /** Platform ID */ platform_id: string /** Content rule to apply */ content_rule: ContentRule /** Hours between each publish"
      },
      {
        "name": "start_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.start_at }}",
        "description": "Preview Campaign Workflow Generates a preview of all content that would be published in a campaign. Does NOT publish anything - just generates and validates content. / export interface PreviewCampaignInput { /** Campaign name */ name: string /** Product IDs to preview */ product_ids: string[] /** Platform ID */ platform_id: string /** Content rule to apply */ content_rule: ContentRule /** Hours between each publish */ interval_hours: number /** Start date/time"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/scheduled-publishing/preview-campaign-workflow.ts"
  },
  "process-campaign-item": {
    "fields": [
      {
        "name": "product_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.product_id }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish"
      },
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID"
      },
      {
        "name": "platform_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.platform_name }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID */ platform_id: string /** Platform name"
      },
      {
        "name": "campaign_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.campaign_name }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID */ platform_id: string /** Platform name */ platform_name: string /** Campaign name"
      },
      {
        "name": "campaign_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.campaign_id }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID */ platform_id: string /** Platform name */ platform_name: string /** Campaign name */ campaign_name: string /** Campaign ID (parent workflow transaction ID)"
      },
      {
        "name": "item_index",
        "type": "number",
        "required": true,
        "placeholder": "0",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID */ platform_id: string /** Platform name */ platform_name: string /** Campaign name */ campaign_name: string /** Campaign ID (parent workflow transaction ID) */ campaign_id: string /** Item index in campaign"
      },
      {
        "name": "content_rule",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.content_rule }}",
        "description": "Process Campaign Item Workflow Processes a single item from a publishing campaign. This is called by the scheduler for each item when its publish time arrives. This separation allows: - Independent processing of each item - Easy retry of failed items - No complex async waiting within the workflow / export interface ProcessCampaignItemInput { /** Product ID to publish */ product_id: string /** Platform ID */ platform_id: string /** Platform name */ platform_name: string /** Campaign name */ campaign_name: string /** Campaign ID (parent workflow transaction ID) */ campaign_id: string /** Item index in campaign */ item_index: number /** Content rule to apply"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/scheduled-publishing/process-campaign-item-workflow.ts"
  },
  "sync-platform-hashtags-mentions": {
    "fields": [
      {
        "name": "platform_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "access_token",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.access_token }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/sync-platform-hashtags-mentions.ts"
  },
  "update--social-platform": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "category",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.category }}"
      },
      {
        "name": "auth_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.auth_type }}"
      },
      {
        "name": "icon_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.icon_url }}"
      },
      {
        "name": "base_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.base_url }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "api_config",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/update-social-platform.ts"
  },
  "update-social-post": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "platform_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.platform_id }}"
      },
      {
        "name": "post_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.post_url }}"
      },
      {
        "name": "caption",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.caption }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "scheduled_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.scheduled_at }}"
      },
      {
        "name": "posted_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.posted_at }}"
      },
      {
        "name": "insights",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "media_attachments",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "notes",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.notes }}"
      },
      {
        "name": "error_message",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.error_message }}"
      },
      {
        "name": "related_item_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.related_item_type }}"
      },
      {
        "name": "related_item_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.related_item_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/socials/update-social-post.ts"
  },
  "create-store-with-defaults": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/stores/create-store-with-defaults.ts"
  },
  "provision-storefront": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "partner_name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.partner_name }}"
      },
      {
        "name": "handle",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.handle }}"
      },
      {
        "name": "publishable_key",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.publishable_key }}"
      },
      {
        "name": "root_domain",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.root_domain }}"
      },
      {
        "name": "storefront_repo",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.storefront_repo }}"
      },
      {
        "name": "storefront_root_dir",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.storefront_root_dir }}"
      },
      {
        "name": "storefront_branch",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.storefront_branch }}"
      },
      {
        "name": "medusa_backend_url",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.medusa_backend_url }}"
      },
      {
        "name": "stripe_publishable_key",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.stripe_publishable_key }}"
      },
      {
        "name": "s3_hostname",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.s3_hostname }}"
      },
      {
        "name": "s3_pathname",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.s3_pathname }}"
      },
      {
        "name": "preferred_provider",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.preferred_provider }}",
        "description": "Preferred hosting provider for this NEW storefront. Defaults to DEFAULT_HOSTING_PROVIDER env (or \"vercel\"). The selector rotates across that provider's accounts, spills to other providers, then falls back to the legacy env-single-account path. Default is \"vercel\" (not \"cloudflare\"): Vercel handles custom domains — including bare apex — natively (A @ → 76.76.21.21) on any DNS provider, with auto TLS, so it \"just works\" for every partner. The Cloudflare Workers tier is deferred (its SaaS custom-hostname path can't serve a bare apex without Enterprise apex-proxying); re-enable it later by setting DEFAULT_HOSTING_PROVIDER=cloudflare and reactivating the CF account."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/stores/provision-storefront.ts"
  },
  "redeploy-storefront": {
    "fields": [
      {
        "name": "partner_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.partner_id }}"
      },
      {
        "name": "vercel_project_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.vercel_project_id }}"
      },
      {
        "name": "vercel_project_name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.vercel_project_name }}"
      },
      {
        "name": "storefront_repo",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.storefront_repo }}"
      },
      {
        "name": "ref",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.ref }}"
      },
      {
        "name": "update_env",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.update_env }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/stores/redeploy-storefront.ts"
  },
  "delete-task-template": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/task-templates/delete-template.ts"
  },
  "list-single-task-template": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/task-templates/list-single-template.ts"
  },
  "list-task-templates-categories": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/task-templates/list-template-categories.ts"
  },
  "list-task-templates": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/task-templates/list-templates.ts"
  },
  "update-task-template": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/task-templates/update-template.ts"
  },
  "delete-task": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/tasks/delete-task.ts"
  },
  "list-tasks": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/tasks/list-tasks.ts"
  },
  "update-task": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/tasks/update-task.ts"
  },
  "classify-product-tax-class": {
    "fields": [
      {
        "name": "product_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.product_id }}"
      },
      {
        "name": "dry_run",
        "type": "boolean",
        "required": false,
        "placeholder": "false",
        "description": "Roadmap item 5 follow-up — classify a product into the JYT-managed \"textile_over_2500\" product_type based on its maximum INR variant price, so the IN tax_region's 18% rule fires correctly at cart time (vs. the 5% default that's correct for sub-₹2,500 items). Why a product-level type and not per-variant: Medusa's `tax_rate_rule.reference` only accepts `product`, `product_type`, or `shipping_option`. A single product carries one `type_id`, so when variants straddle the ₹2,500 threshold we go conservative — use the MAX variant price. That over-collects on cheap variants but never under-collects, which is the right side of statutory tax liability. Documented in `apps/docs/notes/TAX_NOTES.md`. Safe to call repeatedly. Only mutates the product when the resolved tax class differs from the current `type_id` AND the current `type_id` is either NULL or one of the JYT-managed values — never overwrites a partner-assigned type. / // ₹2,500 — INR stored as whole rupees in Medusa 2.x (currency has // decimal_digits=0), confirmed by the prod /store/products probe // returning `calculated_amount: 4999` for a ₹4,999 variant. export const IN_TEXTILE_THRESHOLD_INR = 2500 // The JYT-managed product_type value (stored on // product_type.value). The seed script `seed-in-textile-tax-class.ts` // is the only writer that creates rows with these values. export const TAX_CLASS_OVER_2500_VALUE = \"jyt_tax_in_textile_over_2500\" // Set of all type values the classifier owns. Used to decide whether // it's safe to clear/replace a product's current type_id. Anything // not in this set is partner-managed and must not be touched. export const JYT_MANAGED_TAX_CLASS_VALUES = new Set<string>([ TAX_CLASS_OVER_2500_VALUE, ]) export type ClassifyProductInput = { product_id: string /** When true, compute the decision (`assigned` / `cleared` / `no-change` / `skipped`) but DO NOT mutate the product's type_id. Used by the backfill's `--dry-run` so a preview is actually a preview. The returned `decision` still reflects what WOULD happen."
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/tax/classify-product-tax-class.ts"
  },
  "list-user-auth-identities": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/users/list-auth-identities.ts"
  },
  "suspend-user-workflow": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/users/suspend-user.ts"
  },
  "unsuspend-user-workflow": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/users/unsuspend-user.ts"
  },
  "create-visual-flow": {
    "fields": [
      {
        "name": "data",
        "type": "object",
        "required": true,
        "description": "Fields for the new record",
        "placeholder": "{{ $last }}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/visual-flows/create-visual-flow.ts"
  },
  "delete-visual-flow": {
    "fields": [
      {
        "name": "flowId",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.flowId }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/visual-flows/delete-visual-flow.ts"
  },
  "update-visual-flow": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "data",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "inferred",
    "sourceFile": "src/workflows/visual-flows/update-visual-flow.ts"
  },
  "create-website": {
    "fields": [
      {
        "name": "domain",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.domain }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "primary_language",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.primary_language }}"
      },
      {
        "name": "supported_languages",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "favicon_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.favicon_url }}"
      },
      {
        "name": "analytics_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.analytics_id }}"
      },
      {
        "name": "analytics_provider",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_provider }}"
      },
      {
        "name": "analytics_custom_head",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_custom_head }}"
      },
      {
        "name": "analytics_custom_body_end",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_custom_body_end }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/create-website.ts"
  },
  "delete-website": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/delete-website.ts"
  },
  "find-website-by-domain": {
    "fields": [
      {
        "name": "domain",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.domain }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/find-website-by-domain.ts"
  },
  "list-website": {
    "fields": [
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/list-website.ts"
  },
  "create-batch-blocks": {
    "fields": [
      {
        "name": "blocks",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.blocks }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/create-batch-blocks.ts"
  },
  "create-block": {
    "fields": [
      {
        "name": "page_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.page_id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": true,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "content",
        "type": "object",
        "required": true,
        "placeholder": "{}"
      },
      {
        "name": "settings",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/create-block.ts"
  },
  "delete-block": {
    "fields": [
      {
        "name": "block_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.block_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/delete-block.ts"
  },
  "list-blocks": {
    "fields": [
      {
        "name": "page_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.page_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/list-blocks.ts"
  },
  "list-single-block": {
    "fields": [
      {
        "name": "block_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.block_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/list-single-block.ts"
  },
  "update-block": {
    "fields": [
      {
        "name": "block_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.block_id }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "content",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "settings",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "order",
        "type": "number",
        "required": false,
        "placeholder": "0"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/page-blocks/update-block.ts"
  },
  "seed-default-pages": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/seed-default-pages.ts"
  },
  "update-website": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "domain",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.domain }}"
      },
      {
        "name": "name",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.name }}"
      },
      {
        "name": "description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.description }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "primary_language",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.primary_language }}"
      },
      {
        "name": "supported_languages",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "favicon_url",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.favicon_url }}"
      },
      {
        "name": "analytics_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.analytics_id }}"
      },
      {
        "name": "analytics_provider",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_provider }}"
      },
      {
        "name": "analytics_custom_head",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_custom_head }}"
      },
      {
        "name": "analytics_custom_body_end",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.analytics_custom_body_end }}"
      },
      {
        "name": "google_site_verification",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.google_site_verification }}"
      },
      {
        "name": "theme",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/update-website.ts"
  },
  "create-bulk-pages": {
    "fields": [
      {
        "name": "pages",
        "type": "array",
        "required": true,
        "placeholder": "{{ $last.pages }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/website-page/create-bulk-pages.ts"
  },
  "delete-page": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/website-page/delete-page.ts"
  },
  "list-page": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}",
        "description": "Required — pages are always scoped to a website"
      },
      {
        "name": "filters",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/website-page/list-page.ts"
  },
  "list-single-page": {
    "fields": [
      {
        "name": "website_id",
        "type": "id",
        "required": false,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "page_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.page_id }}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/website-page/list-single-page.ts"
  },
  "update-page": {
    "fields": [
      {
        "name": "id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.id }}"
      },
      {
        "name": "website_id",
        "type": "id",
        "required": true,
        "placeholder": "{{ $last.website_id }}"
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.title }}"
      },
      {
        "name": "slug",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.slug }}"
      },
      {
        "name": "content",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.content }}"
      },
      {
        "name": "page_type",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.page_type }}"
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.status }}"
      },
      {
        "name": "meta_title",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.meta_title }}"
      },
      {
        "name": "meta_description",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.meta_description }}"
      },
      {
        "name": "meta_keywords",
        "type": "string",
        "required": false,
        "placeholder": "{{ $last.meta_keywords }}"
      },
      {
        "name": "published_at",
        "type": "date",
        "required": false,
        "placeholder": "{{ $trigger.published_at }}"
      },
      {
        "name": "metadata",
        "type": "object",
        "required": false,
        "placeholder": "{}"
      }
    ],
    "source": "custom_ts",
    "sourceFile": "src/workflows/website/website-page/update-page.ts"
  }
}