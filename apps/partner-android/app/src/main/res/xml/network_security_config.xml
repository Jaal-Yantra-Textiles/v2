<?xml version="1.0" encoding="utf-8"?>
<!-- Production network policy — HTTPS everywhere except the emulator's
     host-loopback alias (harmless in the field; there is nothing to
     intercept on the emulator's own localhost). Debug builds overlay this
     with an anything-goes copy so LAN backends keep working in dev. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
