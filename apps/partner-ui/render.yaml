services:
  - type: web
    name: partner-ui
    runtime: static
    buildCommand: pnpm run vercel-build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
