# Agent Daemon — Best Practices (append-only)

- When the queued next task is the "big risky" slice (live-path rewrite, prod Dockerfile, ambiguous), prefer a clean adjacent low-risk slice the same issue already lists — e.g. deleting confirmed-dead code (0 refs repo-wide) is a legit, mergeable chunk and removes a "edit the wrong file" hazard. Verify deadness with BOTH the filename grep AND a grep for each exported symbol across `src` (exclude `.medusa/server/**` = build output) before deleting; also fix any stale comments that name the removed file.
- If the daemon's own uncommitted files (e.g. `scripts/agent-daemon/launch.sh`) block `git checkout -b … origin/main`, do NOT stash/reset (forbidden — nukes the SDK). Instead `git add <that one daemon file> && git commit` it onto the current daemon branch to preserve it, then branch off origin/main with a clean tree. A modified submodule gitlink (`apps/storefront-starter`) does not block the checkout and carries over harmlessly — never stage it.
- `scripts/` is UNTRACKED on `origin/main` (the 215MB daemon SDK lives there). After branching off main it shows as `?? scripts/`. Stage feature files by explicit path only; `git rm` already stages a deletion so don't re-`git add` the deleted path (pathspec won't match the gone file).
- zod v4 (this repo, 4.3.6) `z.record(z.any())` is malformed — leaves the value schema undefined → validateBody throws `Cannot read properties of undefined (reading '_zod')`, but ONLY when the field is non-empty (an empty object short-circuits validation, so a partial integration test can pass while the real path 500s). Always use the two-arg form `z.record(z.string(), z.any())`. Capture the real 500 body via a try/catch + console.log in the spec — the custom errorHandler suppresses console logging under TEST_TYPE, so a failing assertion alone won't show the stack.
- Stacked PRs for dependent backend slices: when a follow-up needs files from an unmerged PR (e.g. a registry that only exists on the parent branch), branch off that parent branch and `gh pr create --base <parent-branch>`. GitHub auto-retargets the stacked PR to main once the parent merges — keeps the diff focused on just the new slice. (#471 on #470.)
- Zod `.partial()` does NOT strip a field's `.default()`: `panelBaseSchema.partial()` where a field is `z.record(...).default({})` still yields `{}` for that key when the client omits it. A route guard like `if (data.x || data.y)` then fires even on updates that never touched x/y — e.g. the admin stats-panel PUT re-validates `operation_options` on every metadata-only update and 400s when the panel's options are required. When extending such a route, mirror the real client flow in tests (send the full payload) rather than fighting the latent default-injection; note it as a separate cleanup instead of widening the slice.
- An unmerged review backlog is a real signal: when the active-issue stack already has N PRs awaiting human review ("drain before adding more"), pick a *fresh independent* roadmap issue off main for the next chunk instead of stacking a 3rd dependent PR. A self-contained backend restore (delete-then-readd from git history) verifiable with a per-file spec is the ideal low-risk fill. Pull deleted code back verbatim with `git show <commit>~1:<path>` and re-check WHY it was removed (here #284 deferred to the very issue being built; #283 had dropped a *different* gate on purpose — don't reinstate that one).
- Zod `.partial()` does NOT remove a field's `.default()` — defaults still fire on omitted keys, silently injecting values into "partial" update payloads (real data-loss + 400 source). Any create-base schema reused via `.partial()` for updates must re-declare its defaulted fields as `.optional()` on the update variant. Verify the interaction empirically with the project's own `@medusajs/framework/zod` (a throwaway .mjs under apps/backend) before assuming behavior. (PR #473)

- When fixing a Zod `.partial()`+`.default()` injection bug, grep the whole `src/api` for the *same* pattern (`grep -rl ".partial()"` ∩ `.default(`) — it is rarely a single instance. Verify empirically (`base.partial().parse({})` in a throwaway node script) that the default actually injects, and confirm the route spreads `...validatedBody` before claiming a clobber bug; transient action-triggers (default=safe no-op) and nested per-item defaults are NOT bugs.
- When the review backlog is full of stacked PRs, pick an independent off-main roadmap item whose acceptance is fully specified in the issue (e.g. #377's domain-precedence order) and whose core logic is a PURE function — unit-testable with a stubbed query.graph, no DB, no migration, no Playwright. Ships clean without adding integration-test/dev-server risk.
- When the daemon review backlog is full (many unmerged stacked PRs) and all remaining queue items are blocked/gated/stacking, an ANALYSIS chunk that writes a *build-ready* design doc (exact model/route/test/wiring + carried gotchas) for the next queued slice is higher-leverage than stacking a 7th PR — it lets the next session build without re-discovery. (chunk 8/8, #457 audit-log)
- New-route import depth bites: an API route under `[id]/run/` is one dir deeper than a sibling list route, so `../` counts differ by one (6 vs 5 to reach `src/modules`). An off-by-one shows up as a single TS2307 over baseline — diff the count against a working sibling import, don't eyeball it.
- `medusa db:generate <module>` emits a `.snapshot-<module>.json` next to the migration — stage it too (every existing module tracks its snapshot); forgetting it leaves the next generate dirty.
- To integration-test a new audit/side-effect on an existing route, extend that route's existing spec file rather than adding a new one — reuses the shared-DB boot and dodges the per-file TRUNCATE-vs-CONCURRENTLY deadlock. A guard-path call that returns 200 with an inner error (e.g. bulk recalc vs a missing id) is a zero-seed way to exercise the write.
- A registry-of-jobs pattern (e.g. #457 maintenance-jobs) is the ideal daemon slice when the review backlog has an unmerged PR touching the SAME feature: adding an Nth entry to the registry array + its pure helpers conflicts ONLY if the prior PR edited that exact file. Check `git diff origin/main...origin/<unmerged-branch> --name-only` first — if registry.ts isn't in it, you can branch off main, add the job, and stay conflict-free (no stacking, no rebase risk).
- Ops-registry unit specs live under `src/api/.../__tests__/*.unit.spec.ts` — run with `TEST_TYPE=unit` (the `integration:modules` testMatch only globs `src/modules/*/__tests__/`). A pure-helper accumulator can have a different internal field name (`.date`) than the function return shape (`order_date`); annotate them separately or tsc throws TS2339.
- When branching off `origin/main` to extend a registry that sibling unmerged PRs also touch (e.g. #457 MAINTENANCE_JOBS), expect a NEW utils import (`Modules`) to be missing on main even though it exists on the sibling branch — `tsc` catches it as TS2552; add the import. Off-main = registry has only the merged jobs, so mirror the main version's test file (no sibling-branch test refs like `res.data.audit`).
- #484 partner search/filter: the partner-ui search box is fine (URL `q` → SDK `query`); the bug is backend `/partners/*` list routes that never read `req.query` and return the full list. Always check the backend route reads `q` before assuming a UI bug. The reusable fix pattern is an in-app post-filter on sku/title (see partners/inventory-items/raw-materials route).

- When draining the #484 partner-search route fixes, branch each follow-up off `origin/main` (NOT off the prior chunk-branch) so PRs stay independent for human review; the in-app `q` post-filter pattern (read q/limit/offset → filter → slice → count=matched) is copy-paste from PR #487 inventory-items for any route that already loads its full set in memory (customers, raw-materials).
- When a partner list route paginates inside `query.graph` but filters status/q in-app, the filter runs on a single page and `count` is per-page (page-vs-set bug). Fix = drop `query.graph` pagination, fetch the full partner-scoped set, then filter+paginate in a pure helper (count = total matched). Extracting that helper makes it unit-testable (`*.unit.spec.ts`, `TEST_TYPE=unit`) — far cheaper than an integration test that needs a fully linked inventory order. `req.validatedQuery` is typed `unknown` on partner routes whose middleware borrows the admin schema; cast `as <LocalQueryType>` when destructuring or tsc TS2322 fires on any typed consumer.
- #484 designs route: the page-vs-set bug recurs anywhere a /partners/* list paginates inside query.graph (skip:offset/take:limit) before an in-app merge/post-filter — fix is always: fetch full set (capped take), filter+paginate in a pure unit-tested helper, count=total matched. 4th identical fix (#487/#488/#489/#490).

- **#484 partner-search has 5 backend list surfaces, all with the same root cause** (route never reads `q`; some also paginate before filtering = page-vs-set per-page count). Fix pattern that worked across #487–#491: read `q`, fetch the full partner-scoped set, then filter+paginate in a PURE helper (`apply<X>ListFilters`) with count=total matched, and a `TEST_TYPE=unit` spec on the helper — avoids booting Medusa for heavily-linked fixtures (reservations/orders need inventory-item+stock-location+sales-channel links).
- Multi-store currency trap (#485): in a now-multi-tenant deployment, `query.graph({entity:"store"})` returns ALL stores and `stores?.[0]` grabs an arbitrary one (prod: the platform "JYT Medu Store" = EUR), not the partner's store (INR). Verify store cardinality with `GET /admin/stores?fields=*supported_currencies` before assuming "the store" is singular. Any `stores[0]` in a currency/region path is a latent bug.
- When a prod "native addon won't load" issue blames `--ignore-scripts`, first check the addon's `engines.node` + shipped `prebuilds/` ABIs vs the Docker base image's Node ABI. Modern addons (isolated-vm v6 = prebuildify) need NO build tools — they need a matching prebuild. A Docker `docker run --rm node:<ver>-slim` one-liner that `npm i`s the pkg and requires it proves load/fail in seconds without a full image build. (#494: isolated-vm@6.1.2 needs Node>=22, prod was node:20-slim.)
- A many-to-many *within* a single Medusa module (e.g. customer↔customer_group) is NOT a `remoteLink` target — `remoteLink.create/dismiss` 500s on the undefined link. Use the module service's own methods (addCustomerToGroup/removeCustomerFromGroup) or the core workflow that wraps them (linkCustomerGroupsToCustomerWorkflow). Read the admin route first to confirm (#495/PR #500).

- **Single shared workspace + concurrent agents = branch-switch collisions.** If another process checks out a different branch between your `git checkout -b` and your `git commit && git push`, your commit lands on the WRONG branch and the push pushes the stale HEAD → `gh pr create` fails "No commits between main and <branch>" and your working-tree edits look reverted. They're NOT lost: `git reflog` shows your commit hash; `git checkout <your-branch>` (still at origin/main) then `git cherry-pick <hash>` recovers your exact delta. Mitigation: commit+push in ONE chained command right after editing, and always verify `git log --oneline origin/main..HEAD` shows your commit before `gh pr create`.
- When moving a dep between `dependencies`/`optionalDependencies` in package.json, the Dockerfile builds with `pnpm install --frozen-lockfile` — so `pnpm-lock.yaml` MUST be updated too or the prod build fails. Hand-edit the lockfile (no repo-root `pnpm install`): move the entry between the importer's `dependencies:`/`optionalDependencies:` blocks (same spec/version); the package snapshot stays unchanged. Validate with `node -e "require('js-yaml').load(...)"`.
- #485/#457: when a backfill must re-stamp an order column, confirm the column is in `UpdateOrderDTO` (e.g. `currency_code?`) AND that `orderService.updateOrders([{id,...}])` is already used in-tree before assuming the mutation is allowed — order is heavily guarded. Resolve order→partner via the link table `partnerOrderLink.entryPoint` (filter `partner_id`), NOT the `partner.orders` graph accessor (pluralisation not guaranteed; per list-partner-orders.ts).

- **Before building a "rewire all N call sites" forward-fix, audit each site for whether the needed context is actually in scope.** #485 was framed as "rewire 8 `stores[0]` sites"; the audit found only 3 were genuine duplicated currency scans, 1 was already correct, and the rest were storefront (not currency) — and partner is never in scope at currency-stamping time anyway (linked post-creation). The honest deliverable was a behaviour-preserving centralisation + a memory note correcting the framing, not a risky partner-plumb that cannot work.
- UI-slice verification: if `yarn dev` is already running (check `lsof -iTCP:9000`/`:5173`) you CAN Playwright-verify admin UI — but the admin needs a login. No creds are in `.env`; create a throwaway admin with `npx medusa user -e x@jyt.local -p Pass123!` (additive to the local dev DB, operator deletes later). Admin login fields are `input[name="email"]`/`input[name="password"]`, served at `localhost:9000/app`, custom settings routes at `/app/settings/<folder>`.
- Admin custom UI that consumes an already-merged API does NOT need to stack on unmerged backend PRs — branch off `origin/main` for a clean independent PR (the UI is generic over the backend contract).
- Design-first chunks: write the notes doc + post the open decisions as an issue comment (✅-marked recommendations the user can veto), then SHIP THE DOC AS A DOCS-ONLY PR off origin/main and STOP. Don't build past the decisions. Mirrors the #485/#494 steer loop; keeps the daemon unblocked without guessing on a data-model/UX fork.
- `medusa db:generate <module>` runs WITHOUT a live DB (diffs models vs the `.snapshot-*.json`) and emits idempotent `add column if not exists` ALTERs itself — handy to regenerate the snapshot, but per the create-if-not-exists hazard keep your hand-written migration and just delete the generated dup, retaining the regenerated snapshot.
- Stacked slices: when slice N depends on an unmerged slice N-1, branch off N-1's branch and open the PR with `--base <slice-N-1-branch>` so the PR diff is only slice N (not N-1's already-reviewed code).
- cwd-shift footgun: a `cd <dir> && …` Bash call leaves the persistent shell cwd in `<dir>`; a subsequent Write can land a file one level too deep (e.g. `apps/backend/apps/backend/…`). Always `cd` to an absolute path at the top of each Bash block and `git status` before staging. The HTTP integration runner takes a NAME substring, not a path: `pnpm test:integration:http:shared -- ops-maintenance-batches` (a full path → "0 matches").
- A Medusa route file can export multiple HTTP methods: add `GET` to an existing `batches/route.ts` next to `POST` for the index/list (no new file), and only spin up `batches/[id]/route.ts` for the param detail. Each method needs its OWN middleware matcher entry (same path, different `method`). #508 slice 3.
- #508 jobs (slice 4+) are registry-only appends to `registry.ts` + `MAINTENANCE_JOBS` — they do NOT depend on the batch stack (slices 1-3 never touch `registry.ts`), so branch each off `origin/main` as an INDEPENDENT PR (no stacking). Verify with `git diff origin/main..<slice-branch> --stat -- .../registry.ts` (empty = safe to branch off main).
- When a "backfill" job targets a denormalized COLUMN that mirrors a module LINK, scan FROM the link table (`link.entryPoint`, query.graph fields = the remoteLink.create payload keys, e.g. `production_runs_id`/`consumption_log_id`) — it's far smaller than all null-column rows and is exactly the drift population. Verify which side is the actual READ path first (cost-summary read the column, not the link), so you know which to backfill into. Fill only when the link is unambiguous (exactly one); never overwrite an existing value (idempotent); surface multi-link "ambiguous" as a summary skip, not an errors[] entry (mirror the prior job's skip-clause convention).
- A Playwright-gated UI slice can still yield a clean BUILD chunk headless: split out the pure data layer (react-query hooks render nothing → no screenshot needed, tsc-verifiable) as its own PR + ship an implementation-ready render spec, instead of analysis-only. De-risks the next UI session while keeping the daemon productive.

- When the active issue's only remaining slice is Playwright-gated UI (headless daemon can't verify), look upstream for a backend gap the UI will need (e.g. a missing query filter the render spec implies) — that's a clean, integration-testable slice that unblocks the UI without needing dev/Playwright. (#508 → PR #517: added `batch_id` filter to GET /runs because the "All runs" tab needs single-job-only rows.)
- To start an independent PR off origin/main while sitting on an unmerged feature branch that edited a shared file (e.g. ops-maintenance.ts also touched by an open PR): `git checkout -- <shared-file>` to drop your working edits, THEN `git checkout -b new origin/main` — git cleanly resets the now-unmodified shared file to main's version, while keeping your edits to files whose HEAD==main. Avoids dragging the other PR's content in. Defer the shared-file edit to the PR that already owns it.

- Medusa validated routes (`validateAndTransformQuery` + zod) REJECT unknown query params with a 400 — they are NOT silently ignored. So an admin UI must not send a param the *live* backend route doesn't accept yet: a forward-compat field added for an unmerged backend PR (#508 render slice's `batch_id`, awaiting #517) will 400 the call and break the tab. Decouple: don't send it until that PR merges; leave a one-line TODO. (Caught only because dev was up + Playwright surfaced the 400.)
- A Playwright-gated UI slice IS buildable headless when the operator's `yarn dev` is already up (check `lsof -iTCP:9000`/`:5173`). Two setup gotchas: (1) the local dev DB often lacks merged-to-main migrations → `TableNotFoundException` on new-module tables; `npx medusa db:migrate` syncs it (additive). (2) Seed history with a NO-OP applied batch (jobs with huge thresholds → 0 changes, e.g. `prune-ops-audit-runs older_than_days:9999`) via `POST .../batches {dry_run:false}` — exercises the full UI without mutating real local data.
- Robust admin login in Playwright: `waitForURL('**/app**')` matches `/app/login` immediately and returns before auth completes. Instead `await Promise.all([page.waitForResponse(r=>r.url().includes('/auth/')&&r.request().method()==='POST'), page.click('submit')])` then `waitForFunction(()=>!location.pathname.endsWith('/login'))`. Password field selector `input[type="password"]` is more robust than `input[name="password"]`.
- Medusa admin vs partner-ui ports: the **admin** (extension routes like /stats, Settings consoles) is served by the backend at `http://localhost:9000/app`. Port **:5173 (Vite) is the separate partner dashboard** ("Welcome to Partner Dashboard") — its router 404s on `/app/*`. Always Playwright-verify admin UI against `:9000/app`, not `:5173`.
- When this Mac's python3.14 is broken (pyexpat dlopen on import), skip the Python webapp-testing path: `npm install playwright` in **/tmp** (never repo-root — sweeps the 215MB SDK) and drive with Node ESM; chromium is already cached in `~/Library/Caches/ms-playwright`.
- Target a specific Medusa `ActionMenu` (the "…" button) by scoping to a sibling: e.g. a stats panel card's edit menu is `div:has(> button[aria-label='Refresh']) button[aria-haspopup='menu']`. A bare `button[aria-haspopup='menu'].first()` matches the top-left store/user menu instead.
- Partner routes have NO global auth: `/partners*` middleware only adds CORS + locale; every new partner route needs its OWN `authenticate("partner",["session","bearer"])` matcher in `src/api/middlewares.ts` (a GET with no body needs nothing else). Forgetting it = route runs unauthenticated.

- #337 partner parity slices follow a tight recipe: copy the admin route's data query verbatim (`listDesignComponents`, lineage walk, etc.), wrap with `assertPartnerOwnsDesign`, then add a PURE `scope*ToPartner` helper (filters by `owner_partner_id`) with its own unit spec under `partners/designs/__tests__/`. Self-referential design_component: `used-in` = query by `component_design_id` + relation `parent_design`; scope on `parent_design.owner_partner_id`.

- #337 `components` (PR #527) completes the design_component pair: `components` = query by `parent_design_id` + relation `component_design`, scope on `component_design.owner_partner_id` (mirror of `used-in`). When mirroring an admin route that has GET+POST, copy ONLY the GET for partner read parity — partner CUD on shared BOM structure is a separate (often locked) decision.
- #337 parity: confirm whether prior series PRs MERGED before assuming you must stack — `git show origin/main:<central-file> | grep -c <prior-matcher>`. Once the series base merged, later slices branch independently off origin/main (no stacking, no conflict). The #337 revisions/used-in/components matchers were all on main, so #528 (tasks) branched clean.
- A handoff-queued "next read-parity slice" can be wrong: verify the admin route's HTTP method before assuming it's a GET mirror. `admin/designs/:id/segment` was queued as a "read" but is a POST fal.ai op. Cheap check: `grep -oE "export (const|async function) (GET|POST|PUT|DELETE)" <route>` per admin subroute — discovers method + reveals whether the clean-read series is actually exhausted.
- Partner AI routes are quota-gated, NOT naive admin mirrors. Before mirroring an admin AI/fal endpoint, check `modules/ai_usage` — add a `MONTHLY_QUOTA` key (no migration, `operation` is a free string) and copy describe-image's checkQuota→402→recordUsage(before-call)→fal pattern. This keeps a "decision-bearing" AI route buildable without a new product policy (the soft-paywall precedent IS the policy).
- Medusa middleware matchers are EXACT-path, not prefix globs: a matcher for `/x/:id/segment` does NOT auth-cover `/x/:id/segment/depth` — every nested subroute needs its own matcher block. (chunk 3, PR #530 depth route.)
- A batch of routes flagged "decision-bearing" by a prior handoff can still hide ONE clean buildable slice — don't accept the label without checking. Chunk 4 found `revise` (lumped with approve/notify-customer as "changes canonical design state") was actually safe: reading `reviseDesignWorkflow` showed it RE-LINKS the partner to the new revision (`linkPartnersToNewDesignStep`), so an own-design fork stays partner-owned with no external side-effects. The 30s of reading the workflow turned a "needs a decision" into a shipped PR (#531). Verify the actual side-effects (product creation? customer email? cross-tenant link?) before deferring a mutation as decision-bearing.
- When a chunk is mostly analysis but yields one clean build, do BOTH: ship the clean slice as a PR AND write the decision doc for the rest (`apps/docs/notes/337_PARTNER_DESIGN_MUTATION_PARITY.md`) so the deferred routes are teed-up (per-route decision + recommendation + build order), not just "decision needed". Next chunk/user resumes without re-deriving.
- Re-confirm a flagged "decision-bearing" route against its ACTUAL schema, not the JSDoc. Chunk 5 found `POST tasks` (deferred for an "assignee" cross-tenant worry) had NO assignee field in `AdminPostDesignTasksReq` — the JSDoc's `assigneeId` example was stale. Reading the validator + workflow (~1 min) turned the 4th "decision-bearing" mutation into a shipped clean own-design slice (PR #532), same as `revise` (#531). Two of the five "deferred" #337 mutations were actually clean.
- When a chunk concludes "no clean buildable slice" because a candidate looks risky/destructive, CHECK FOR AN EXISTING MERGED PRECEDENT first. `repair-inventory-raw-material-links` (PR #533) looked like a risky novel link-mutation build until I found the merged `repair-partner-region-links` (#514) doing the exact same orphan-enumerate-then-dismiss pattern — turning a "switch to analysis" call into a clean buildable mirror. A precedented destructive op is low-risk; an unprecedented one is the risky one.
- Before writing a data-plumbing backfill, check what `NULL` MEANS for the column: if NULL is a valid semantic state (e.g. `design.owner_partner_id IS NULL` == admin/canonical, used as a filter discriminator), backfilling it from a weaker signal (an assignment link) is HARMFUL re-scoping, not a fix — the "job" is a non-job. (chunk 7, #336 analysis)
- New Medusa module needs its resolve path in BOTH `medusa-config.ts` and `medusa-config.prod.ts` (the partner-payment-config precedent does NOT register in `.dev.ts`); a hand-written create-table migration needs no `.snapshot-*.json` (snapshots only feed `db:generate` diffing). For bigNumber money cols, the migration must add a `raw_<col>` jsonb sidecar alongside the `numeric` col, and enums become `text check (col in ('a','b'))` — mirror `internal_payments/migrations/Migration20250105072006.ts` rather than guessing.
- When a slice depends on code that only exists on an unmerged prior-slice branch (e.g. Slice 1 imports `compute-fee.ts` shipped in Slice 0's PR), you MUST stack on that branch (not origin/main) even if there's no shared-file conflict — otherwise the import target is missing and the slice can't build standalone. This is dependency-stacking, distinct from shared-registry-file-conflict stacking.

- **Resolve a partner FROM an order_id:** query the D3 link table directly by entryPoint — `query.graph({entity: partnerOrderLink.entryPoint, fields:["partner_id"], filters:{order_id}})` (import the default export of `src/links/partner-order.ts`). No row ⇒ retail order ⇒ skip. Don't trust a `partner.orders`/`order.partners` graph accessor (pluralisation not guaranteed). Same pattern reversed (`fields:["order_id"], filters:{partner_id}`) lists a partner's orders.
- **Test an `order.placed`/`order.canceled` subscriber without the event bus:** create the order via `container.resolve(Modules.ORDER).createOrders({currency_code,email,items:[...]})`, create any link via `container.resolve(ContainerRegistrationKeys.LINK).create([{[PARTNER_MODULE]:{partner_id},[Modules.ORDER]:{order_id},data:{...}}])`, then call the exported handler directly: `await handler({event:{data:{id}}, container} as any)`. Mirror `integration-tests/http/order-placed-production-runs.spec.ts`. Per-file run only (`pnpm test:integration:http:shared -- <spec>`).
- #336 reversal/compensation slices: put the idempotency+gating logic in a SERVICE method (`reverseFeeForOrder`) not the subscriber — keeps the subscriber a thin best-effort wrapper and the logic unit/integration-testable; return null when nothing changed so the subscriber logs only real transitions. For cancel, reverse ONLY `accrued` rows (skip invoiced/waived/reversed) so you never silently undo a billed fee.

- **Read-API roll-ups: compute over the full filtered set, slice for pagination AFTER (#484 page-vs-set).** For `GET .../[id]/fees` (#336 Slice 4) the summary is built from `listPartnerFees(filters)` then `all.slice(offset,offset+limit)` for the page — never summarize the page or counts go per-page. Pure summarizer (`summarizePartnerFees`) is unit-tested in isolation; the route just wires it. Partner-side mirror = same handler body + `getPartnerFromAuthContext` 403-on-mismatch guard.

- **Historical "catch-up" backfill jobs must mirror the live subscriber's net outcome, not just its accrue path.** #336 Slice 5 (`backfill-partner-order-fees`) skips `canceled` orders because the order.placed accrual subscriber (Slice 2) + order.canceled reversal (Slice 3) net to zero for them — accruing-and-leaving a canceled order `accrued` would over-count partner net commission. Encode that as a pure `shouldBackfillOrderFee(existingFee, orderStatus)` so the eligibility decision is unit-tested without the DB. When a registry job re-runs the same flow as a subscriber, reuse the SAME pure helpers (`resolvePartnerFeeRate`/`computeFee`/`findFeeForOrder`) so math/idempotency can't drift, and copy the closest existing job (`backfillPartnerOrderCurrencyJob`) wholesale for the partner↔order-link enumeration + dry-run/apply/errors[] envelope.
- #522: stats panel compute is `resolvePanel` (modules/stats/resolver.ts) dispatching to visual-flows `operationRegistry` (modules/visual_flows/operations/), NOT a stats-local executor — metric/series logic lives in aggregate-data.ts/time-series.ts. seed-stats-dashboards.ts SKIPS existing dashboards, so editing a panel def there never retro-patches live DB rows.
- UI-slice gotcha: a freshly-merged module's table may be ABSENT from the local dev DB → the route 500s with `TableNotFoundException` (not the UI's fault). Run `cd apps/backend && npx medusa db:migrate` (additive, safe while `yarn dev` runs) before Playwright-verifying, then seed sample rows via `npx medusa exec` resolving the module service. Before writing "order-test coverage", grep existing integration specs — #541's accrual/reversal/read-API were already fully covered; don't add redundant tests.
- Analysis chunks: fan out 3 parallel Explore agents (module-audit A, module-audit B, pipeline-trace+read-existing-docs) in ONE message → synthesize into one build-ready doc. Cheaper + faster than serial grepping; the repo already had ~25 ANALYTICS_*.md docs the tracer agent summarized instead of re-deriving.
- Branch off origin/main while a docs/feature commit sits on your current branch: `git checkout -b new origin/main` leaves the committed work behind on the old branch and carries only the uncommitted working-tree changes — clean way to peel a new slice off a stale branch without stash/reset (both forbidden).
- Adding an idempotency key to an existing model: edit the model.define + add an `.indexes([{on:["event_id"]}])` entry AND hand-write a separate `ALTER TABLE ... add column if not exists` migration (a new timestamped Migration file) — never edit the original create-table migration (won't re-run on prod). Mirror the analytics_session utm migration exactly (partial index `WHERE deleted_at IS NULL`).
- For a public-but-machine-authed endpoint (edge worker → backend), set `AUTHENTICATE = false` and gate inside the handler with a pure timing-safe `verifyIngestAuth` helper (shared-secret header OR HMAC over raw body). Return 401 on bad creds — unlike the public `/track` route which always 200s for a browser client. Keep the auth logic pure so it's unit-testable without booting Medusa.

- **HMAC-signed POST routes need `bodyParser: { preserveRawBody: true }` in middlewares.ts** — Medusa's default JSON parser discards raw bytes; verifying HMAC over `JSON.stringify(req.body)` is fragile (key order/whitespace drift vs the signer). Add a per-route matcher (mirror the WhatsApp webhook near the `/web/*` CORS entry); `req.rawBody`=Buffer, `req.body` stays parsed. Matchers compose, so a wildcard CORS entry still applies.
- **A daemon branch may already be MERGED to origin/main by the next chunk** (#547→main mid-session). After `git fetch`, run `git merge-base --is-ancestor <parent> origin/main` before assuming you must "stack" on a prior PR — if its commit is on main, branch off main independently.
- Volume/sizing analysis chunks can use REAL prod data headlessly: `aws ssm get-parameter --name /jyt/prod/ADMIN_OPENAPI_CATALOG_TOKEN --with-decryption --region us-east-1` → Basic-auth (`base64(token:)`) against `https://v3.jaalyantra.com/admin/*`. Beats guessing — turned #344 slice-5 from a weak estimate into a hard KV-vs-Queues decision (~249 events/day, 9 sites). `/admin/analytics-events/stats?website_id=&days=` overview gives total_events; `/timeseries` came back empty.
- When a queue item says "needs $or $ilike vs in-app-filter decision", check existing admin routes first: the codebase standardizes on DB-level `$or`/`$ilike` for `q` search (admin/persons/partner, google-merchant). In-app filtering is only the #484 workaround for routes where `q` can't reach the query layer. DB-level keeps count/pagination correct — prefer it; it's a mirror, not a decision.
- **SSE/long-lived stream routes aren't HTTP-integration-testable** (the shared runner can't drive a timer-driven `text/event-stream`). When a slice touches one, extract the pure aggregation into a sibling `lib.ts` (`computeLiveStats`-style) and unit-test THAT; the stream/interval wiring is mechanical (mirror the existing heartbeat). Multi-instance count correctness on Fargate is best fixed with a periodic **DB-poll re-push** of an authoritative snapshot (no Redis/DO needed at low volume) — reuse a message `type` the UI already renders so it's a backend-only, zero-UI-change, reversible fix.
- A "patch live DB rows the seed/code-fix can't retro-apply" follow-up is best shipped as a #457-style guarded maintenance job (dry-run/apply + pure idempotent diff helper), not a one-off curl/ECS script — it's unit-testable, registry-only (low conflict), and runnable from the Data Plumbing console. Pattern reused for #522 stats-panel window backfill (PR #552).
- For an analytics "granular breakdown / filtered aggregate" slice, mirror the existing `get-analytics-stats.ts` fetch-then-aggregate shape: workflow fetches events scoped by website+timestamp range, then delegates ALL slicing/filtering to a framework-free `*-lib.ts` (pure fns over plain rows) so it's unit-testable without booting Medusa. Keep the dimension list + null-normalization (referrer→"direct", country→"unknown", is_404→bool-string) in the lib; route just validates + maps query → workflow input. Adding a dimension = one array + one `select[]` entry. New additive route (no middleware/registry edit) → branch off origin/main, zero conflict.
- Analytics integration tests seed deterministically via `container.resolve("custom_analytics").createAnalyticsEvents([...])` (auto-gen MedusaService method) rather than POSTing /track — avoids UA-parse/async-session timing flakiness; then hit the read endpoint with `adminHeaders`. The CREATE INDEX CONCURRENTLY deadlock console.error at boot is harmless shared-DB noise, not a failure.

- **`apps/analytics/dist/` is gitignored** — the client tracker's `analytics.min.js` is a CDN build artifact (regenerated on deploy via `apps/analytics/scripts/build.js` + terser). Edit only `apps/analytics/src/analytics.js`; rebuild locally to verify but never commit dist (git add will reject it as ignored). terser is already in `apps/analytics/node_modules`, build with `node scripts/build.js`.
- Admin-side react-query hooks are decoupled from server code (they just fetch a URL), so a "consume endpoint X" slice can branch off origin/main independently even when X's PR is still unmerged — the hook compiles regardless. Put the query-string builder + response types in a PURE framework-free file (no React/sdk imports) co-located with a `__tests__/*.unit.spec.ts`, so the headless daemon gets a real `TEST_TYPE=unit` test even though the page render is Playwright-gated. `src/admin` is tsconfig-excluded from `medusa build`, so typecheck the pure file standalone with `npx tsc --noEmit --skipLibCheck --strict <file>`.
- #569 S1: the website-analytics overview has NO website→analytics_session module link — pull session rows by resolving the `custom_analytics` module service and calling `listAnalyticsSessions({website_id, started_at:{$gte,$lte}})` (mirrors ad-planning bulk-resolve-attributions), and wrap it best-effort try/catch→zeroed metrics so the overview never 500s if sessions are unavailable.
- #569 S2: when a new ranked report aggregates SESSIONS (entry/exit page) rather than events, mirror `breakdown-lib.ts`'s envelope shape exactly but rename `total_events`→`total_sessions` and keep `{value,count,unique_visitors,percentage}` buckets — same pure-helper-+-best-effort-workflow-+-route pattern as S1, so the UI can reuse the ranked-bar component. A route returning BOTH dimensions by default (with an optional `?dimension=` scope) saves the UI a round-trip for paired panels; 400 on an explicitly-bad dimension, but treat absent/empty as "return all".
- The shared analytics limit-clamp idiom `Math.max(1, Math.min(MAX, Math.floor(n) || DEFAULT))` makes `limit=0` fall BACK to DEFAULT (0 is falsy), NOT clamp to 1 — only negative/over-cap values clamp. Mirror this when copying session-pages-lib/outbound-links-lib; don't write a unit test expecting limit=0→1 (#569 S5a).
- #569 S6: the analytics `breakdown` route has NO hardcoded dimension enum — it validates via `isBreakdownDimension`/`FILTERABLE_FIELDS` which derive from `BREAKDOWN_DIMENSIONS`. So adding a dimension is purely data: append to the array (server `breakdown-lib.ts` AND admin-mirror `analytics-breakdown-query.ts`, BOTH the type union and the array) + `AnalyticsEventRow` + workflow `select[]`; route + filters work with zero route edits. `referrer` (full URL) is a SEPARATE column/dimension from the parsed `referrer_source` — both exist on the analytics_event model. Add a `NULL_LABELS` entry (`referrer→"direct"`) so empty values fold like referrer_source.
- Bash cwd PERSISTS across tool calls — a second `cd apps/backend` (already inside it) silently fails and subsequent relative `cat` reads the wrong dir and reports "No such file" for files that exist. Use absolute paths or check `pwd` before chained cd.
- Prod admin API SSM token `/jyt/prod/ADMIN_OPENAPI_CATALOG_TOKEN` lives in region **us-east-1** (aws ssm without --region fails "must specify a region"; ap-south-1 → ParameterNotFound). Use it to read prod state headless, e.g. `curl -u "$TOKEN:" https://v3.jaalyantra.com/admin/email-templates?limit=100` — turns "verify X exists in prod" from a human-tail item into a checked fact. ⚠ a `fields=` projection on /admin/email-templates returns 0 rows; omit it.
- When auditing "does feature X actually work", check for the artifact that proves intent vs wiring: a DB email template that exists + is_active but whose subscriber is an empty stub = a real, unambiguous gap to fix (not a product decision). `partner-task-assigned` template was active in prod while `task-assigned.ts` did nothing (#332).
- The shared HTTP integration runner (`setupSharedTestSuite`/`medusaIntegrationTestRunner`) TRUNCATES the DB between EACH test (afterEach), but admin-user/auth survives. Seed prerequisite rows (email templates, etc.) in `beforeEach`, NOT `beforeAll` — a beforeAll seed is wiped after test 1, and the symptom is misleading: test 1 (which asserts the seeded row exists) passes, later tests fail with "not found". `POST /admin/email-templates` is `.strict()` → `template_type` is REQUIRED (400 without it).
- Email workflows that mirror `sendOrderConfirmationWorkflow` end with a bare `sendNotificationEmailStep(...)` and return NO `WorkflowResponse` → `.run()` gives `result === undefined`. Don't assert on `result`; assert `run.errors` is `[]` (completing without throwing = success).
- When mirroring an event-driven email send, check EVERY emit site for the event — payloads differ. `production_run.cancelled` carries `partner_id` from the partner-decline workflow but NOT from the admin cancel route; always resolve the missing field from the entity (e.g. `run.partner_id`) instead of trusting the event payload. (#576 slice B)
- For a synchronous ROUTE-triggered email (not an event subscriber), run the email workflow best-effort: `await wf(req.scope).run({ input, throwOnError:false })` inside a try/catch, then check `run.errors?.length` and log. `fetchEmailTemplateStep` THROWS on a missing template row, so without throwOnError:false a not-yet-authored prod template would 500 the request. Keep the existing side-effect (feed notification) as the source of truth and treat the email as additive. (#576 slice C, PR #580)
- Reuse an existing workflow's logic inside another workflow's STEP via `someWorkflow(container).run({ input })` — there is NO `step.invoke` API; the repo-wide pattern is workflow-in-step (grep confirmed in designs/regions/production-runs). (#581 S1)
- When a slice STACKS only because it imports a *type* from the prior slice's lib, prefer re-implementing tiny pure helpers locally over importing from a directory scheduled for deletion (e.g. `workflows/email/lib/` under #578). The digest-email lib (#581 S2) re-implements `derivePartnerFromEmail` in 4 lines rather than importing it, so the slice has zero coupling to a dissolving tree — and the stack stays minimal (type-only import of the digest shape). (#581 S2, PR #583)
- A visual-flow op that wraps an OTHER PR's workflow (imports its function/types) MUST stack on THAT PR's branch, not main — even if the op only "runs the workflow at runtime", the `import` makes it a compile-time dep (S3 #584 had to branch off S1 #582, not S2 #583). Check what you actually `import` before deciding the PR base.
- Editor-gated visual-flow SEED scripts can't be live-verified headless — export the static `FLOW_DEF` and write a small structural unit test (`__tests__/<seed>.unit.spec.ts`, TEST_TYPE=unit) that PINS the op/workflow STRING refs (`operation_type`, `bulk_trigger_workflow.workflow_name`, `items`/`input_template`) + the linear graph. A typo'd string ref silently no-ops at runtime, so this is the only guard that catches it.
- A seed that references ops/workflows by STRING name only (no `import`) can branch off main INDEPENDENTLY even when the ops/workflows live on unmerged stacked branches — it just won't run until they merge+deploy. Seed it as `draft`.
- Recipient/eligibility filters on a fan-out op: filter the op's OUTPUT array (reusing the per-item compute's already-resolved signal, e.g. digest.website) rather than re-querying the source pivot at selection time — single source of truth, pure + unit-testable, no duplicated join logic. (#589/PR#590)
- Prod email templates are authored via admin API and are NOT re-seeded on deploy — a seed `html_content` edit only lands in fresh envs; an active template needs a manual admin-API `html_content` PATCH post-merge. Note this in the PR/handoff for any email-template change.
- A later slice that edits the SAME files as a still-open prior slice's PR MUST stack on that PR's branch (set base to it), not main — branching off main silently drops the prior slice's work (it's not in main yet) AND textually conflicts on merge. Symptom while editing: a function you expect (e.g. slice-2's `digestHasData`) is missing on your fresh-off-main branch. Fix: `git checkout <prior-branch>` (uncommitted edits on files the prior branch doesn't touch carry over cleanly) then branch off it. (#589 slice 3 / PR #592 on #591)
- ⚠ DON'T emit literal control-char ranges in regex/strings via Edit/Write — typing a `[<NUL>-<US><DEL>]` class lands REAL control bytes (incl. NUL) in the source, which breaks tooling (`python3` "source cannot contain null bytes", grep, splits string literals across lines). Always write `\uXXXX` escapes: `/[ -]+/g`, `"ab"`. If bytes already leaked, fix byte-level with a Python script (read `rb`, `.replace(bytes([...]), b"...")`, write `wb`) — the Edit tool can't match the corrupted bytes.
- Live model-call wiring you can't verify headless can still ship as a real BUILD: split the deterministic prompt-builder + an orchestration that takes the model call as an INJECTED seam (`type Generate = (args)=>Promise<string>`), unit-test with a fake generate, and gate the real OpenRouter-backed call behind a default-OFF op option (mirror `operations/ai-extract.ts`). Default-off = zero live behaviour change; flipping the flag becomes the only interactive step. (#589 item3, PR #593)
- #498: a design's production runs come back as parent + one child per partner assignment (children share design_id; parent.quantity already = Σchildren) — to sum/aggregate a design's runs, count LEAF runs only (not referenced as another run's parent_run_id), never naive reduce over the whole list.
- Parity "audit" chunks often turn out to be partial-build chunks: before assuming a partner route is missing, `ls apps/backend/src/api/partners/stores/[id]/<module>/` — Sales Channel was already built. Audit the existing route + apply only the consumer-safe wire fixes (e.g. DELETE `object` string), and DEFER behavior-changing drifts (status codes, scope, linking) that hinge on an open modeling decision. Grep `apps/partner-ui` source (not built `.next`) to prove a wire fix is consumer-safe.
- Parity-audit `object` envelope: admin's DELETE `object` casing is per-module, NOT uniformly kebab — sales-channel uses `"sales-channel"` but shipping-option uses snake_case `"shipping_option"`. Always grep the admin `route.js` DELETE before "fixing" the partner envelope.
- When two independent parity-audit PRs both append to the shared `PARTNER_API_PARITY.md`, `git checkout -b <new> origin/main` ABORTS (local doc differs from main). Stack the new branch off the prior audit's branch instead (sanctioned shared-file STACK) — base the PR on it and merge parent-first.
- Delegating a unit spec to the free drafter works cleanly (KEPT as-is) when the prompt is fully self-contained: function signature + field types + one `it()` per case + **precomputed exact expected outputs** + an instruction to `as any`-cast type-mismatched inputs. Precomputing the numbers yourself makes you a fast, precise verifier and leaves the drafter zero judgement calls. (#594, PR #598)
- The free-drafter unit-spec recipe replicates across helpers: 2nd run (`formatDuration`, #594 chunk2) also KEPT as-is, zero edits. Running tally 2/2 kept. For numeric formatters, the highest-value untested branches to specify are rounding boundaries that CROSS a unit (`59.6 → "1m"`), exact-boundary (`60 → "1m"`, no seconds), negative/NaN clamps, and "no higher unit" cases (`3600 → "60m"`, not `1h`). Trace these by hand and hand the drafter the exact strings.
- To DB-resolve an AI provider for any visual-flow op (instead of hardcoding OPENROUTER_API_KEY), reuse `getAiPlatformForRole(container, role)` + `buildChatModel(cfg, modelOverride)` from `src/mastra/services/ai-platforms.ts`; `operations/ai-extract-platform.ts` is the canonical reference impl. Resolve the platform ONCE before any per-item loop, and on null fall back to leaving the field unset (best-effort) rather than throwing — matches the digest's continue_on_error semantics (#589 item4, PR #610).
- UI-bug diagnosis: drive Playwright with a `page.on('console', m=>m.type()==='error' && errors.push(m.text()))` collector BEFORE guessing root cause. The #568 "edit is broken" turned out to be a `useRouteModal must be used within a RouteModalProvider` crash visible only in the console — the backend PUT was fine. Console-error capture turns a vague "broken" into the exact line in one run.
- Before adding an admin route, `ls apps/backend/src/api/admin/` for a near-name (singular vs plural): an `exchange-rate` already existed when the task said create `exchange-rates`. Reuse its underlying helper (here `fetchExchangeRate`) instead of re-implementing the upstream fetch+cache.
- Switching branches mid-chunk reverts working-tree files the dev server serves — verify each PR's UI change while ITS branch is checked out, not after switching. (Shipped #558 verify, then switched to #501's branch which correctly showed the file back at z-50.)
- When the run-assigned tasks turn out already-shipped by a prior chunk (check `gh issue view 352` pointer + `gh pr list` FIRST), pick the next buildable backlog item rather than redoing them — and prefer a clean `apps/backend` slice (#449 new visual-flow op) over a risky out-of-tree port (#521 storefront-starter submodule, which can't use the backend test harness).
- When run-assigned tasks are already shipped (check #352 pointer + `gh pr list`), don't redo them — jump straight to the next buildable Wave item from the latest session pointer. Saved a full re-derive this chunk (#558/#501/#568 were #612/#614/#615).
- Email-template "add more templates" tasks (#450): grep `template_key` across `src/scripts/*.ts` FIRST — decided lists go stale, several "missing" templates (order-confirmation→order-placed, shipment→order-shipment-created, payment→payment-captured) often already ship. Add only net-new keys; assert no-collision in the unit test.
- Shared integration runner: two `it`s in one spec with a TRUNCATE between them can hit the @medusajs/index CREATE INDEX CONCURRENTLY-vs-TRUNCATE deadlock even per-file. Merge related assertions into ONE `it` to minimise truncate cycles (fixed a flaky #452 spec).
- For a "create a DB record + email" touchpoint, put the load-bearing linkage (e.g. order_id) in a TYPED nullable column via an add-column-if-not-exists migration, NOT metadata — it survives the later metadata-replacing update and gives a clean idempotency key. Keep the email send conditional via `when(has recipient).then(...)` + best-effort try/catch in the subscriber.
- Playwright admin login (form, :9000/app): after `click('button[type=submit]')` do NOT `waitForURL('**/app**')` — the login page is itself under `/app`, so it returns instantly without auth (all subsequent API calls 401). Instead `waitForLoadState('networkidle')` then `waitForFunction(() => !location.pathname.endsWith('/login'))`. Creds `daemon2@jyt.local`/`Pw123456!`. Playwright pkg lives in `.claude/skills/playwright-skill/node_modules`; browsers cached in `~/Library/Caches/ms-playwright` (no install needed). `timeout`/`gtimeout` not on macOS PATH — don't wrap commands in `timeout`.
- Backend+migration UI slices CAN be live-verified without restarting the user's dev server: run `npx medusa db:migrate` (additive new table is safe) — the running Medusa dev server hot-reloads new model relations + module-model registration on file save, so the create/read round-trip works against :9000 immediately.
- AI External Platforms store provider_type in `metadata.provider_type` and the model in `api_config.default_model` — NOT `api_config.provider` (that's for social/email/sms). The detail General section's `providerType` var only covers non-AI categories; read metadata for AI-specific banners. (#613)
- `verify-analysis.mjs` FAILs (exit 1) on PROPOSED new files cited in a PR-plan section — that's expected, not a hallucination. Treat the failure as a checklist: confirm the EXISTING-code citations pass, label proposed files "(proposed)" in-doc, and note it in the PR body. Always hand-spot-check a few load-bearing `path:symbol` line numbers too (the free model gets the file right but sometimes the line number wrong).
- Before building a #569 (or any "S<n>" sliced) slice, GREP for the artifact first — many backend slices were already built/merged by prior chunks (S1/S2/S5a/S6/S7a were all done); don't rebuild. The issue's checkbox list lags the code.
- Adding a value to a shared discriminated-union type (e.g. a breakdown `dimension`) can break a `Record<Union,...>` map in an UNRELATED admin file with TS2741 — but admin is tsconfig-excluded so `medusa build`/PR-CI won't catch it. Scoped-tsc the touched component to surface it, and fix the label map in the same PR.
- Playwright admin login race: after `page.click(submit)` on `/app/login`, `waitForURL('**/app**')` matches even when the session cookie isn't set yet (next nav 401s "Unauthorized"). Wait for the actual landing route (`**/app/orders**`) before navigating to a deep route.
- A `data-testid` reused by both a drawer form AND the page behind it makes a Playwright `count()` match >1 surface (bit the #628 CF-warning check). Scope the locator to the drawer/dialog container, or assert on the field's sibling.
- To prove "zero NEW tsc errors" on a file that already has pre-existing type-identity errors: back up your version to /tmp, `git show HEAD:<path> > <path>`, run scoped `tsc`, count, then restore from /tmp (in-place — a renamed-copy doesn't reproduce react-hook-form TS2719 identity errors).
- **Playwright via skill module is CJS:** `import pkg from '.../playwright-skill/node_modules/playwright/index.js'; const {chromium}=pkg;` — named `import {chromium}` throws "Named export not found". (chunk 3, #569 S3)
- When a chunk prompt lists "analysis docs" as tasks, FIRST `gh pr list` for `docs(#NNN)` branches — prior chunks may have already shipped them as OPEN PRs (Wave-D #626/#627/#629). Don't re-author; just note "already open, human review pending" in the handoff and spend tokens on the real remaining build.
- #569 UI render slices are self-contained sibling cards (`analytics-<feature>-card.tsx`) imported into `website-analytics-modal.tsx` — mirror the existing `analytics-top-404-card.tsx` (inline ranked bars, Medusa tokens, Skeleton, empty state) rather than editing RankedBarList; keeps the central-file diff to a 2-line import + insert.

- **#569 UI cards: seed `link_out`/custom analytics events via PUBLIC `/web/analytics/track`** (admin POST has no path for arbitrary event_name) — `{event_type:"custom_event",event_name:"link_out",metadata:{href}}`, ~2s before it lands (batch-ingest buffer). Same trick used for S4 404 seeding.
- Server-side paginated admin DataTable: mirror `src/admin/components/forms/form-responses-section.tsx` — `useState<DataTablePaginationState>`, `offset=pageIndex*pageSize`, hook with `keepPreviousData`, pass `rowCount={count}`+`pagination:{state,onPaginationChange}` to `useDataTable`. The `<DataTable.Pagination/>` then drives the server fetch automatically. (#569 S7b, PR #636)
- Medusa workflow step Date inputs come back JSON-serialized as ISO strings — `new Date(input.date)` at the top before any `<=`/loop, or comparisons silently become `Date <= string` → NaN → empty results (bit #569 timeseries: 0 buckets). Wiring the first UI consumer of an endpoint is the cheapest way to flush these latent `res.body`/date-serialization bugs.
- Order-flow integration tests: `medusaIntegrationTestRunner` does NOT keep a beforeAll-created ORDER visible across separate `it` blocks (later `it` → "Order not found"). Build the order in beforeAll and assert everything in ONE `it`, or rebuild per test. (Bit shiprocket-rates.spec.ts.)
- Throwaway admin user for Playwright when `npx medusa user` says "already exists" and you can't reset the password: write a one-off `medusa exec` script creating the user + an emailpass auth identity with `Scrypt.kdf(pw,{logN:15,r:8,p:1}).toString("base64")` (mirror integration-tests/helpers/create-admin-user.ts), then `POST /auth/user/emailpass`.
- Shiprocket has no sandbox → stub `global.fetch` (bind real fetch first, pass non-shiprocket URLs through), mock `/auth/login` + `/settings/company/pickup` + `/courier/serviceability/`. Template: shiprocket-pickup-registration.spec.ts.
- lib/ dissolution refactor (#578): after `git mv`, `git add`-ing the OLD (now-nonexistent) paths aborts the whole `git add` before the modified importers stage — stage renames via `git mv` and add ONLY the modified files. Always grep `integration-tests/` too (it imports `src/.../lib/...`), and fix moved files' own `../parent` imports (they're one level shallower at module root). tsc baseline for apps/backend = 69 errors (0-new gate).

- **lib→*-lib.ts sibling moves are zero-churn when depth is equal** (#578 email slice): `workflows/email/lib/X.ts` → `workflows/email/workflows/X-lib.ts` are both 1 level under `email/`, so moved files' OWN relative imports stay valid — only the *importers* change. Confirm equal depth before assuming you must rewrite internal imports. Integration specs live under `apps/backend/integration-tests/` (NOT repo root) — grep there too.
- A long-running `medusa develop` server (check `ps aux | grep develop`) holds DB locks that DEADLOCK the shared test-harness TRUNCATE — integration specs fail deterministically with "deadlock detected" on EVERY run, not a flake. Don't kill the user's dev server; rely on unit tests locally and let the integration spec run in CI. Note it in the handoff.
- A `model.bigNumber()` column requires a companion `raw_<col>` jsonb column in the hand-written migration (it stores the full-precision value, like `raw_estimated_cost`). Omitting it passes tsc + snapshot but FAILS at runtime: `column "raw_<col>" does not exist` on the first create/update. Always add both `numeric` and `raw_* jsonb` ALTERs for a bigNumber field.
- Daemon workspace collision is REAL and silent: a concurrent chunk can `git checkout -b <otherbranch>` between your branch-create and your `git commit`, so your commit lands on the wrong branch. After committing, ALWAYS verify `git rev-parse --abbrev-ref HEAD` is your branch before pushing. Recover ref-only (never touches working tree / untracked files): `git branch -f <yourbranch> <yourcommit>; git checkout <yourbranch>; git branch -f <hijacked-branch> <its-prior-sha-from-reflog>`. reflog shows the prior SHA.

- **Daemon branch-switch collision recovery (commit landed on wrong branch):** if a concurrent chunk switches branches mid-flight and your `git commit` attaches to the wrong branch, DON'T checkout/reset (collides with the live chunk). Recover with pure plumbing that never touches HEAD/worktree: `export GIT_INDEX_FILE=/tmp/idx; git read-tree origin/main; git diff <C>^ <C> | git apply --cached -; TREE=$(git write-tree); NEW=$(git log -1 --format=%B <C> | git commit-tree $TREE -p origin/main); git push origin $NEW:refs/heads/<mybranch> --force-with-lease=...:<old-remote-sha>`. Verify `git diff --name-only origin/main $NEW` = only your files first. Leave the polluted local branch alone if its remote is already clean.
- **#348 env rename:** slice B standardized platform tax-ID env on `PLATFORM_TAX_ID_JYT`/`PLATFORM_TAX_ID_KHT` (slice A used `JYT_PLATFORM_TAX_ID`); the decision-doc names won.

- Shared HTTP test harness TRUNCATEs every table (incl. custom-module tables) BEFORE each test — migration-seeded rows do NOT survive into a test. Seed via the module service INSIDE each `it()` (beforeAll/beforeEach get wiped). Confirm by grepping the test's TRUNCATE error for your table name.
- A custom module is NOT in the remote-query index, so `query.graph({entity:"<custom_module_table>"})` silently returns [] (caught → empty). Load custom-module rows via `container.resolve(MODULE).list<Plural>()` instead. Also: a boolean `{is_active:true}` list filter resolved unreliably across container scopes — list all + filter in a pure helper.
- `model.array()` (Medusa 2.12.5 DML) maps to a Postgres `text[]` column; pair with a hand-written `"col" text[] not null` in the create-table migration.
- When a Medusa update-workflow already spreads `...rest` into `updateX()` (e.g. updateDesignStep), exposing a NEW typed column via API needs only a route + validator — no workflow/step change. Verify by reading the step's destructure before assuming you must edit it.
- Before building a "remaining scope" item, AUDIT the live code first — #349's issue thread claimed JSON-LD was remaining, but Product/Breadcrumb/Org/WebSite JSON-LD were all already shipped & rendering. Stale issue scope wastes a chunk if you trust it blindly; grep the target surface for the feature before planning a build.
- Watch shell CWD when exploring git submodules: `cd apps/storefront-starter` then `git rev-parse --show-toplevel` returns the SUBMODULE root, and relative `cd apps/...` compounds wrong. Use absolute paths after touching a submodule, and never `git add` a drifted submodule pointer.

- **Migration class names must be globally unique across ALL modules.** Medusa tracks executed module migrations by class name in a tracking table shared across modules; two modules with the same `MigrationYYYYMMDDHHMMSS` → the 2nd (alphabetically-later module) is silently SKIPPED as "up-to-date" and its columns never land (incl. prod). Bit #348-A (partner) vs #604-A (designs), both `Migration20260622120000` → `partner.tax_id` never landed anywhere (PR #661 renamed partner→...120001). When hand-writing an ALTER migration on the same day as another, bump the timestamp seconds.
- **When an integration test fails on a "column does not exist" for a recently-merged column, isolate migration-vs-code FIRST:** `psql -c 'create database tmp'; DATABASE_URL=postgres://postgres@localhost:5432/tmp npx medusa db:migrate` then inspect `information_schema.columns`. If the column is missing after a REAL CLI migrate, it's a migration bug (e.g. name collision), not your code or the test harness. Avoids hours chasing the test-runner's DB reuse / stale `.medusa/server`.
- Before an analysis doc claims a feature is "missing", grep the module for it — #649's "current state" listed the carrier abstraction as TODO but the normalized interface + resolver + 6 adapters + a calculatePrice→getRates checkout bridge already shipped. The real analysis value is reframing "build X" into "X exists; the gap is config-scoping/surfacing X".
- Analysis-mode specs are far more useful when they cite a concrete *reference file to mirror* per artifact (e.g. "new module = copy `modules/ops_audit/{index,service}.ts`; `model.define` w/ json+indexes = `analytics-daily-stats.ts`") than when they describe the pattern abstractly — the future build chunk jumps straight to a real file instead of re-deriving. Grep-verify every reference path before shipping the doc and add a "real-path appendix" table.
- When a slice has an open *product* decision, check whether it actually gates THIS slice before deferring the whole thing: #659's "One Goal" decision turned out to only pick a `metric_key` *value*, not the snapshot column shape — so the foundation slice is buildable now with a goal-agnostic schema. Surface the decision but don't let it block a slice it doesn't actually constrain.
- Hallucination-guard Layer B must validate on the RAW LLM output (placeholders still `{TOKEN}`), NOT the post-substitution text — otherwise legit placeholder-derived display numbers (e.g. ₹1,84,320) get re-extracted and flagged as stray. (#659 slice 2 PR-1)
- Number-suffix regex: require the K/L/M/Cr suffix IMMEDIATELY adjacent (no `\s?` before it) so "5 Killer ideas" parses as 5, not 5000. (#659 guard lib)

- #659 slice-2 PR-2: the guard extracts numeric literals from RAW output INCLUDING digits inside `{TOKEN}` placeholder names — so any generated metric_key with a digit (e.g. `q3_gmv`→`{Q3_GMV}`) trips Layer B as a stray "3". Keep metric_keys digit-free end-to-end (snapshot job + tests).
- Make the AI call an injectable fn param on a plain async core function (not a Medusa step), with the Medusa workflow wrapper injecting the real call — lets the integration test stub the LLM directly (CI never hits a live model) without fighting workflow-step DI.
- Medusa workflow `result` type is the UNION of every `StepResponse` first-arg shape; if one branch returns an inline literal missing a field other branches have (e.g. `reason`), `result.reason` fails to typecheck downstream. Fix: type the literal as the shared result type (`const ok: SendIdeasEmailResult = {...}; return new StepResponse(ok)`) so every branch is the same shape.
- Base `medusa-config.ts` registers the `local` notification provider for channels `["feed","email"]` — so integration tests CAN assert a real `notification` row for channel "email" (Resend's real send only lives in `.prod.ts`/`.dev.ts`). Assert the row, not delivery.
- Shared HTTP integration suite (`test:integration:http:shared`) does NOT retain module-service-seeded rows across `it()`s — only the FIRST test in a suite sees `beforeAll`-seeded data; later tests get 0 rows. Seed fixtures in `beforeEach` (use `>=` assertions so re-seed dupes don't break). There's no explicit afterEach truncation in `integration-tests/setup.js`; treat per-test isolation as the default for module-created rows.
- In unit specs, `[[], null, undefined] as const` makes `[]` a `readonly []` that fails to assign to a `T[] | null | undefined` param (TS2345). Use a typed `const cases: Array<T[]|null|undefined> = [...]` loop instead of `as const`.
- When a cron computes a "daily" business metric and runs early-AM, compute for the LAST COMPLETE day (yesterday in business TZ), not "today" — matches analytics_daily_stats and avoids a near-zero partial-day number. Derive the business-day boundary explicitly (cron is server-TZ=UTC in prod).
- Defer risky multi-hop query.graph reads (partner→store→sales_channel→order) rather than guessing the join shape headless — they need a live `medusa exec --dry-run`. A goal-agnostic snapshot schema lets you ship the safe metrics now and add the multi-hop one later without a schema change.

- **#659 read routes (PR #678):** an admin read route that depends on a not-yet-merged optimisation (Redis `cache.ts` lives only on #677, not main) should branch off `origin/main` and read Postgres directly — the snapshot/rollup table IS the durable cache, so the route is correct + self-sufficient without the cache, and the Redis hot-path becomes a trivial fail-soft follow-up once the dep lands. Avoids a needless 3-deep PR stack.
- When a spec doc predates the merged code, always read the SHIPPED model before writing libs against it — the marketing_outreach model on main diverged from the slice-4 draft spec (campaign vs kind, bounce_unreliable vs bounce_reason, external_id vs provider_message_id). Align libs to the model, not the doc.
- Before stamping an "owner_user_id"/actor from a handoff note, CHECK the SHIPPED model has that column — drop it if absent rather than shoving load-bearing state into metadata (the marketing_outreach model had no owner col; PR-4c #680).
- An optional container dependency makes a maintenance job a safe scaffold: resolve a well-known key in try/catch (`container.resolve(KEY)` throws AwilixResolutionError when unregistered) and no-op when absent. Lets you ship the registry/ops-console/audit surface now and activate it later by registering the adapter — no speculative network code. (PR #681 outreach sync provider.)
- Pure-lib ↔ registry circular import is fine when the back-edge is type-only: sync-lib/diff-lib `import type { MaintenanceChange } from registry`, registry imports VALUES from sync-lib. Type imports erase at compile → no runtime cycle. (PR #681.)
- To convert a "daily cron" feature into an operator-editable VISUAL FLOW: do NOT keep a `src/jobs/*` cron with `config.schedule` (Medusa auto-registers it → double scheduler). Lift the orchestrator into a plain lib (NO `config`/default export) + wrap it in a visual_flows operation node, and seed a `status:'draft'` schedule-triggered flow. `run-scheduled-visual-flows.ts` fires it; cadence = `trigger_config.cron`, editable from canvas. (PR #684, #659.)
- A visual-flow operation must NEVER throw: keep the heavy logic in a never-throws orchestrator (swallow runner errors → summary) AND try/catch in `execute` for parse/config errors. Mirror an existing op (partner_analytics_digest) exactly. Test the orchestrator with INJECTED workflow stubs (no live LLM/email in CI).
- The workspace branch can be switched out from under you by a parallel daemon chunk (saw HEAD jump feat-branch→main mid-session). Re-run `git rev-parse HEAD`/`git branch --show-current` right before branching, and always create your feature branch explicitly off `origin/main`. (Daemon collision risk — #684.)

- Type-only imports across api subdirs: jest/babel STRIP them, so a wrong relative path still PASSES jest but FAILS `tsc`. Always `npx tsc --noEmit -p tsconfig.json` + grep your changed files after any new `import type`. (Bit chunk-1/3 of the #659 wave: `…/maintenance-jobs/__tests__/` → `admin/marketing/` is `../../../marketing/...`, but registry.ts itself uses `../../marketing/...`.)
- Stack same-registry-array slices: when ≥2 new #457 jobs land in one daemon run, each appends to `MAINTENANCE_JOBS[]` + imports near the top — branch each off the PRIOR slice's branch (base=that PR) so the shared-file edits append in sequence and never textually conflict. Note "stacked on #N (merge parent-first)" in the title/handoff.
- ad_planning CustomerScore from another module: read via the resolved MODULE SERVICE (`adPlanning.listCustomerScores({score_type})`), NOT query.graph — `person_id` is a plain text column, not a module link, so there's nothing to join. The registry already does person/ad_planning reads this way.
- Enum stored as a Postgres text CHECK (not a pg enum type): MikroORM names inline column checks `<table>_<column>_check`. To extend, hand-write an ALTER migration that `drop constraint if exists` + `add constraint ... check (col in (...))` with a matching `down()` — NEVER edit the original `create table if not exists` migration (won't re-run on existing/prod DBs). Confirm the value list lives in MANY places (model enum + zod validator + TS unions + UI options) — grep the literal (e.g. "Portfolio") to find them all and keep in sync.
- #697: cost-summary buckets consumption logs by the DIRECT `production_run_id` column (not a module link) — so a backfill job only needs to set that column + `design_id` on a `createConsumptionLogs` call; no remoteLink needed for the cost-summary or the design-level energy job to see it. For idempotency on a "create synthetic rows" backfill there's no `force` (would duplicate) — skip when the target already has ANY relevant row, checked in-JS via `.some()` over a `take:null` list (robust vs relying on a `$in` filter).
- Before prod-verifying a cost/rate-derived fix, confirm the load-bearing PRECONDITION data exists in prod (e.g. cost-summary energy total stays 0 unless an active `energy_electricity` energy_rate exists — the route has no rate fallback when log unit_cost is null). One `GET /admin/energy-rates` saved a wasted "fix didn't work" cycle.
- When the focus issue's code is already merged and only an ECS deploy stands between you and prod-verify (18–45m), DON'T block-watch and DON'T start a large unrelated issue fresh — re-verify the merged code is correct vs its consumer, confirm prod preconditions, and hand off prod-verify as a ready-to-run command. Cheaper + de-risks the next chunk.
- #699 UI verify: brew python is 3.14 and `venv`/`ensurepip` FAILS — use `uv venv /tmp/x --python 3.12 && uv pip install --python /tmp/x/bin/python playwright && /tmp/x/bin/python -m playwright install chromium` to get a working Playwright (the webapp-testing skill ships NO bundled playwright). Dev server already on :9000 hot-reloads admin after a branch checkout, so no `yarn dev` start needed — just drive it. Admin login: POST creds at `/app/login`, then `goto` the deep route. Read role-Select options via `[role='option']` after clicking the trigger by its current label text.
- Single-source a drifting UI enum: when a creator hardcodes an enum that a resolver/service union has already outgrown, factor the labelled set + pure form<->storage helpers into a sibling `*.ts` module the form imports, and switch the schema from `z.enum([...])` to `z.string()` + a `superRefine` (known-value OR a sentinel+slug). Lets operators add values with zero code change while keeping validation. Test the PURE helpers (resolve/round-trip/slug), not the React component.
- Backend code lives under `apps/backend/src/` (repo-root CLAUDE.md paths like `src/api/...` are relative to apps/backend). A `find src/...` from repo root returns nothing — prefix `apps/backend/`. Also `git ls-tree -r --name-only origin/main | grep <path>` confirms a file exists on main without checking it out.
- #701 logger waves: check the Medusa `Logger` interface BEFORE mechanically swapping — `.info/.warn/.debug` take a SINGLE string only, so `console.log(msg, {summaryObj})` won't compile as `logger.info(msg, obj)`; inline the object into the template string (`key=${v}`). Only `.error` accepts a 2nd arg (an `Error`). This is the one non-mechanical decision per call; everything else is a 1:1 transport swap. Verify with project `tsc` filtered to touched FILES (baseline has ~69 pre-existing errors; grep matches in integration-tests are spec-file import-extension noise, not your source).
- #701 wave 2 (non-admin api): the `cd apps/backend` inside Bash often FAILS with "no such file or directory" because the daemon shell CWD is ALREADY apps/backend (relative `src/api/...` paths just work) — don't fight it, drop the `cd`. To verify a logging-swap wave is complete + clean in one shot: `grep -rn 'console\.' src/api/<dirs> --include='*.ts' | grep -v __tests__` should be empty, then `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'src/api/<dir>/'` and eyeball that any hit is a file you did NOT touch (pre-existing baseline). For a pure helper whose container arg is OPTIONAL, use the global `import { logger } from "@medusajs/framework"` rather than threading a maybe-undefined container — facebook's leaf handlers (no req/scope) do this and compile clean even while top-level handlers shadow it with a local `req.scope.resolve` logger.
- macOS has NO `timeout(1)` binary — `timeout 300 npx tsc … | grep <file>` SILENTLY produces no output (the wrapper fails to exec, tsc never runs) and the empty grep reads as a FALSE "typecheck clean". Run `npx tsc` directly (no `timeout`/`gtimeout` wrapper) and gate on `grep -c 'error TS'`. Bit a #701 wave verify.
- delegate.mjs (free drafter) is RELIABLE for the high-repetition slice of a mechanical wave (e.g. 19 near-identical meta-ads route files: add import + handler-top `const logger` + console→logger swap) but its run can exit **1 / ok:false** purely from its OWN scope-tracker false-positives (saw a `pps/backend` path-typo + in-scope files listed under `out_of_scope`). The file edits were correct — VERIFY the diff + tsc, do NOT discard on exit code alone. Keep the judgement-heavy folding cases (objects→template) for yourself.
- #704 diagnosable fetch failures: undici (`fetch`, Node ≥18) throws a generic `TypeError: fetch failed` on network errors and the real reason is on `error.cause` (`{code,errno,syscall,address,port,hostname}`). A pure `describeFetchError(err,{url?,label?})` util that unwraps cause→`"<syscall> <code> <addr>:<port>"` (fallback `err.message`/`String(err)`) is the reusable fix; wrap only the THROWING network fetches (not the `!resp.ok` branch, which already has status+body). Respect return-`null` contracts (e.g. WhatsApp media graceful-degrade) — LOG the cause there instead of throwing. To populate a visual-flow execution-log `operation_id` (was null because canvas node ids ≠ DB ids), stash `db_operation_id: dbOp?.id` onto the `nodeIdToOperation` object at build time (the `dbOpByKey` map already exists) and read it in `executeSingleOperation`. `ContainerRegistrationKeys` is exported from `@medusajs/utils` too, not only `@medusajs/framework/utils`.
- Before "rebuild the stale committed bundle" tasks: check `git ls-files <dir>` and the `.gitignore` — if `dist/` is gitignored and built by a CI workflow (e.g. deploy-analytics.yml → R2/CDN), the local artifact mtime is irrelevant and committing it is WRONG. Prove what's actually deployed by `curl`-ing the live CDN URL and `diff`-ing vs a fresh local build; if byte-identical, the deploy is current and the bug is elsewhere (or not-a-bug). Saves opening a no-op/harmful PR.
- When a partner-UI "filters don't work" bug is reported, check the partner API route FIRST: it often hand-rolls a tiny filter allow-list (status/q) and drops the rest of the params the UI's query hook sends. The UI side is usually correct; the fix is backend (mirror admin's filterableFields). Confirm by diffing the UI query-hook param list vs what the route reads.

- Never write `*/` inside a JSDoc `/** ... */` block comment (e.g. `energy_*/labor`) — it closes the comment early and tsc explodes with cascading TS1434/TS1005 parse errors that look unrelated to the real line. Reword to `energy and labor`.
- Prod check-constraint drift (`create table if not exists` predating an enum widening) is INVISIBLE to `medusa db:generate` because model+snapshot already match — only the live DB is narrow. Fix is a hand-written drop-if-exists+re-add ALTER; an integration test is tautological (fresh test DB builds the wide constraint from the create-table migration) so skip it and document why.
- A WhatsApp/visual-flow "result field" (e.g. `admin_url`) is bound BY NAME in a DB-seeded message template (`{{ step.result.admin_url }}`); to change its meaning, change the VALUE the workflow computes and KEEP the field name — renaming breaks prod until re-seed.
- Logger waves: after delegating console.*→logger, ALWAYS re-grep each file — the free drafter can add the `logger` decl to a handler yet miss a console call in a *sibling* handler of the same file (happened in persons/[id]/contacts/route.ts:136). A per-file "uses logger w/o decl" grep won't catch it; only `tsc` (out-of-scope name) or a raw `console.` re-grep will. Exclude JSDoc-comment-trap files from the delegate `--files` set and hand-edit them.
- Mechanical edits (logger swaps etc.): the Edit tool requires a prior in-conversation Read of EACH file — grep line content alone isn't enough. Delegating the whole batch to delegate.mjs sidesteps your reads entirely; you then verify cheaply via `git diff` + `tsc` (input tokens) instead of editing 30 files by hand (output tokens). Free model (deepseek-flash) handled multi-line console.log(obj) and same-scope logger reuse correctly. delegate.mjs cd's to repo root — re-cd to apps/backend after; its last-result.json out_of_scope can show a 1-char-truncated path glitch.
- "Add an email template" tasks (#450): the order + partner lifecycle sets are COMPLETE (~45 templates). `grep -h template_key src/scripts/seed-*email*.ts | sort -u` BEFORE drafting — only re-engagement (win-back/back-in-stock/browse-abandonment/feedback-reminder) was genuinely missing. Templates being editable in admin is the quick win; only wire a trigger where a clean event/job already exists. `feedback-reminder` is the tractable one — detection lives entirely in the feedback module's own rows (status:"pending" + metadata.source:"post_delivery_request" + age), no new tracking infra; stamp `metadata.reminder_sent_at` for idempotency. back-in-stock has NO inventory-restock event/subscriber → template+TODO.
- `git checkout origin/main`-based branch can be BLOCKED by uncommitted edits to tracked daemon files (scripts/agent-daemon/CODEBASE_MAP.md / BEST_PRACTICES.md) when they differ between HEAD and origin/main. Stash/reset are forbidden (delete the daemon SDK). Fix: `git add` + `git commit` those two files on the CURRENT branch to park them (recoverable), THEN branch off origin/main cleanly. Use `git -c submodule.recurse=false checkout` to skip the apps/storefront-starter submodule churn.
- #457 seed jobs: dry-run-safety means you CANNOT reuse a seed's default fn (always writes, no dry-run). Instead `export` the seed's template/data array and reimplement an idempotent skip-existing loop over it in seed-jobs.ts. Keeps content single-sourced; planner stays pure + unit-testable. seed-jobs↔registry: import `type` only one way, value the other → no runtime cycle.
- Media randomiser as a default pool (#720/#452): when a feature needs "some images, whatever exists" use `listAllMediasWorkflow(req.scope).run({input:{filters:{is_public:true,file_type:"image"},config:{take,skip}}})` → `result.media_files`. ⚠ filter key is `file_type` (MediaFile column), NOT `type` — the workflow whitelists per-entity columns and silently drops `type`, so `GET /web/media`'s unscoped `filters.type` never actually filters. Reuse the existing pure seeded selector (`selectArtworkChoices`, FNV-1a→mulberry32) on top; only swap WHERE the pool comes from. Prefer null-as-"general pool" over a hardcoded album id default — a fresh admin won't have your curated album → empty UI.
- #450 re-engagement triggers: wire only where a HONEST signal already exists. `win-back` is tractable with ZERO new infra — list `has_account` customers (`Modules.CUSTOMER`) + their orders (`Modules.ORDER` `listOrders({customer_id:{$in}})`, mirrors `ad-planning/segments/build-segment.ts`), reduce to per-customer latest-order, select lapsed>N days, idempotent stamp `customer.metadata.winback_sent_at` with a COOLDOWN (not just presence) so a still-lapsed customer isn't re-mailed every cycle. Keep it INDEPENDENT of #659's churn `marketing_outreach` (different audience → merging double-sends). `back-in-stock` (no opt-in/notify-me model + no inventory-restock subscriber) and `browse-abandonment` (analytics is PII-free: `analytics_session.visitor_id` is a hashed fingerprint with NO customer-email link) have NO honest signal → design-doc them, don't half-wire a send.
- Doc-only parity-gate chunks (e.g. #622): read the live driver + EVERY operation executor + the target (compiler) comprehensively, then enumerate each behavior as a checkbox citing exact `path:line` + the test that proves parity — the value is grounding, not prose. Flag where the target type drops data the live path carries (CompiledNode lost position_x/y + db_operation_id) as explicit "gaps the rewrite MUST close".
- Validate copilot manifest YAML edits with node `require("js-yaml")` (no python yaml module locally); confirm the new key resolves under `variables:`. Non-secret prod env → `variables:` block, secrets → `secrets:` (SSM path).

- #460: inbound-email create emits NO event (Medusa auto-CRUD) — to react to new emails, use a scheduled job scanning `listInboundEmails` + a pure selector, not a subscriber. Drive downstream follow-up by emitting a custom event (`lead.followup_due`) that the existing visual-flow-event-trigger consumes — zero-template, zero-UI nudge.
- New Medusa module migrations don't need a generated `.snapshot-*.json` to RUN — the `Migration*.ts` is what `db:migrate` executes; snapshots are only for future `db:generate` diffing. Fresh-DB integration tests prove the hand-written migration runs. (#648 partner-onboarding-profile)
- UI Playwright verify when the skill's python lacks the `playwright` module and system python is 3.14 (ensurepip broken): make a venv from `python3.12` (`/tmp/pw_venv`), `pip install playwright` — chromium binaries are already cached in `~/Library/Caches/ms-playwright`, no `playwright install` needed. To verify a media/photo render against a near-empty local DB, seed one row via the admin API (`PUT /admin/inventory-items/{id}/rawmaterials/{rmId}` with `{rawMaterialData:{media:{files:[url]}}}`) so the screenshot shows a real image, not just the placeholder.
- query.graph relation `inventory_item.raw_materials` (belongsTo) returns a SINGLE object, not an array — normalize before iterating (`rms[Symbol.iterator] is not a function`). Same trap likely for other belongsTo relations fetched via `.*`.
- `/store/*` routes are gated by `x-publishable-api-key` (400 if missing) — integration tests must mint a publishable key + link it to the store's default sales channel, then pass it as a header. Symptom otherwise: 400 "Publishable API key required".
- partner-ui uses **vitest** (`npm run test` = `vitest --run`), NOT jest; specs in `src/lib/__tests__/*.spec.ts`, `import { describe, it, expect } from "vitest"`. Run one file: `cd apps/partner-ui && npx vitest --run <path>`. tsc: `npx tsc --noEmit -p tsconfig.json` then grep for changed-file names (repo has ~4 pre-existing errors in order-create-claim/exchange — filter them out).
- Media JSON blobs in this repo come in many shapes (`{files:[...]}`, `{files:[{url}]}`, raw arrays, single string/object). Canonical PROD shape is `{ files: ["url"] }`. Admin has `apps/backend/src/admin/lib/utils/first-media-url.ts`; partner-ui now has `apps/partner-ui/src/lib/first-media-url.ts` (firstMediaUrl + mediaUrls). Reuse these — don't write inline `m.url`-only extraction (it silently drops the `{files}` shape).
- Prod migration recorded-but-not-executed (cols missing despite migration on main, #722/#736): don't edit the original — write a NEW migration with a globally-unique class name (grep all modules to confirm; timestamps collide across modules) that re-issues the original up()'s idempotent DDL verbatim. Two in one week = flag systemic prod-migration issue.
- CI changed-spec runner uses `jest --runTestsByPath '<path>'`. A unit spec under a Next-style `[id]`/`[param]` dynamic-route dir → jest reads `[id]` as a REGEX CHAR CLASS → 0 matches → exit 1 = false-negative `test` job (#732). FIX: keep route under `[id]` but move the pure lib + its `__tests__/*.unit.spec.ts` to a NON-bracketed sibling dir (e.g. `<feature>-lib/`) and update the route import. RULE: never put unit specs under a `[param]` dir.
- Integration `*:shared` tests use ONE shared DB — NEVER launch multiple `pnpm test:integration:http:shared` runs concurrently; parallel runs deadlock on `@medusajs/index` CREATE INDEX CONCURRENTLY vs the per-test TRUNCATE, surfacing as every request returning AxiosError 500 (looks like a route bug but isn't). Kill stray `jest.js` procs (NOT agent-daemon) before re-running. After flaky 500s also `npx jest --clearCache`.
- When a Medusa route returns 500 with no server log in the shared runner: the 500 is often from `beforeEach` seeding (its api.post propagates as AxiosError on every test), not the route under test. Instrument the beforeEach call with `.catch(e=>console.log(e.response.status, JSON.stringify(e.response.data)))` to surface the real MikroORM/validation message (e.g. "Value for X.description is required").
- Confirm a new admin API route is live without auth: `curl -s -o /dev/null -w "%{http_code}" <url>` → 401 = registered, 404 = missing. `medusa develop` hot-reloads new route files on :9000.
- SEO canonical-host fixes belong in ENV (`NEXT_PUBLIC_BASE_URL`), not hardcoded — Next.js relative canonicals (`alternates.canonical` path) take the host from `metadataBase`/getBaseURL, so one env var flips canonical+sitemap+robots+JSON-LD together. Derive domain-consolidation 301s in `next.config.js` from that same env so the redirect is INERT until the operator sets it (can't break prod on merge, loop-safe via apex===host guard). (#734)
- For console→logger sweep chunks (#701), grep the worklist EXCLUDING comment lines first: `grep ... | grep -vE ":[0-9]+:\s*[\*/]"`. Many `console.*` hits in `src/api` routes are JSDoc example snippets (client `fetch().then(res=>console.log())` usage docs) — they are NOT runtime code and MUST NOT be converted. A whole "wave" can be a NO-OP once comments are filtered; verify against fresh origin/main, not the current branch. As of 2026-06-24 the ENTIRE `src/api/**` surface is done; remaining real-code is workflows 342, mastra 403, modules 119.

- When a route mutates a model field DIRECTLY (read-modify-write) instead of via its workflow, any workflow step that fires a derived side-effect (e.g. #739 thumbnail sync) is SILENTLY bypassed — replicate the side-effect in the route (reuse the pure decision helper, not the impure step) and make it best-effort try/catch so it never fails the primary write.
- Storefront (`apps/storefront`) vitest can't run in-daemon: vitest@4 needs vite≥6 but root hoists vite@5.4.21 (`ERR_PACKAGE_PATH_NOT_EXPORTED ./module-runner`). Verify pure-fn logic with `apps/storefront/node_modules/.bin/tsx` smoke-assert script (absolute import path; `import type` is erased so no alias resolver needed). `npm run build` exits before compiling unless `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_test` is set; with it you reach `✓ Compiled successfully` (prerender then fails on no-backend — that's the env, compile success is the signal).
